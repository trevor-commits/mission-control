# TODO

## Active Next Steps
If it's not here, it isn't remembered.
Capture the current goal plus the concrete dependency-ordered steps that are still open.
- Keep this section short, current, and ordered by impact/dependency.
- When this repo has a configured Linear workspace, each actionable item should carry the matching issue ID inline using the repo's real team prefix.
- Keep the matching `Linear Issue Ledger` entry current whenever an item's repo-side home or urgency changes.
- Put audit-created actionable execution items at the top of this section so audit follow-through is the next queue to execute.
- If the current chat creates or discovers more urgent execution-ready work than the existing queue reflects, persist and move that fresher work to the top of this section before handoff so the chat is not the only durable record.
- When a step is verified complete, move or summarize it in `## Completed` instead of deleting the history.
- Full-repo audit continuation (paused at Trevor's 2026-07-13 stop boundary): preserve the explicit provenance/completion supersession and resume only on a new Trevor direction. On resume, rerun the browser gate and complete the authoritative verifier, collect the first natural Delegation Audit schedule result, reconcile the fifth natural Morning Brief/operator evidence when it exists, and run a new independent closeout audit. The goal remains active; the stop-boundary verifier is interrupted/incomplete, not green or failed. | owner: Codex thread `019f59f8-bb9e-70c0-9497-a9686ea24154` | linear: repo-only.
- Cross-repo Delegation Audit natural-run proof (time-gated): source, tests, protected runtime, empty-state status, plist, and notification-free dry-run are repaired through global `27aa2e3`; the loaded job still reports `runs=0`, `last exit=(never exited)` because the first 08:45 schedule has not occurred. Keep the separate Nightly Review defer truthful. | owner: next bounded status pass after the natural schedule | linear: repo-only.
- ER-107 Morning Brief (highest, deterministic safety complete; elapsed/authorization proof open): the implementation is independently review-clean, contained by canonical `main`, and install/stamp/launchd verified without provider calibration. Four natural briefs (July 10–13) are durably recorded; the July 13 05:00 brief and 05:20 deadman both completed naturally with exit `0`. Remaining gates are the fifth natural morning, Trevor comprehension check-ins, a separately authorized privacy-screened provider calibration, and an explicit Outcome Extractor activation or deferral decision. | owner: Codex thread `019f59f8-bb9e-70c0-9497-a9686ea24154` | linear: repo-only; no Mission Control Linear team is configured.
- ER-089 usage-aware autonomous routing (high): design and implement provider-usage adapters only where safe credentials/sources exist, then expose the routing signal in Mission Control without inventing missing provider percentages. Blocker: Trevor-held provider auth for z.ai/OpenAI/GitHub if live usage is required. | owner: next Mission Control routing session | linear: self-contained until Linear is configured.
- ER-090 autonomous coding-hygiene loop (high): Trevor's 2026-07-11 autonomous-portfolio request approves the bounded executor direction: consume Mission Control truth, rank by leverage/harm, fix and verify in scoped owner repos, use independent challenge when warranted, and raise only genuine decisions/blockers. Persistent unattended scheduling remains dependency-gated—not approval-gated—until atomic lease/epoch/fencing and transactional receipt-outbox semantics prevent duplicate or stale chat execution. The first interactive governor cycle is tracked by global ER-118. | owner: Codex thread `019f51c1-5817-7872-a6ce-8b65428277ed` | linear: self-contained until Linear is configured.
- P14 end-of-day loose-ends robot (after ER-090/P12 boundaries): consume the new Open work ledger in dry-run first, auto-fix only mechanically safe items, and escalate decision items loudly without merging, force-pushing, deleting, or editing human docs. | owner: future Mission Control autonomy session | linear: self-contained until Linear is configured.

## Linear Issue Ledger
If it's not here, it isn't remembered.
Mirror every configured Linear issue here with the repo-side home that explains why it exists.
- Each entry should capture:
  - `issue`
  - `status`
  - `todo home`
  - `why this exists`
  - `origin source`
  - `last synced`
- If this repo is intentionally `repo-only` or no configured Linear workspace exists yet, keep an explicit note here instead of leaving the section absent.
- Current mode: repo-only. No Mission Control Linear team/prefix has been verified; Trevor has been asked whether to set one up. Do not invent issue IDs.

## Completed
If it's not here, it isn't remembered.
Preserve a durable completion trail for verified work instead of deleting it from active planning.
- 2026-07-13 | Global session lookup repair — implemented the documented multi-word `chat-source find` route, removed executable help backticks, preserved empty metadata fields, bounded Cursor indexing, and loaded global `34a9f77`; fixture/docs/governance gates pass and installed `find mission control` returned five exact sources under its 120-second bound.
- 2026-07-13 | Delegation Audit code/runtime repair — restored the protected runtime, replaced calendar-decaying fixtures, repaired empty-state status, passed `66/0` focused tests and a notification-free real dry-run, and landed `27aa2e3` on global main; first natural schedule proof remains active above.
- 2026-07-13 | Morning schedule and nightly truth producer repair — aligned every current 05:00/05:20 schedule surface, moved inactive Outcome Extractor retries to 04:40/04:47/04:54, tightened the nightly full-ingest SLA to 28 hours, installed/stamped a stable schema-8 chat-graph runtime at Mission `c66d7fb`, and repaired global Nightly Review `6f21f4f` so the local refresh completes before model-resource admission. A real kickstart advanced the marker and the normal Chats feed now reports SLA `100800` fresh; full record in `records/2026-07-12-full-repo-convergence-audit.md`.
- 2026-07-13 | Full-repo convergence re-audit — re-enumerated provider-native Mission Control sessions and current branches, found and closed the original ER-087 reviewer's only unlanded test-authority requirement with privacy-safe committed feeder-output/provider-record specimens exercised at the real dashboard-wrapper and chat-graph collector boundaries, reran the authoritative matrix (`21/21`, dashboard `67/0`, browser `253`), and converged through two challenger findings to exact `REVIEW-CLEAN` at `59814aa`; full addendum in `records/2026-07-12-full-repo-convergence-audit.md`.
- 2026-07-13 | Full-repo convergence audit and ER-134 desktop-first usability — reconciled every identified Mission Control session/branch, repaired all accepted independent and GitHub review findings, converged at repeated `21/21` full-suite passes plus dashboard `66/0`, ER-134 `50/0`, browser `253`, independent `REVIEW-CLEAN`, CodeRabbit approval, GitGuardian pass, and merged PR #9. Exact merge `c44e5c9` was installed/stamp-verified; menu-bar panel runs; redundant patch-contained branches/worktrees were removed; external elapsed/calibration gates remain honest.
- 2026-07-11 | Chat-graph full-ingest health truth — separated the 30-minute incremental catch-up cadence from the 30-hour nightly full-ingest SLA in `show` and `doctor`, made missing and future-dated full-ingest markers fail health, and locked healthy/stale/missing/skew plus explicit-override boundaries in hermetic regression tests; implementation `245fb31`, audit repair `dcf11c2`, and test hardening `9843607`; full record below.
- 2026-07-11 | Nightly repository-bundle health — registered the fail-closed semantic receipt, proved the repaired job green in the live Automation feed, and removed the unregistered blind spot through `4de0aef`; full record below.
- 2026-07-11 | Chat truth-layer lifecycle repair — strict same-cycle Git evidence, owner-fenced ingest locking, and bounded feeder cleanup landed as `e2f5d60`, independently review-clean and installed/live-proven through `4de0aef`; full record below.
- Going forward, prefer one-line entries in this shape: `YYYY-MM-DD | <issue-or-scope>: short title — landed as <SHA>; full record in Work Record Log YYYY-MM-DD`.
- 2026-07-11 | ER-109 direct deadman transport — replaced the production mobile-connect dependency with a locked, Keychain-capable stdlib Telegram sender and 14 focused security/transport tests; landed on the activation branch as `b8e9cbe`; full record below.
- 2026-07-11 | ER-109 `faad4dbe` final-gate repair — strict feed/stamp wire validation, non-empty required inputs, immutable/serialized install provenance, adjacent installed imports, checked plist deployment, direct deadman transport, and unsupported-install-argument prevention landed through code commit `00da629`; immutable evidence SHA `1261c48` passed separate code/runtime and records/provenance audits with no P0-P3 findings, was fast-forwarded to `origin/main`, and its superseded branches/worktrees were removed; full record below.
- 2026-07-11 | ER-109 Morning Brief merge-gate round 8 — installer exact-set + stamp-write fail-closed hardening, extreme epoch handling, and nested full-ingest unknown surfacing; full record below.
- 2026-07-11 | ER-109 Morning Brief merge-gate round 7 (governor handshake `5cf8866d`) — closed the exact-midnight expiry hole the governor confirmed on the post-round-6 tree (`0a4b62f`): `valid_until` was only a "stay fresh until" window, so at `now == valid_until` the `now < valid_until` fresh check was false and the verdict fell through to the poll-cadence age ladder — a brief composed 23:59 (valid to next midnight) read `fresh` AT midnight and `aging` at 00:05 instead of `stale`. Fix: a present `valid_until` is now a HARD expiry on BOTH the Python (`mission_control_common.feed_health`) and rendered-JS (`dashboard/index.html` `guard`) sides — `now >= valid_until` ⇒ `state=stale`, `red=True` immediately, inserted before the fresh-window branch so the existing far-future/47h/30h horizon negatives (all `now < valid_until`) fall through unchanged. Before/after synthetic at the three boundaries: 23:59:30 `fresh`→`fresh`; exact 00:00 `fresh`→`stale`; 00:05 `aging`→`stale`. One regression per boundary on each side (`mission-control-common.test.sh` noon/next-midnight-anchored triple; `dashboard-render-smoke.js` same three via a pinned `Date` proxy). Landed as `e380d65`; reinstalled from that SHA (stamp `head`, verify ok, installed `index.html` + `mission_control_common.py` byte-identical to HEAD); full record below.
- 2026-07-11 | ER-109 Morning Brief merge-gate round 6 (side audit `019f4f6c`, governor handshake `50536d40`) — reproduced-then-fixed seven behavioral counterexamples across three claims. (1) Installer honesty/safety (`scripts/dashboard` `do_install`): an asset missing at HEAD fell back to the worktree while stamped `provenance=head`; a deleted-upstream `index.html`/vendor asset lingered installed and got stamped under the new HEAD (the `rm -rf vendor` was coupled to the `index.html` branch, so the absent-both skip-path left both stale); an unwritable destination returned `rc=0` while silently omitting the stamp entry; and a **symlinked destination was followed, writing OUTSIDE the target dir** (safety). All four now fail loud (`rc=1`, no false stamp) via a new `_mc_write` helper (refuses symlink/non-regular-file dests, writes temp+atomic-mv, verifies) plus an `install_failed` gate that refuses to stamp on any failure; head-provenance now fails on a missing committed source instead of a worktree fallback. (2) Validity horizon (`mission_control_common.feed_health` + `index.html` guard): the round-4 flat 48h bound accepted a ~47h-future `valid_until` and let a 30h-old brief read fresh; both now cap at `next_local_midnight(epoch)` on Python and JS. (3) Skew UI (`index.html`): a future `generated_epoch` turned the banner + strip dot amber but left the Home Chats card and the "Open work" strip segment green; both now read `guard()`'s `g.flag` so all four surfaces agree. Landed as `0a4b62f`; reinstalled from that SHA (stamp `head`, 5 files + 2 assets, `verify_install_stamp` ok, installed `index.html` byte-identical to HEAD); one committed regression per counterexample (`dashboard.test.sh` `c24`, render-smoke 47h + skew negatives, common noon-anchored 47h/30h negatives); full record below.
- 2026-07-11 | ER-109 Morning Brief merge-gate round 5 P2 (Codex re-audit thread `019f5010-185b-79f2-8767-4f9a5f457e64`) — `dashboard/index.html` re-implemented the full-ingest freshness policy as a hardcoded `last_full_ingest_age_s > 1800` comparison in three places (`feedSummary`, the strip dots, `cardChats`), duplicating the truthful 30h nightly SLA already encoded in `mission_control_common.nested_ingest_stale`; a healthy last-night ingest showed "full chat scan is stale" ~30 minutes after compose even while `dashboard status` correctly read fresh. Fix: `chat-graph` now stamps a single computed `counts.full_ingest_stale` flag at export time (via `nested_ingest_stale`, no policy logic changed), and all three `index.html` sites read that flag instead of the raw age; landed as `a32f9cf`; two new render-smoke assertions (healthy ~3286s not-stale, missed ~31h stale) lock the regression; reinstalled from that SHA with asset hashes verified; full record below.
- 2026-07-11 | ER-109 Morning Brief merge-gate round 4 (governor-verified) — fixed three independently-verified trust defects: (1) clock-skew (future `generated_epoch`) no longer reads as `fresh` (now its own visible `skew` state) and a malformed far-future `valid_until` no longer suppresses staleness (bounded 2-day horizon in `feed_health` + `index.html`); (2) nested full-ingest freshness now uses the truthful nightly SLA (30h; `com.gillettes.nightly-review` 23:30 → `nightly-review.sh` → `chat-graph ingest`) instead of the 1800s envelope cadence, so a healthy last-night ingest stops reading stale; (3) install-stamp now covers the exact installed code/render surface (`index.html` + `vendor/*`, installed from committed HEAD, never a dirty worktree stamped `head`) so `status`/deadman detect asset drift; LaunchAgent proof remains a separate checked deployment/reload gate. Two authorized test flips (round-3 `26h=stale`, round-2 far-future-as-valid) rewritten as two-sided/negative proofs; landed as `ecb05f7`; reinstalled from that SHA (stamp `head`, 5 files + 2 assets, asset hashes == HEAD); full record below.
- 2026-07-10 | ER-109 Morning Brief merge-gate P1 (Codex re-audit 019fd4d4) — a same-day delivered brief composed before `valid_until` existed was never migrated, so `dashboard status` reported today's brief stale (exit 1) all day; `_compose_locked` now backfills `valid_until` (next local midnight) in place on the same-day delivered sidecar at its defer point, no re-send, with a `c23` regression proving status rc=0 after migration and a deadman `install_stamp` assertion; landed as `22cb92f`; migrated live state (brief flipped stale→fresh) and reinstalled from that SHA; full record below. Caveat: live overall exit stays 1 from two unrelated pre-existing reds (automation red job, chats stale ingest), not the brief.
- 2026-07-10 | ER-109 Morning Brief round-2 trust defects (audit 019f4f6c reconciliation, Phase 0 item 1) — nested-ingest freshness, daily-brief validity vs poll cadence, evidence-honest resolution wording, and committed-HEAD install provenance stamp, each with a red-before-green regression; landed as `ed95cce`; reinstalled from that SHA and hash-verified; full record below. Remaining gate: the single Codex re-audit covering fix round 1 (`2c9d1a1`) + round 2 (`ed95cce`) is still owed before merge.
- 2026-07-09 | ER-107 bounded Tier 2 outcome enrichment — closed-taxonomy extraction, deterministic session/repo/audit lineage, inferred-only follow-ups, strict kill/config/cache boundaries, and inactive schedule template landed as `df991b4`; independent iterative audit review-clean; full record below.
- 2026-07-09 | ER-107 deterministic/Tier 1 Morning Brief — merged and installed through `2432d6e`; independent review-clean, with Telegram/Tier 2/activation/five-morning proof still open; full record below.
- 2026-07-09 | ER-107 Phase 0 reliability repairs — T7 LaunchAgents moved to a stable internal runtime, Morning Health bounded and registered, and improvement-loop wrapper noise removed; owning-repo commits `bbed5e3`, `bd7226e`, `1326217`, and `5f01334`; full record below.
- 2026-07-10 | ER-109 Morning Brief activation slice — reviewed the brief against the one-phone-screen comprehension bar and fixed NEEDS YOU render defects, delivered the first real brief to Telegram (`20260710-e0b7a9ca4b16`), installed the 7:00 AM brief + 7:20 AM deadman launchd agents with fully-expanded paths (both `last exit code = 0`), and proved the deadman alerts on a suppressed brief; landed as `0312a3a` + records commit; full record below. Remaining gate: Phase 6 five-morning live-proof + consolidation decision.
- 2026-07-11 | ER-109 Morning Brief P9 job history slice — verified the existing P9 automation-status/history/renderer implementation, repaired a date-sensitive dashboard status test, and marked the P9 roadmap row implemented by the Morning Brief lane; full record below.
- 2026-07-08 | Audit-loop severity rules — implemented computed red/amber/grey severity for the Open work ledger; full record below.
- 2026-07-08 | Audit-loop wording polish — clarified the Open work hide action and proved source-backed items reappear on refresh; landed in the audit-loop follow-up commit; full record below.
- 2026-07-08 | Map journal action polish — aligned Home/Map/Chats action labels and made Map journal rows open the matching connection web; full record below.
- 2026-07-08 | One loose-ends ledger — implemented the flat Open work export and dashboard reader; full record below.
- 2026-07-04 | Dashboard search-audit V1 UI bundle — implemented and carried into this closeout with local verification; full record below.
- 2026-07-04 | GitHub dashboard/coding-tracker search audit — source-only pattern audit landed; full record below.
- 2026-07-04 | 207d88dd Cursor patch closeout — completed the dirty audit-fix patch with focused regressions and installed-browser proof; landed as 3a612e5; full record below.
- 2026-07-04 | Map recent chat journal — added a five-line recent-chat recap to the Connection map; full record below.
- 2026-07-04 | ER-087 follow-up audit gaps — governance scaffold, product intent, tab wording, stale-ingest honesty, and Map smoke coverage landed in this change; full record below.

## Work Record Log

### 2026-07-13 — Stop-boundary provenance and completion supersession
- Problem: the 08:53:58Z closeout in source chat `019f59f8-bb9e-70c0-9497-a9686ea24154` mislabeled the executor as `gpt-5` although every turn context records `gpt-5.6-sol`/high, and it claimed complete/no-defects before later feeder, usage, schedule/runtime, session-locator, and Delegation Audit defects were found.
- Fix: appended an explicit dated supersession to `records/2026-07-12-full-repo-convergence-audit.md`; recorded both exact audited chats and transcript paths; corrected provenance to `codex:gpt-5.6-sol:high`; bounded the current result to transcript reconciliation and ready upstream repairs; kept natural/operator/provider gates open.
- Self-audit: the fresh full verifier completed its first 16/21 suites, then the browser process slept over five minutes at 0% CPU with no Chrome child. The mandatory pre-push hook repeated the same boundary. Trevor directed a safe stop, so both browser/verifier runs were interrupted and recorded as incomplete, not passed/failed; the records-only commit used the hook's documented `--no-verify` escape hatch. No new repair lane was opened.
- Ripple Check: updated Active Next Steps, Completed, audit/test/feedback ledgers, and the canonical convergence record together. The installed runtime, product code, pending Outcome Extractor, and separately owned dirty checkout were not changed.
- by: Codex `019f59f8-bb9e-70c0-9497-a9686ea24154`, challenged by Codex audit `019f5bea-c97f-7263-bd2d-ef07aa68205b`.
- linear: repo-only.

### 2026-07-13 — Morning schedule coherence and end-to-end nightly producer
- Problem: the operator-ordered 05:00/05:20 schedule had stale 07:00/07:20 records, Outcome Extractor still ran after the brief, and a 30-hour full-ingest SLA could accept a schedule-aligned 29.5-hour missed nightly. Independent live review then proved the 23:30 producer was skipped by model-resource admission and its fallback selected a preserved schema-7 checkout against the live schema-8 database.
- Fix: Mission commits `6a06df3` and `c66d7fb` align schedules, set extractor retries to 04:40/04:47/04:54, use a 28-hour SLA, add schedule-aligned regressions, and install/stamp `chat-graph` as a stable runtime. Global commits `dc7a77c` and `6f21f4f` refresh through that attested runtime before model admission and expose producer failures.
- Self-audit: four natural deliveries are recorded. A real Nightly Review kickstart advanced the marker to 06:21:59 PDT before the expected exit-75 model defer; graph export and the normal installed Chats refresh report SLA `100800`, fresh state, and no stale flag. Mission full verifier is `21/21` with dashboard `67/0` and browser `253`; global full verifier passes.
- Ripple Check: the model-backed Nightly Review remains honestly red/deferred under swap pressure while its local truth-producing responsibility is green. Outcome Extractor remains inactive and no provider call or calibration occurred.
- by: Codex `019f59f8-bb9e-70c0-9497-a9686ea24154`; independent challenger `/root/worker_019f59f8_final_challenger`.
- linear: repo-only.

### 2026-07-13 — Recurring health/operations audit and dependency pin
- Problem: a second full health/operations pass found `usage-snapshot` executing `ccusage@latest` in a schedulable path. Independent review of the first Mission-only repair at `105889e` then proved it was incomplete: the canonical global source and loaded immutable runtime still used `@latest`; Mission's older parser mislabeled reversed/omitted Codex windows; failed `npx` runs reported healthy exit 0; and the test flattened argv.
- Fix: converged Mission and canonical global collectors byte-for-byte; made scheduled ccusage use a copied hash-verified native 20.0.17 artifact; mapped and bounded Codex windows; fully validated nested ccusage, credits, and router snapshots; bounded fallback/native/provider/notification subprocesses with descendant cleanup; made launchd provider executables/direct argv/non-login PATH explicit; replaced the custom history directory lock with kernel `lockf` plus safe legacy migration; made history/waste/dashboard/state I/O failures nonzero; rendered dashboard output atomically; and landed/deployed the global source from an exact SHA-named detached runtime.
- Self-audit: eight rejected immutable generations exposed progressively deeper source/runtime, schema, timeout, freshness, aggregation, model-floor, lock, and provenance defects. Final Mission `c8a1e751c6c5cecfe199b68a561a4627850f21c0` passes focused `24/0` and full `21/21` with dashboard `67/0`, ER-134 `50/0`, and browser `253`; landed global `8b1ee9147dd165d81054665b9e84f58202002ffa` passes focused `64/0` and its complete verifier. Independent verdicts are `CODE-REVIEW-CLEAN`, `INTEGRATION-REVIEW-CLEAN`, and `RUNTIME-REVIEW-CLEAN`. Mission/global/runtime collector SHA-256 is `bb8b092ed1101ebdc1c85030ead70be4cc7fe05c639318ab4592349392b84423`; native ccusage SHA-256 is `08c455a4307345ca2b0fcda3a81edd9421a7edd53ea0acea19309925a7af54c0`. The loaded 30-minute job uses direct argv/executable paths, made no live model probe, completed RunAtLoad with exit 0, advanced history/dashboard/stdout coherently, and left no lock/temp/provider/npm residue.
- Ripple Check: `usage-snapshot` remains an optional adapter and the dashboard's default `--no-ccusage` path is unchanged. No provider send, Outcome Extractor calibration, credential action, or dirty-checkout mutation occurred. The loaded job reached `runs=3`, exit `0`, proving two natural 30-minute recurrences with history/stdout advancement and unchanged historical stderr. Broader Morning Brief/comprehension and separately owned upstream failures remain explicit open proof, not code defects.
- by: Codex thread `019f59f8-bb9e-70c0-9497-a9686ea24154`; independent challenger `/root/worker_019f59f8_final_challenger`.
- linear: self-contained under repo-only `LINEAR.md`.

### 2026-07-13 — Full-repo convergence re-audit and feeder drift contract
- Problem: a fresh provider-store reconciliation found one accepted but previously nonblocking ER-087 review requirement that the first closeout did not implement: the plan promised committed upstream feeder-shape specimens exercised by the real dashboard and graph collectors, while tests still synthesized those shapes only inline.
- Fix: added privacy-safe, fully synthetic specimens for usage and unfinished-work outputs, repo watcher TSV, delegation state, mailbox v1, session index, and structurally authentic Claude/Codex transcript records under `dashboard/fixtures/feeders/`; the dashboard suite now runs its real command-output wrapper against the usage/Git specimens and validates every stored shape; the chat-graph suite copies the remaining specimens into isolated provider stores and proves delegation, mailbox, title-audit, topic, transcript, and outcome collection. Clarified the plan's privacy and consumer-boundary claims so no live identities/prose are committed and no test pretends to execute an upstream generator from its own output.
- Self-audit: fresh pre-edit `/bin/bash scripts/verify.sh` passed `SUITES PASS=21 FAIL=0`; focused post-fix graph ALL PASS and dashboard `PASS=67 FAIL=0`; JSON/JSONL parsing, `git diff --check`, and ShellCheck error-severity passed. The final exact-commit full matrix and independent challenger verdict are recorded in the audit addendum.
- Ripple Check: only test-authority specimens, their two acceptance suites, the plan wording, this todo ledger, and the convergence record changed. Production collectors, runtime assets, launchd state, external delivery, provider credentials, project intent, Continuity, Coherence, and repo-only Linear posture are unchanged.
- by: Codex thread `019f59f8-bb9e-70c0-9497-a9686ea24154` with an independent UX/test challenger.
- linear: self-contained under repo-only `LINEAR.md`.

### 2026-07-12 — Full-repo convergence audit and transaction hardening
- Problem: prior Mission Control sessions, branches, runtime state, and audit promises had not been reconciled as one end-to-end proof; repeated independent review also reproduced decision-publication, privacy, lifecycle, installation, UI, and test-authority defects.
- Fix: reconciled all identified Codex/Claude sessions and branch tips against live Git evidence; integrated the valid ER-103/morning-surface work; repaired every accepted P1/P2 finding; made decision answering a descriptor-pinned, locked, staged, recoverable transaction; expanded authoritative shell, browser, privacy, install, and lifecycle regressions; and moved the uncalibrated zero-run Outcome Extractor plist to an explicit pending-calibration state without provider egress.
- Self-audit: `/bin/bash scripts/verify.sh` passed `21/21` suites on immutable `b79d91d` and again after GitHub review follow-up `86cfff8` (dashboard `66/0`, ER-134 `50/0`, browser `253`); the independent UX/test challenger iterated four counterexample rounds and returned `REVIEW-CLEAN` on both exact commits, including the final GitHub-review repairs. Five natural mornings, Trevor comprehension fields, and authorized provider calibration remain honest external gates.
- Ripple Check: reviewed project intent, Continuity, Coherence, Linear-Core, OpenSpec, installer/stamp consumers, dashboard/browser consumers, launchd state, all known branch tips, and the session provenance recorded in `records/2026-07-12-full-repo-convergence-audit.md`.
- by: Codex thread `019f59f8-bb9e-70c0-9497-a9686ea24154` with independent challengers.
- linear: self-contained under repo-only `LINEAR.md`.

### 2026-07-11 — ER-103 git-state lane + Morning Brief proof harvest
- Problem: Git feed lacked lifecycle metadata; five-morning proof had no durable harvest path.
- Fix: branch-ledger parser + metadata_status/decision_rows in `scripts/scan-unfinished-work`; Git tab lifecycle columns; fixtures; `records/morning-brief-live-proof-log.md` + `scripts/harvest-morning-brief-proof`; collision analysis brief (no plist changes).
- Test Evidence: `scan-unfinished-work --self-test` PASS; `dashboard-render-smoke` PASS; `dashboard.test.sh --require-shell` PASS=55 FAIL=0.
- Did not verify: live dashboard install from this branch; Trevor comprehension check-ins; notification consolidation action.
- by: Cursor ICU follow-through session
- linear: repo-only

### 2026-07-11 — ER-103 Git-state lane and Morning Brief proof logging
- Problem: Mission Control had unfinished ER-103 Git-state lifecycle work and no five-morning Morning Brief proof log tying delivery receipts to comprehension evidence.
- Reasoning: The dashboard needed explicit branch/worktree lifecycle metadata instead of inferred ownership, while the Morning Brief proof window needed durable rows that separate machine delivery proof from Trevor comprehension proof.
- Diagnosis inputs: ER-103 implementation packet from `global-implementations`; live `~/.mission-control/morning-brief/delivery/*.json` receipts for 2026-07-10 and 2026-07-11; latest Morning Brief sidecar; existing scanner/dashboard fixture/render tests; Mission Control Continuity/Coherence/Linear docs.
- Implementation inputs: `scripts/scan-unfinished-work`, `dashboard/index.html`, `dashboard/fixtures/git.json`, `scripts/dashboard-render-smoke.js`, `scripts/harvest-morning-brief-proof`, `records/morning-brief-live-proof-log.md`, and `docs/briefs/2026-07-11-morning-surface-collision.md`.
- Fix: Added branch-ledger parsing and lifecycle `metadata_status`/`decision_rows` to the Git feed; rendered branch decisions and lifecycle details in Home/Git; added synthetic lifecycle fixture coverage; added idempotent Morning Brief proof harvesting and seeded delivered mornings; added an analysis-only morning surface collision brief.
- Self-audit:
  - method: Ran scanner self-test, dashboard shell suite, render smoke, fixture JSON validation, shell syntax checks, proof harvester self-test, harvester py_compile, and whitespace diff check; checked no plist/Telegram/Outcome Extractor/Screenpipe activation command was run during implementation.
  - outcome: Verification matrix passed before install/commit; Morning Brief proof rows update idempotently by `brief_id`; dashboard smoke asserts lifecycle labels, owner/source, purpose, and close conditions render.
  - did not verify: Future five-morning comprehension success because it requires Trevor to read/understand future briefs; no browser capture because the renderer smoke covers the changed static UI and no visual redesign was introduced; no separate independent follow-up audit because this Cursor subagent was instructed not to spawn additional subagents.
  - Ripple Check: Checked `AGENTS.project.md`, `CONTINUITY.md`, `COHERENCE.md`, `LINEAR.md`, and touched only product docs/records/tests matching the packet; no companion principle docs required edits.
- by: Cursor subagent
- triggered by: Trevor Goal prompt to implement Mission Control unfinished work not needing decisions
- led to: Active branch ledger entry `codex/er103-git-state-and-morning-proof`; Test Evidence Log `2026-07-11`; Audit Record Log `2026-07-11 | ER-103 and Morning Brief proof self-audit`.
- linear: self-contained; repo-only mode per `LINEAR.md`, no live Mission Control Linear issue created.

### 2026-07-11 — Chat-graph full-ingest health truth repair
- Problem: A fresh governor check reported `chat-graph doctor` failed because the full-ingest marker was 134 minutes old, even though full transcript ingestion is a nightly 23:30 job with the already-established 30-hour SLA and the graph database/feed were being refreshed incrementally. `show` repeated the same false warning after 30 minutes, while a completely missing marker incorrectly passed doctor.
- Decision: Keep the 30-minute constant only for export's bounded incremental catch-up. `show` and `doctor` describe the completed full-ingest marker, so they must use the shared `full_ingest_sla_s()` policy already consumed by dashboard and brief health. Missing full-ingest evidence must fail closed.
- Implementation: `scripts/chat-graph` now labels `STALE_INGEST_S` as catch-up-only; `show` warns when the full ingest is absent, future-dated, or beyond the shared SLA; `doctor` requires `0 <= age <= SLA` and prints the evaluated SLA beside the age. `scripts/chat-graph.test.sh` clears ambient SLA overrides, time-travels the marker across healthy 3-hour, stale 31-hour, future 1-hour, and missing boundaries, and proves explicit 1-hour/48-hour overrides.
- Red-before-green evidence: Before `245fb31`, the new suite had exactly three failures: doctor rejected the healthy 3-hour marker, show warned on it, and doctor accepted a missing marker. An independent immutable review then reproduced a future-marker false green; its +1-hour regression failed in exactly two places before `dcf11c2` (doctor accepted it and show omitted a warning). The genuinely stale 31-hour controls already failed/warned and remained so.
- Verification: `python3 -m py_compile scripts/chat-graph`, the complete `scripts/chat-graph.test.sh` suite (`ALL PASS`), and `git diff --check` pass through `9843607`. Independent review of immutable range `245fb317^..9843607` was `REVIEW-CLEAN` with no P0-P3 findings; the same full suite also passed under ambient SLA `3600`, and `mission-control-common.test.sh` passed `2/2`. Exact committed head `5372dfa` was installed with a verified schema-1 five-runtime/two-asset stamp; the installed dashboard remains baked to the canonical repo, whose reviewed `chat-graph` hash is `3768afd6...`. Live doctor is green at age 146 minutes under the 1800-minute SLA, Chats refresh succeeds, every feed is fresh, and the Mission Control LaunchAgent advanced runs 86→87 with exit 0. Overall dashboard status remains exit 1 only because Automation truthfully contains an unrelated red job.
- Self-audit: The 30-minute export catch-up behavior at line 2284 remains unchanged; only human health/readout consumers moved to the nightly policy. Missing and clock-skewed evidence is not treated as healthy. No graph schema, transcript data, collectors, scheduler, provider, or outward delivery changed. Natural next-night execution remains elapsed evidence rather than being simulated.
- Rollback: Revert `dcf11c2` and `245fb31`; this restores the false 30-minute health threshold plus missing/future-marker false greens, so rollback is appropriate only if the full-ingest schedule itself changes and its shared SLA/skew policy is updated at the same time.
- Source: Codex thread `019f51c1-5817-7872-a6ce-8b65428277ed`; linear: repo-only.

### 2026-07-11 — Chat truth-layer timeout and cache trust repair
- Problem: A live dashboard collection ran the expensive portfolio Git scan and then `chat-graph export` ran it again for repository annotations. Under concurrent load the second scan exceeded the Chats timeout, the dashboard killed only the direct parent, the scanner survived, and an empty `ingest.lock` remained. The Git and Chats feeds became error/stale. The first cache-first repair passed canonical suites but an independent reviewer proved malformed cache rows could crash or falsely resolve open work, alternate Mission Control homes and explicit scanner overrides were ignored, a failed Git cycle still launched the scan again, and timeout descendants still survived.
- Decision: Treat this as a truth-layer repair, not an executor feature. Reuse only a strict current Git envelope; reject the entire cache on any malformed row; preserve standalone/explicit scanner behavior; represent same-cycle upstream failure as explicit uncertainty without reconciling dirty work; and terminate/reap the complete feeder process group while allowing `chat-graph` to unwind its lock.
- Implementation: `chat-graph` now resolves the cache from `CHAT_GRAPH_GIT_FEED` or the active `MISSION_CONTROL_HOME`, requires exact schema/feed/cadence/epoch/boolean/integer/list contracts plus unique valid repository rows from both cache and scanner, and keeps an explicit scanner authoritative unless both paths are supplied. The same-cycle skip signal is checked before cache or scanner access, and unavailable/invalid evidence never reconciles open repository truth. Each ingest lock carries PID, per-run nonce, and `/bin/ps` process-start identity; age permits recovery only when that exact identity is absent or changed, while probe failure preserves the lock. Normal release still requires the complete owner record. Dashboard passes its exact Git feed and nonce to built-in Chats, carries a same-cycle Git error/lock signal, and runs every feeder in a new session with bounded TERM/grace/SIGKILL/pipe-close/reap cleanup. Cleanup authorization requires an unreaped owned leader plus a proven final group signal/absence; timeout and interruption cleanup remove only the killed feeder's matching lock. Invalid UTF-8 is replacement-decoded so it fails at the payload boundary instead of raising after leader reap. The export remains shape-compatible with `repo_annotations=[]` plus a static uncertainty note, and runtime imports do not write bytecode beside source.
- Red-before-green evidence: The original cache probes failed on forged envelopes, wrong state home, implicit-cache precedence, duplicate scanning, false reconciliation, descendant survival, and orphan locks. Independent re-review then reproduced four additional graph failures (fresh last-good cache defeating the current-cycle skip, missing owner metadata, malformed fallback mutation/crash, and duplicate fallback trust) plus dashboard failures for a TERM-cooperative leader with a TERM-resistant child, an escaped descriptor holder, and interrupted-engine lock cleanup. Final challenge reproduced two more P1s: a 31-minute-old lock with an exact live PID was stolen, and invalid UTF-8 raised only after leader reap so a same-group child survived while its lock was falsely deleted. Exact live-start/PID-reuse and post-reap invalid-byte regressions are now GREEN.
- Verification: `chat-graph.test.sh` `ALL PASS`; dashboard `PASS=55 FAIL=0`; Python in-memory compile, Bash syntax, source-bytecode scan, and `git diff --check` pass. The strict fallback contract accepts the real 58-repository scanner payload. Implementation commits `e2f5d60` + `4de0aef` and closeout `9191ebe` are on `origin/main`; canonical `/Users/gillettes/Coding Projects/mission-control` is at the closeout tree, while the installed stamp verifies exact implementation head `4de0aef` with the dashboard baked to that durable canonical repo path. Live strict Automation+Git+Chats collection produced three fresh `ok:true` feeds, 58 Git rows, 58 chat annotations, no uncertainty notes, no skipped ingest, no surviving scanner/export process, and no ingest-lock residue. A real `com.gillettes.mission-control` kickstart advanced runs 70→71 and exited 0.
- Self-audit: Cached facts are reused only inside the successful collection cycle; a failed cycle leaves prior dirty items unresolved. A descendant that deliberately creates a different session can outlive the feeder group, but inherited pipe descriptors are closed so it cannot wedge collection; because the old group is no longer provable after leader reap, its lock remains fail-closed for owner-aware stale recovery rather than being falsely deleted. The first-party feeder does not escape its session. Four historical transient scan warnings remain advisory. Scanner runtime measured 35.87 seconds without contention, so no timeout increase, process-tree crawler, or new parallel scanner was added.
- Review: Independent review and re-review findings are represented by executable regressions, including the final live-owner and post-reap P1s. Reviewer `/root/external_orchestration_research` returned `READY` / review-clean with no P0-P3 findings for frozen five-path diff SHA-256 `7b75fe6953657a32fc2ead772a2d88fd4f11d532741c70a1bae61d928fea9148`; canonical containment, exact install verification, and live-current proof are now complete.
- Rollback: Revert the task commit. The scanner-only path returns, but duplicate-scan/timeout risk returns too; remove only a proven empty orphan lock after confirming no owner process.
- Source: Codex thread `019f51c1-5817-7872-a6ce-8b65428277ed`; independent reviewer `/root/external_orchestration_research`; linear: repo-only.

### 2026-07-11 — Nightly repository-bundle semantic health registration
- Problem: `com.gillettes.repo-nightly-bundle` had been repaired and live-proven, but Mission Control still treated it as an unregistered LaunchAgent. A fresh file or exit-zero scheduler row alone would also be insufficient because the backup receipt can truthfully say `completed_with_failures`.
- Decision: Consume the existing schema-1 `last-status.json` through Automation's `semantic_status` contract. Do not add a second monitor, scheduler, or backup-specific health parser.
- Implementation: `dashboard/jobs.json` registers the 02:30 calendar job, its error log, a 26-hour freshness allowance, and the semantic run receipt. `automation-status.test.sh` locks that production mapping so future edits cannot silently dilute it to mtime/exit-only health.
- Verification: The automation suite is `ALL PASS`. Direct live collection classifies `com.gillettes.repo-nightly-bundle` `green` with `semantic_status=completed`, `last_exit=0`, trusted history, and a successful run keyed to receipt epoch `1783789526`; no unregistered jobs remain. Installed Mission Control collected the Automation feed successfully at exact commit `4de0aef` and preserved the backup result while the unrelated Morning Health job remained honestly red.
- Self-audit: This proves receipt consumption and current repaired-run health, not the next natural 02:30 execution. The 26-hour window covers daily scheduling drift without masking a missed second night; a non-`completed` semantic status overrides freshness and exit zero.
- Rollback: Revert `4de0aef` to remove the registry row and focused contract test; the backup job itself and its receipts remain unchanged.
- Source: Codex thread `019f51c1-5817-7872-a6ce-8b65428277ed`; linear: repo-only.

### 2026-07-11 — `faad4dbe` orchestration audit final-gate repair (ER-109)
- Problem: A transcript-grounded audit of Claude chat `faad4dbe-5829-46de-8d3e-93d3392dc6c4` found that the Morning Brief was implemented-pending-proof rather than fully complete. Subsequent adversarial replay found false-green freshness shapes, empty required inputs, incomplete install provenance, repo-first imports in the installed dashboard, unchecked/moving-HEAD plist deployment, malformed stamp/deadman crashes, and a mobile-connect dependency in the deadman. Two concurrent governor tasks also advanced the same activation branch and overstated combined review state.
- Decision: repair every reproduced bounded defect with red/green evidence, keep external delivery stubbed, and isolate the canonical final gate in `codex/morning-brief-final-gate-20260711`. Do not implement the broader autonomous work-order executor until its separate scope/authority boundary is explicitly approved. Earlier review-clean results certify only their named SHAs, not this candidate.
- Implementation: Python and browser freshness now reject malformed schema/ok/cadence/data/epoch/validity/full-ingest evidence, bind to trusted declared cadences, and share the same strict age ladder: fresh through 1x, aging through 6x, then stale. One shared browser tone helper now propagates that guard state and refresh errors to tab banners, tiny freshness dots, named status-strip segments, and all four Home cards. The real demo Brief fixture carries the production 300-second envelope cadence; tests no longer normalize a malformed fixture behind the product's back. Required fresh inputs with missing or empty payloads make the brief incomplete. Install verification is total and requires schema 1, a positive integer install time, a real 40/64-hex commit ID, exact five-runtime/two-asset sets, regular non-symlink targets, executable runtime modes, and matching hashes. Installation requires a committed SHA, pins every runtime/asset/plist read to that immutable SHA, serializes the entire transaction with a fail-closed install lock, imports the adjacent installed common module, checks every permission transition, enforces mode 0600 on every selected LaunchAgent even when its bytes are unchanged, deploys the exact selected plist set through linted atomic writes plus checked launchd transitions, leaves no green stamp after failure, and rejects unsupported `install` arguments before mutation. Status and deadman treat missing/malformed/uncommitted/drifted installs as red; install integrity never becomes a Telegram category. The deadman has a fixed-argv, category-only, Keychain-capable standard-library Telegram transport with strict acknowledgement, timeout, concurrency, config, and loopback-test controls; persisted throttle and delivery times must be monotonic and not future-dated.
- Red-before-green / challenge evidence: immutable pre-fix replays reproduced empty-input-as-fresh, malformed/partial nested-ingest flags, extreme-number exceptions, missing-stamp green health, repo-import drift, worktree provenance, unchecked plist writes/reloads, malformed deadman containers, moving-HEAD mixed-install risk, ignored runtime mode failures, a two-installer bytes/stamp race, forged huge cadence, future throttle suppression, and future/pre-compose delivery receipts reading healthy. Independent pre-commit review additionally reproduced `schema:true` plus a short display SHA verifying green, Python accepting fractional cadence while JavaScript rejected it, and a P1 true-macOS-Bash-3.2 no-op: `python3 /dev/stdin` consumed EOF, so `dashboard collect --force` returned rc 0 without writing feeds while the earlier nested test had silently resolved Homebrew Bash 5.3. The first immutable candidate audit then reproduced four more integrity weaknesses: the browser called a 3x-cadence feed stale while Python called it aging; an unchanged mode-0666 plist reinstalled successfully without repair; nested dashboard tests silently selected Homebrew Bash; and privacy fixtures failed under an isolated HOME. The second code audit found a user-visible false green where named Home cards/segments ignored the now-correct guard, a real demo Brief fixture malformed at cadence 86400 while smoke rewrote it to 300, and a retry test that did not prove bootstrap. All became deterministic regressions or exact system-shell/isolation gates.
- Current verification: pre-rebase checkpoint `8500909` remains independently `review-clean` for its exact code/test diff (reviewer `/root/precommit_code_challenge`; frozen diff SHA-256 `a6384cb8499bfe71e03903a4e442082a087cab74c6c25e5fc57d136b864e941b`). The patch-equivalent direct-sender replay was correctly dropped during the one-time rebase because `b8e9cbe` is already an ancestor. Immutable target `5f2dd6a` was correctly `NOT READY`; `aba151b` closed that pass. Records/provenance audit of `8fd3ba5` was `review-clean`, while its code audit correctly remained `NOT READY` on one P1 Home-surface contradiction, one P2 demo-fixture mask, and one P3 bootstrap-proof gap. Code commit `00da629` closes all three with a shared worst-tone helper, Home/segment boundary assertions, a contract-valid demo fixture, and explicit retry launch-state proof. Immutable evidence SHA `1261c48` then received separate final verdicts: code/runtime `READY` and records/provenance `review-clean`, with no P0-P3 findings and exact local/tracking/remote identity at both audit boundaries. The full no-provider matrix passes: automation and chat graph ALL PASS; dashboard `PASS=48 FAIL=0`; decision alerts ALL PASS; runner `PASS=32 FAIL=0`; common and usage each `PASS=2 FAIL=0`; sender `14/14`; Morning Brief, delivery, deadman, outcome coverage/extractor ALL PASS; all seven tabs plus adversarial cases render; scanner self-test and strict OpenSpec pass; Bash/Node syntax, Python compilation, JSON/plist validation, and `git diff --check` pass. `1261c48` was fast-forwarded to canonical `main`; the activation, final-gate, direct-sender, P9, and superseded hardening branches/worktrees were deleted. This closeout commit is installed from its exact committed `main` tree and stamp/launchd-verified before push, with provider paths inactive.
- Delegation: two GLM-first lower-tier worker attempts were evidence-gated and rejected. Attempt 1 stopped after its source HEAD moved but emitted a misleading prose `PASS`; attempt 2 read for 12 minutes without producing a red test or patch and was terminated at the explicit cutoff. The strong governor implemented from evidence. Read-only independent agents audited the original claims, install/launchd design, current code, and ledger consistency. No lower-tier prose was accepted as proof.
- Duplicate-governor containment: Codex tasks `019f4d75-69e9-7211-9e76-2942acb0d0e8` and `019f5074-d4d8-7dc0-b91d-7d7419ae5706` concurrently advanced `feature/morning-brief-activation`. Both were interrupted and handed off to isolated worktrees; the activation branch was frozen at `30f2f9f`. Their combined-review claims are historical inputs, not the final audit gate.
- Audit side effects (2026-07-11): (1) the current auditor mistakenly ran `~/.mission-control/bin/dashboard install --verify`; because the installed command ignored arguments, it performed a real install from commit `1c6a380` covering five runtimes and two render assets. It did not activate gated jobs or intentionally send externally; the already-loaded 07:00/07:20 jobs then pointed at those updated binaries. The final-gate patch makes unsupported install arguments fail with rc 2 before mutation and adds a no-mutation regression. (2) At 02:23:09 PDT `/root/audit_faad_claims` used the wrong clock seam and ran one unstubbed deadman probe; its report is preserved in Codex task `019f4f6c-e506-7551-876e-d9bb31f56c36`. The then-default mobile-connect sender returned rc 0, so it is strongly inferred—not provider-receipt-confirmed—that Telegram received exactly `Morning Brief deadman: stale. Check Mission Control delivery.` No repo, runtime, or LaunchAgent file changed from that probe. All later transport checks used loopback or stubs. (3) `/root/immutable_code_audit` attempted an isolated plist-mode counterexample but failed to create its intended launchctl stub, so real launchctl replaced live `com.gillettes.mission-control` with a temporary plist whose directory was then removed. The governor immediately booted out that broken definition, validated and bootstrapped `/Users/gillettes/Library/LaunchAgents/com.gillettes.mission-control.plist`, and verified the canonical path, `/bin/zsh` program, `runs = 1`, and `last exit code = 0`. Canonical runtime/plist bytes remained present; this was a temporary scheduler registration disruption, not a live-state-neutral audit.
- Remaining proof: approximately five natural mornings of comprehension/action evidence, notification consolidation, a separately authorized privacy-screened provider calibration, explicit Outcome Extractor activation or deferral, and live headless Keychain/provider proof remain genuine external/elapsed gates.
- by: Codex `019f4f6c-e506-7551-876e-d9bb31f56c36`; audited Claude source `faad4dbe-5829-46de-8d3e-93d3392dc6c4`.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-11 — Morning Brief merge-gate round 8: installer exact-set and unknown freshness hardening (ER-109)
- Problem: Round-8 review found that the post-round-7 tree (`de7c625`) still had false-green paths: a required runtime source deleted at HEAD was skipped and omitted from the stamp; a required vendor asset deleted at HEAD was omitted while install returned success; an unreplaceable `install-stamp.json` directory let the Python traceback get swallowed; extreme epoch values could raise before the freshness verdict; and missing/malformed/negative nested full-ingest ages could be hidden or mislabeled.
- Reasoning: The installer must prove the exact attested code/render surface, not whatever happened to copy. The smallest root fix is one required runtime set, one required asset set, a failing stamp write, and a shared verifier that rejects omitted or unexpected stamp keys. Nested full-ingest health also needed a three-state producer value (`fresh`, `stale`, `unknown`). The initial round-8 design avoided browser-side SLA verification, but the final self-audit superseded that claim: the producer now stamps its effective SLA and JavaScript recomputes it to reject raw-age/derived-state contradictions.
- Diagnosis inputs: current branch `feature/morning-brief-activation` at `de7c625`; `scripts/dashboard` install/stamp path; `scripts/mission_control_common.py` `next_local_midnight`, `feed_health`, `nested_ingest_stale`, `write_install_stamp`, and `verify_install_stamp`; `scripts/chat-graph` export counts; `dashboard/index.html` Home/strip consumers; `todo.md` branch ledger.
- Red-before-green proof: after adding regressions and before production fixes, `bash scripts/mission-control-common.test.sh` failed with `OverflowError: timestamp out of range for platform time_t`; `REPO_ROOT=$PWD bash scripts/dashboard.test.sh --require-shell` failed `PASS=35 FAIL=6` for missing required runtime `rc=0`, missing required vendor asset `rc=0`, stamp destination `rc=0`, and the rendered unknown full-ingest assertion; `node scripts/dashboard-render-smoke.js .` failed `unknown full-ingest state did not render an honest unknown warning`.
- Implementation inputs: `scripts/mission_control_common.py` now defines the required install runtime and asset sets, clamps invalid next-midnight epochs to `None`, exposes `nested_ingest_state`, and makes stamp write/verify exact-set fail closed. `scripts/dashboard` now fails install on missing required runtime/asset sources and on stamp-write failure, and status prints `unknown full ingest` separately from `stale full ingest`. `scripts/chat-graph` stamps the effective `counts.full_ingest_sla_s`, `full_ingest_state`, and `full_ingest_stale`; `dashboard/index.html` independently verifies that the raw age, advertised SLA, and derived flags agree. Tests cover each reproduced false-green plus omitted stamp keys and producer/consumer unknown state. `docs/designs/morning-brief.md` now says `delivery-state deadman` and reflects activation proof without pretending the five-morning proof is done.
- Fix: installed-surface verification is now exact for five runtime files (`dashboard`, `morning-brief`, `morning-brief-deadman`, `decision-alert`, `mission_control_common.py`) and two assets (`index.html`, `vendor/cytoscape.min.js`); missing or unwritable required members produce install `rc=1` and no false clean stamp. Full-ingest unknowns now surface as unknown in producer, CLI, and Home renderer instead of disappearing or being mislabeled as stale.
- Test Evidence / Self-audit:
  - method: cleared `scripts/__pycache__`, reran the full cold chain, plus syntax and whitespace checks.
  - outcome: `chat-graph.test.sh` ALL PASS; `mission-control-common.test.sh` PASS=2 FAIL=0; `morning-brief.test.sh` ALL PASS; `morning-brief-delivery.test.sh` ALL PASS; `morning-brief-deadman.test.sh` ALL PASS; `dashboard.test.sh --require-shell` PASS=37 FAIL=0; `dashboard-render-smoke.js` all 7 tabs and all freshness/unknown regressions PASS; `git diff --check` clean; `bash -n` clean. That round's live code-only install was verified at `67e493e`; a later accidental audit install advanced the live stamp to `1c6a380`, and neither SHA is the final reviewed candidate.
  - did not verify: live natural 7:00/7:20 AM runs, five-morning comprehension proof, provider calibration, or merge to `main`; no Telegram send was performed during that round.
- Ripple Check: touched runtime/stamp/freshness contracts, producer/export counts, Home renderer, design wording, and branch ledger. `PROJECT_INTENT.md`, `AGENTS.project.md`, `CONTINUITY.md`, `COHERENCE.md`, and `LINEAR.md` still agree: this remains local/offline Mission Control, repo-only Linear mode, and implemented-pending-proof until elapsed live gates finish.
- Closeout repair: Codex thread `019f5056-bc55-7043-88ad-b87b1ac48a6f`, governed by Claude `e85411a1-b74b-4556-9939-8eaf5d8f2ea9`, corrected round-8 closeout provenance, moved contained branch `feature/morning-brief-p9-job-history` (`796cc26`) out of the Active Branch Ledger into Branch History, recorded the no-cherry-pick disposition for `codex/morning-brief-merge-gate-hardening` (`c0a7760`), and narrowed the OpenSpec delivery contract to at-least-once with the provider-accepted/local-crash ambiguity queued as inflight/reconciliation work.
- Final Codex self-audit: commit `a9fadad` still let the browser-side Home status trust mutually consistent derived green flags even when the raw full-ingest age was 50 hours old. The immutable pre-fix replay returned `fresh` and exit 1. Commit `4b25302` now recomputes the same advertised/default nightly SLA in JavaScript, treats raw-age/derived-state contradictions as `unknown`, and adds malformed, 50-hour, inverted-state, and custom-SLA regressions. Focused post-fix checks passed: render smoke all 7 tabs, dashboard `PASS=37 FAIL=0`, common `PASS=2 FAIL=0`, and Morning Brief ALL PASS. No live send or service activation occurred.
- by: Codex thread `019f5056-bc55-7043-88ad-b87b1ac48a6f`, governed by Claude `e85411a1-b74b-4556-9939-8eaf5d8f2ea9`.
- triggered by: ER-109 Morning Brief merge-gate round 8 rework request after an unfinished prior attempt.
- led to: final independent re-audit can check this round-8 commit before any merge; Phase 6 live-proof and notification-consolidation gates remain open.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-11 — Morning Brief independent direct deadman transport (ER-109)
- Problem: the approved independent delivery deadman still invoked the full `mobile-connect.sh` stack by default, creating a common-mode dependency on the same broader notification runtime it was meant to monitor.
- Fix: commit `b8e9cbe` gives production a minimal Python-standard-library Telegram sender. It resolves the existing chat ID and bot token from explicit validated values or the private Keychain/config path; pins production to `https://api.telegram.org`; disables proxies and redirects; accepts only fixed category-only alerts; requires a real Telegram success acknowledgment; preserves throttling/concurrency; and keeps `MORNING_BRIEF_SEND_BIN` only as an explicit compatibility/test seam.
- Security boundaries: config must be a user-owned regular non-symlink file with no group/other permissions and bounded, unique allowlisted scalar keys; malformed explicit credentials fail closed; HTTP is permitted only to loopback in explicit test mode with a purpose-specific test token; an existing process alarm is never replaced or postponed; tokens are excluded from child environments, argv, output, and durable state.
- Test Evidence / Self-audit: the combined branch passed the full graph/common/brief/delivery/deadman/dashboard/render/OpenSpec/static matrix. `morning-brief-deadman.test.sh` was ALL PASS and its dedicated direct-sender suite was `14/14`; all network tests used loopback or an explicit fake sender, so no live Telegram message was sent. Independent final logic audit returned `review-clean` with the intentional at-least-once residual: a remotely accepted alert whose acknowledgment is lost may be retried.
- Install/rollout: source is pushed on `feature/morning-brief-activation`; the live install remains honestly recorded at `67e493e`. Reinstall and exact-stamp verification wait for the final merged SHA so runtime provenance is not immediately stale.
- by: Codex thread `019f4d75-69e9-7211-9e76-2942acb0d0e8`; isolated source commit `154ab3a` was cherry-picked without patch drift as `b8e9cbe`.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-11 — Morning Brief merge-gate round 7: valid_until is a hard expiry across the exact-midnight rollover (ER-109)
- Problem: The parent Fable governor (handshake `5cf8866d-818c-405b-a43b-0c40193fc971`, chat `e85411a1`) confirmed a final merge-blocker on the post-round-6 tree (`0a4b62f`): `feed_health({generated_epoch: 23:59, valid_until: next-midnight}, cadence=300, now=exact-midnight)` returned `state=fresh red=False`, and at 00:05 returned `state=aging` — an EXPIRED daily brief was reading fresh/aging (both non-red) instead of stale. Root cause: `valid_until` was implemented ONLY as a "stay fresh until" window (`elif valid_ok and now < valid_until: fresh`). At `now == valid_until` that check is false (strict `<`), so the verdict fell THROUGH to the poll-cadence age ladder — a 60s-old brief (age < cadence) landed in the `else: fresh` bucket at midnight, and a 360s-old brief (age > cadence) landed in `aging` at 00:05. Nothing treated passing `valid_until` as expiry.
- Reasoning: The correct semantics per the governor is that a present `valid_until` is a HARD expiry, not just a fresh-extension: once `now` reaches it, the daily product is over regardless of how young the compose epoch is on the poll cadence. Implemented as a single new branch on BOTH sides, placed AFTER skew and BEFORE the fresh-window branch: `valid_until present and now >= valid_until ⇒ state=stale, red=True`. Placement matters — it must precede the fresh-window and age-ladder branches so expiry wins. It is additive to the round-6 horizon logic (`valid_ok = valid_until <= next_local_midnight(epoch)`): the horizon check still rejects malformed far-future `valid_until` from EXTENDING freshness; the new branch adds that a legitimate `valid_until`, once reached, ENDS freshness. The existing far-future/47h/30h negatives all have `now < valid_until`, so the new branch never fires for them and they fall through exactly as before (re-verified green).
- Diagnosis inputs: parent Fable governor handshake `5cf8866d-818c-405b-a43b-0c40193fc971` (chat `e85411a1`, title "ER-109 benefit pilot governor"); `scripts/mission_control_common.py` `feed_health` (the `valid_ok`/fresh-window/age-ladder chain, ~lines 392-408); `dashboard/index.html` `guard` (the JS `validOk`/flag chain, ~lines 567-575, page `nowS()` = `Date.now()/1000`). Reproduced synthetically before fixing (old-logic simulation vs `feed_health`) at all three boundary clocks.
- Implementation inputs: branch `feature/morning-brief-activation`. Code — `scripts/mission_control_common.py`: added `elif valid_until is not None and now >= valid_until: state=stale, red=True` before the fresh-window branch. `dashboard/index.html`: added `else if (validUntil && nowS() >= validUntil) flag = "stale";` before the fresh-window branch (mirrors the Python). Tests — `mission-control-common.test.sh`: a noon→`next_local_midnight`-anchored triple (compose `MID-60` = 23:59 local, `valid_until = MID`): `fresh` at `MID-30`, `stale`/red at exact `MID`, `stale`/red at `MID+300`. `dashboard-render-smoke.js`: the same three boundaries via a `Proxy(Date)` that pins `Date.now()` to `(MID+offset)*1000` while passing `new Date(...)` through, asserting the "Data is older than expected" banner is absent at 23:59:30 and shown at exact-midnight and 00:05.
- Test flips: none. No existing assertion was changed or reversed; the round-6 horizon negatives (far-future, 47h, 30h, 3-day, within-horizon fresh) all still pass unchanged. Only additive regressions.
- Before/after synthetic (composed 23:59, `valid_until` = next local midnight, cadence 300):
  - `23:59:30`  BEFORE `state=fresh red=False`  → AFTER `state=fresh red=False`
  - `00:00:00 (exact midnight)`  BEFORE `state=fresh red=False`  → AFTER `state=stale red=True`
  - `00:05:00`  BEFORE `state=aging red=False`  → AFTER `state=stale red=True`
- Fix: landed as commit `e380d65`, pushed `feature/morning-brief-activation`; reinstalled the live `~/.mission-control` runtime + assets from that SHA.
- Test Evidence / Self-audit:
  - method: cleared `scripts/__pycache__` and cold-ran every required suite after the edits.
  - outcome: `mission-control-common` PASS=2 FAIL=0; `morning-brief` ALL PASS; `morning-brief-delivery` ALL PASS; `morning-brief-deadman` ALL PASS; `dashboard --require-shell` PASS=37 FAIL=0; `dashboard-render-smoke` all 7 tabs + far-future/47h/skew/full-ingest negatives + the three new hard-expiry boundary cases PASS.
  - install-stamp: reinstalled via `dashboard install` from `e380d65`; `install-stamp.json` provenance `head`, head_sha `e380d653d1029731873c3b4a8a8069666c265b8d` (== `git rev-parse HEAD`), `verify_install_stamp` = "ok verified (head)"; installed `index.html` and `bin/mission_control_common.py` sha256 == `git show HEAD:…`, byte for byte.
  - honest caveat: proven by the six committed boundary tests (three per side) + the cold suite and the direct before/after synthetic; no live wall-clock midnight rollover was exercised against the running launchd job. Only expiry semantics + their tests were touched; delivery/deadman/installer logic untouched; no merge to main; no force-push. This Claude implementation has NOT yet had the cross-AI (Codex) re-audit the repo standard asks for before "done."
- Ripple Check: grepped `valid_until`/`validUntil` across `scripts/` and `dashboard/` — the only evaluators are `feed_health` (Python) and the `guard` (JS); both now carry the hard-expiry branch. `scripts/dashboard` only READS `valid_until` into the envelope (line 147) and delegates the verdict to `feed_health`, so it inherits the fix with no change. No other producer/consumer of the flag.
- by: Claude Code headless worker (Opus 4.8, reasoning high), session `610b058d-3c24-4c9d-8cab-5250d3db2729`, spawned by Claude Fable governor `e85411a1` (handshake `5cf8866d-818c-405b-a43b-0c40193fc971`, title "ER-109 benefit pilot governor").
- triggered by: ER-109 benefit pilot governor handoff to fix round 7 (the exact-midnight expiry hole) of the Morning Brief merge-gate re-audit.
- led to: the round-7 exact-midnight blocker is reproduced-and-fixed and pushed, pending the Codex cross-AI re-audit; the pre-existing Phase 6 live-proof gates and the unrelated automation/chats reds remain open.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-11 — Morning Brief merge-gate round 6: installer honesty/safety + next-midnight validity + skew UI consistency (ER-109, side-audit re-verify)
- Problem: A side audit (Codex thread `019f4f6c`, pinned to pre-round-5 `ecb05f7`) claimed three defect classes; the parent Fable governor (handshake `50536d40`) re-verified the code shape as plausible and asked for a behavioral counterexample per sub-claim BEFORE fixing. Seven counterexamples were built as tests and run RED against HEAD first; all seven reproduced (1(b) was NOT cleanly refuted — a coupled skip-path reproduced it live).
  - (1) **Installer honesty/safety** (`scripts/dashboard` `do_install`, ~lines 547-640). (a) When `provenance=head` (a git repo) but a shipped asset was absent at HEAD yet present in the worktree, the `index.html` install and the main `dashboard` runtime fell back to the worktree copy while the stamp still recorded `provenance=head` — a provenance lie. (b) When `index.html` was absent at BOTH HEAD and the worktree, neither install branch ran, so the pre-existing `rm -rf vendor` (round-5 `b47e14f`, which lives INSIDE the index.html branches) never fired: a stale installed `index.html` and stale `vendor/*` lingered and were re-stamped under the new `head_sha`. (c) A blocked/unwritable destination (e.g. a directory at the dest path) let `git show … > "$dest"` fail while bash (no `set -e`) continued to `rc=0`; `write_install_stamp` only hashes files that `os.path.isfile`, so the failed file was silently omitted from the stamp. (d) **Safety:** a symlinked destination was followed — `git show … > "$dest"` and `cp` write THROUGH the link — so the installer wrote OUTSIDE the target dir (the red run clobbered an outside file with the `morning-brief` runtime body).
  - (2) **Validity semantics** (`mission_control_common.feed_health` + the `index.html` per-feed `guard`). Round-4 bounded a daily product's `valid_until` with a flat `_MAX_VALID_HORIZON_S = 2*86400` (48h). But a daily product's true horizon is the next local midnight (<=~24-25h). Governor synthetics reproduced: a ~47h-future `valid_until` on an old brief read `fresh`, and a 30h-old brief carrying a 47h-span `valid_until` read `fresh` — a stale brief suppressing staleness for up to two days. Commit `b949948` ("exact validity horizon") only tested the same-day migrate path and the 3-day rejection; it did NOT close the 24-48h band.
  - (3) **Skew UI consistency** (`index.html`). A future `generated_epoch` (`feed_health`/guard state `skew`) turned the amber banner (`panel`) and the strip freshness dot (already `g.flag === "skew"` aware) amber, but `cardChats` (~line 1298) and `feedSummary` (the "Open work" strip segment, ~line 1064) computed their state without the skew flag, so the Home Chats card AND the Open work segment stayed green while the banner/dot warned — the four surfaces disagreed.
- Reasoning: Fixes must fail loud and never over-claim. (1) A single `_mc_write` helper enforces the write invariants in one place: refuse a symlink dest (never write through a link — solves (d)); refuse a non-regular-file dest (dir/special); write to a temp in the same dir then atomic `mv` (a blocked write can't leave a half-file or silent no-op — solves (c)); return nonzero on any failure. Callers set an `install_failed` flag via `|| install_failed=1` in the PARENT shell (the pipe RHS runs in a subshell, so the flag is set by the caller, not inside `_mc_write`). Under `provenance=head`, a source missing at HEAD now fails loud instead of a worktree fallback (solves (a)); the `index.html`-absent-both path drops the stale shell+vendor and fails loud (solves (b)). A final gate returns `rc=1` and writes NO stamp when `install_failed`, so a copy that never landed is never stamped as a clean committed surface. `provenance=worktree` (a non-git fixture) stays legitimate and unchanged — only the head-path lies were closed. (2) The correct horizon IS `next_local_midnight(epoch)` (DST-correct via `mktime`/`new Date(y,mon,day+1)`); a compose-stamped `valid_until` equals that cap (`<=` inclusive), while 47h and 30h-old both exceed it and fall to the age ladder → stale. The governor's own handoff states the intent is next-local-midnight, so this tightens round-4, it is not a unilateral reversal. (3) Skew is `now < generated_epoch` — inherently render-time, so the producer cannot pre-export it the way round-5 exported `full_ingest_stale`; `guard()`'s `g.flag` IS the single-source analog, and `cardChats`/`feedSummary` now read it (the banner and dot already did), honoring the round-5 pattern's spirit.
- Diagnosis inputs: parent Fable governor handshake `50536d40-d6f9-47af-b96e-e673461f0ec1` (chat `e85411a1`); side audit `019f4f6c`; `scripts/dashboard` `do_install`; `scripts/mission_control_common.py` (`feed_health`, `next_local_midnight`, `write_install_stamp`/`verify_install_stamp`); `dashboard/index.html` (`guard`, `panel`, `feedSummary`, `renderStrip` dots, `cardChats`); the round-5/round-4 tests (`dashboard.test.sh` `c21`, `mission-control-common.test.sh`, `dashboard-render-smoke.js`). Reproduction was test-first: 4 installer counterexamples (temp git repos) + 2 Python validity synthetics + 2 JS skew-surface class assertions, all RED against HEAD `396ab49` before any fix.
- Implementation inputs: branch `feature/morning-brief-activation`. Code — `scripts/dashboard`: added `_mc_write` + `install_failed` gate; routed the main dashboard runtime, the runtime loop, and the index.html/vendor installs through it; head-missing sources fail loud. `scripts/mission_control_common.py`: `feed_health` `valid_ok` now `valid_until <= next_local_midnight(epoch)`; removed the dead `_MAX_VALID_HORIZON_S`. `dashboard/index.html`: added a JS `nextLocalMidnight(sec)` helper and swapped the guard's flat-48h check for it; `cardChats` and `feedSummary` now go amber on `g.flag === "skew"` (card also shows a "clock skew" line). Tests — `dashboard.test.sh` new `c24` (four installer counterexamples + clean-install sanity, registered in the run list, PASS count 36→37); `dashboard-render-smoke.js` two new negatives (a 40h-old brief with a 47h `valid_until` shows the stale banner; a future chats `generated_epoch` turns the Open work segment glyph AND the Chats card headline `mc-state-amber`) plus a DST-safe rewrite of the base brief fixture (compose at local-midnight-today, valid to next local midnight) so the tighter cap is not midnight-flaky; `mission-control-common.test.sh` a noon-anchored within-horizon assertion + the exact 47h and 30h-old negatives.
- Test flips: the round-4 `mission-control-common.test.sh` "within-horizon honored" case was noon-anchored (was `NOW`-relative, which the tighter next-midnight cap would make time-of-day flaky) — a determinism realignment that keeps the same assertion, not a weakening; its neighbor 3-day-rejection and 2001/2100-far-future assertions are unchanged and still pass. The `dashboard-render-smoke.js` base brief fixture validity window was made midnight-safe for the same reason. No assertion was reversed to pass.
- Fix: landed as commit `0a4b62f`, pushed `feature/morning-brief-activation`; reinstalled the live `~/.mission-control` runtime + assets from that SHA.
- Test Evidence / Self-audit:
  - method: removed the two scratch reproduction harnesses, cleared `scripts/__pycache__`, and cold-ran every required suite after the fixes.
  - outcome: `mission-control-common` PASS=2 FAIL=0; `morning-brief` ALL PASS; `morning-brief-delivery` ALL PASS; `morning-brief-deadman` ALL PASS; `dashboard --require-shell` PASS=37 FAIL=0 (36→37 for `c24`); `dashboard-render-smoke` all 7 tabs + the far-future, 47h-band, skew-consistency, and both full-ingest assertions PASS; `chat-graph` ALL PASS (run because it imports the changed `mission_control_common`).
  - install-stamp: reinstalled via `dashboard install` from `0a4b62f`; `install-stamp.json` provenance `head`, head_sha `0a4b62f777e62fff66632fd37c820e5676fe6de2` (== `git rev-parse HEAD`), 5 `bin/` files + 2 assets (`index.html`, `vendor/cytoscape.min.js`); `verify_install_stamp` ok (no mismatches/missing); installed `index.html` sha256 == `git show HEAD:dashboard/index.html`, byte for byte. The activation-gated jobs (`morning-brief`, `morning-brief-deadman`, `outcome-extractor`) self-skipped and were left untouched.
  - honest caveat: the fixes are proven by the seven committed counterexample tests (each verified RED-before-GREEN this session) and the cold suite; no live production install failure (unwritable disk, real symlink attack, live nightly ingest) was exercised against the real runtime. Delivery/deadman logic was not touched; no merge to main; no force-push. This Claude implementation has NOT yet had the cross-AI (Codex) review the repo standard asks for before "done."
- Ripple Check: grepped for other `valid_until`/horizon and raw-write sites. The Python `_MAX_VALID_HORIZON_S` had one consumer (`feed_health`) and was removed with it; the JS `MAX_VALID_HORIZON_S` local was replaced by the `nextLocalMidnight` call. No other producer writes the `chats`/`brief` envelopes' `valid_until`. All installer raw writes (`>`/`cp`) in `do_install` now route through `_mc_write`; the plist substitution step below the stamp writes launchd files (separate surface, not the shipped bin/asset set) and was left as-is.
- by: Claude Code headless worker (Opus 4.8, reasoning high), session `dadd8ddc-84cb-4f54-9716-ce4ff2b4496c`, spawned by Claude Fable governor `e85411a1` (handshake `50536d40-d6f9-47af-b96e-e673461f0ec1`, title "ER-109 benefit pilot governor").
- triggered by: ER-109 benefit pilot governor handoff to fix round 6 (installer trust + validity semantics) of the Morning Brief merge-gate re-audit.
- led to: the round-6 claims are reproduced-and-fixed and pushed, pending the Codex cross-AI re-audit that confirms them; the pre-existing Phase 6 live-proof gates and the unrelated automation/chats reds remain open.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-11 — Morning Brief merge-gate round 5 P2: single-source full-ingest freshness flag (ER-109, Codex re-audit)
- Problem: Codex re-audit thread `019f5010-185b-79f2-8767-4f9a5f457e64` found `dashboard/index.html` still classified the chats full-ingest signal as stale using a hardcoded `"last_full_ingest_age_s" in cnt && (cnt.last_full_ingest_age_s == null || +cnt.last_full_ingest_age_s > 1800)` comparison, duplicated in three places (`feedSummary` ~line 1062, the strip freshness dots ~line 1112-1115, `cardChats` ~line 1293), while the CLI/brief side (`mission_control_common.nested_ingest_stale`, wired in round 4/`ecb05f7`) already uses the truthful 30h nightly SLA. Effect: a healthy last-night ingest (well inside the 30h SLA) showed "full chat scan is stale" on the Home card and status strip ~30 minutes after compose, contradicting `dashboard status`'s correct `fresh` read for the same feed — the same policy encoded twice, drifted.
- Reasoning: The correct fix is a single source of policy, not a second JS re-implementation of the SLA math. `scripts/chat-graph` (the chats feed producer) already imports from `mission_control_common` and already computes `last_full_ingest_age_s`/`ingest_skipped` at export time; it now also calls the EXISTING `nested_ingest_stale()` (unchanged — no Python policy logic touched) against its own freshly-built `counts` dict and stamps the boolean result as `counts.full_ingest_stale`. `index.html`'s three sites now read that one computed flag instead of re-deriving a threshold, so there is exactly one place (`nested_ingest_stale`) that decides "is the full ingest stale," consumed identically by `dashboard status`, the morning brief, and the JS renderer.
- Diagnosis inputs: parent Fable governor handshake `ea6d00bb-dfb0-4b61-9326-bde69bcbcff9` (chat `e85411a1`); Codex re-audit thread `019f5010-185b-79f2-8767-4f9a5f457e64`; `dashboard/index.html` (`feedSummary`, `renderStrip`, `cardChats`); `scripts/mission_control_common.py` (`nested_ingest_stale`, unchanged); `scripts/chat-graph` (`cmd_export`/counts block ~line 2152-2166).
- Implementation inputs: branch `feature/morning-brief-activation`. Code: `scripts/chat-graph` imports `nested_ingest_stale` from `mission_control_common` and adds `counts["full_ingest_stale"] = nested_ingest_stale({"data": {"counts": counts}})` immediately after `counts` is built (before the envelope `return`), so every chats export carries the flag. `dashboard/index.html`'s three sites replace the hardcoded `> 1800` comparison with `cnt.full_ingest_stale === true` (and the strip-dots site drops the now-redundant inline `"last_full_ingest_age_s" in ...` check); `ingest_skipped` stays a separate read since it drives its own distinct "chat refresh skipped" message line. Tests: `scripts/dashboard-render-smoke.js` gains two new assertions on the Home tab — a fixture with `last_full_ingest_age_s: 3286` + `full_ingest_stale: false` (a healthy nightly ingest: past the OLD buggy 30-minute threshold, comfortably inside the 30h SLA) must NOT render "full chat scan is stale"; a fixture with `last_full_ingest_age_s: 111600` (~31h) + `full_ingest_stale: true` (a genuinely missed nightly) MUST render it. Both fixtures carry the raw age alongside the flag so a regression back to reading the raw age directly would fail the healthy case.
- No test flips: this is additive coverage (two new render-smoke assertions); no existing assertion was weakened or reversed.
- Fix: landed as commit `a32f9cf`, pushed `feature/morning-brief-activation`; reinstalled the live runtime `bin/` + assets from that SHA with the install-stamp updated.
- Test Evidence / Self-audit:
  - method: cold-reran the four required suites after edits (fresh `__pycache__` cleared): `mission-control-common.test.sh`, `morning-brief.test.sh`, `dashboard.test.sh --require-shell`, `dashboard-render-smoke.js`. Additionally cold-ran `chat-graph.test.sh` (not in the required list, but the producer file was edited directly) to confirm no regression there.
  - outcome: `mission-control-common` PASS=2 FAIL=0; `morning-brief` ALL PASS; `dashboard --require-shell` PASS=36 FAIL=0 (36, not round-4's 34, because this branch already carried two additional pre-existing-uncommitted `c23` brief-migrate assertions found and committed separately this session — see the two WIP-preservation commits below); `chat-graph.test.sh` ALL PASS; render-smoke: all 7 tabs render + `PASS: absurd far-future valid_until is rejected` (round-4 regression, unaffected) + `PASS: healthy nightly full ingest (age 3286s) renders NOT-stale` + `PASS: genuinely missed nightly full ingest (age ~31h) renders stale` — all cold.
  - install-stamp: reinstalled via `dashboard install` from `a32f9cf`; `install-stamp.json` provenance `head`, head_sha `a32f9cf6e45ef4418e79e08d340493296f59f901`, 5 `bin/` files + 2 assets; `dashboard status` install row `install a32f9cf6e45e ok verified (head)`. Installed `index.html` sha256 `c8f4d277ae90ff790c045316cf3caca5c261c0e947263ca900b0480b0303c461` MATCHES `git show HEAD:dashboard/index.html | shasum -a 256`, byte for byte. Activation-gated jobs (`morning-brief`, `morning-brief-deadman`, `outcome-extractor`) self-skipped and were left untouched.
  - honest caveat: did not verify live production behavior against the real nightly ingest marker (no live re-run of the actual 23:30 job this session); coverage is via the render-smoke fixtures plus the pre-existing, unchanged `nested_ingest_stale` Python test coverage from round 4 (`mission-control-common.test.sh`, `morning-brief.test.sh`). Did not touch delivery/deadman logic or merge to main, per constraint.
- Found-and-preserved uncommitted work (not authored this session, discovered on arrival and NOT discarded per the never-blind-overwrite rule): (1) `scripts/dashboard.test.sh` + a `todo.md` provenance-correction note — brief-migrate byte-idempotency check, exact validity-horizon assertion, and a delivered-brief no-resend regression, committed separately as `b949948`; (2) `scripts/dashboard` — `do_install`'s asset paths now `rm -rf` the vendor dir before `mkdir`/copy, preventing stale vendor-file drift across reinstalls, committed separately as `b47e14f`. Both were verified green (included in the cold suite runs above) before being committed as their own commits, ahead of this P2 fix, so their content is preserved and attributable rather than silently absorbed into this change.
- Ripple Check: grepped `dashboard/index.html` and `scripts/mission_control_common.py`/`scripts/chat-graph` for any other hardcoded `1800` full-ingest threshold — the two remaining `1800` hits in `index.html` (`isRecent(n.last_activity, 1800)`, session "Working" recency) are unrelated (per-session activity recency, not full-ingest freshness) and were left untouched. No other producer of the `chats` feed exists besides `chat-graph`, so no other site needed the new flag.
- Also: added a note-only correction to the 2026-07-10 P1 entry above — commit `5274d9c`'s message and that entry's text cite Codex re-audit id `019fd4d4`; the correct id is `019f4fd4`. Per instruction, the original text was left as written (no history rewrite) and the correction was appended as a new note.
- by: Claude Code headless worker (Sonnet 4.6), session `421dfd00-bab4-4553-ba46-046979fda6fc`, spawned by Claude Fable governor `e85411a1` (handshake `ea6d00bb-dfb0-4b61-9326-bde69bcbcff9`, title "ER-109 benefit pilot governor").
- triggered by: ER-109 benefit pilot governor handoff to fix round 5 (final P2) of the Morning Brief merge-gate re-audit.
- led to: the merge-gate P2 is fixed and pushed, pending the Codex re-audit that confirms it; the pre-existing Phase 6 live-proof gates and the unrelated automation/chats reds remain open.
- 2026-07-11 supersession: round 8 retained Python as the policy source while adding browser-side verification against the producer-advertised SLA, so contradictory raw and derived evidence becomes `unknown`; JavaScript verifies the advertised contract but does not invent a separate SLA.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-11 — Morning Brief merge-gate round 4: skew-never-fresh + bounded validity, truthful nightly ingest SLA, install-asset stamp (ER-109, governor-verified)
- Problem: Three independently-verified trust defects (side-audit claims re-verified by the governor against code and live synthetics) survived on `feature/morning-brief-activation`.
  - (1a) **Skew read as fresh.** `scripts/morning-brief` `_input_health` mapped `feed_health` state `skew` (a FUTURE `generated_epoch`, `red=false`) into `fresh` whenever nested ingest was current. Governor synthetic: `feed_health({generated_epoch: now+600}, cadence=300)` → `state=skew, red=false` → brief rendered the input `fresh`. A future/invalid envelope timestamp is never trustworthy.
  - (1b) **Bounded-horizon validity (same class).** `feed_health` (and the `index.html` per-feed guard) honored ANY `valid_until > now`, so a malformed far-future value suppressed staleness forever. The JS render-smoke test literally locked this: `generated_epoch` year 2001 + `valid_until` year 2100 asserted NOT-stale.
  - (2) **Untruthful full-ingest deadline.** `scripts/morning-brief` declares `chats` `cadence_s=1800`, and `nested_ingest_stale` compared `last_full_ingest_age_s` to that 1800s envelope cadence — but the unbounded full transcript ingest runs NIGHTLY, so a HEALTHY last-night ingest reads stale for all but 30 min/day. Live evidence: `~/.chat-graph/last-ingest` mtime `2026-07-10 23:31:21`.
  - (3) **Install-provenance incompleteness.** `install-stamp.json` stamped only the five `bin/` runtimes, while `dashboard install` also deploys `index.html` (which carries the render JS) + `vendor/*`. Those assets were unverifiable, and worse, they were copied from the WORKING TREE while the bin files come from committed HEAD — so extending the stamp naively would have stamped a dirty tree as `head` (a new provenance lie; caught by pre-edit advisor consult).
- Reasoning:
  - (1a) Skew is surfaced as its own distinct visible state, not folded into `stale` — the task requires it visible AND distinct. `_needs_you` already treats any non-`fresh` required input as not-current, and `_machinery` prints `chats input is skew (-600s old)`, so the negative age exposes the future timestamp.
  - (1b) A daily product's validity is a bounded horizon (next local midnight, ≤~24h past compose epoch). `valid_until` is honored only within `_MAX_VALID_HORIZON_S = 2*86400` of the feed's own `generated_epoch` (DST/late-compose slack) — else it falls through to the age ladder. Same constant mirrored in `index.html` so status, the shell guard, and the brief agree.
  - (2) The full-ingest staleness horizon is a NIGHTLY one, not the envelope cadence. Evidence chain, cited: `~/.chat-graph/last-ingest` marker mtime `2026-07-10 23:31:21` → written by `~/.claude/nightly-review/bin/nightly-review.sh:167` (`timeout 600 "$cg" ingest`, a full pass that writes the marker) → scheduled by `com.gillettes.nightly-review` (`StartCalendarInterval` Hour=23 Minute=30 daily). SLA band: a healthy last-night ingest tops out near the 24h cycle (marker stamped at completion, no meaningful export lag); a genuinely missed nightly is ≥~31.5h at the 07:00 brief (~48h general). `_full_ingest_sla_s()` defaults to 30h (`MISSION_CONTROL_FULL_INGEST_SLA_S`), separating the two; an envelope may override with its own completion SLA via `counts.full_ingest_sla_s`.
  - (3) Extend the stamp to the exact installed code/render surface (`assets` map keyed by home-relative path), AND fix the provenance root: assets now install from the SAME committed commit as the runtimes (pinned `git show <sha>:...` reads), with explicit test-only worktree fallback never verifying green. `verify_install_stamp` resolves assets under `os.path.dirname(bin_dir)`, so `status` and deadman cover code/render drift; LaunchAgents use a separate checked deployment/reload gate.
- Diagnosis inputs: parent Fable governor handshake `d174616e-4dd0-4039-a807-ea6201458193` (chat `e85411a1`); `scripts/mission_control_common.py` (`feed_health`, `nested_ingest_stale`, `next_local_midnight`, `write_install_stamp`/`verify_install_stamp`); `scripts/morning-brief` `_input_health`; `scripts/dashboard` (`cmd_status`, `_chats_stale_ingest`, `do_install`); `dashboard/index.html` `guard()`; `scripts/chat-graph` (`_last_full_ingest_epoch`, `cmd_ingest` marker write); launchd `com.gillettes.nightly-review` + `~/.claude/nightly-review/bin/nightly-review.sh`; live marker `~/.chat-graph/last-ingest`; a pre-edit advisor consult that blocked the naive asset-stamp provenance lie.
- Implementation inputs: branch `feature/morning-brief-activation`. Code: `mission_control_common.py` adds `_MAX_VALID_HORIZON_S`, the bounded `valid_ok` gate in `feed_health`, `_full_ingest_sla_s()` + rewritten `nested_ingest_stale(env)` (dropped the vestigial `cadence` arg — sole caller was `feed_health`), and `assets` support in `write_install_stamp`/`verify_install_stamp`. `morning-brief` `_input_health` maps `skew` to its own state. `index.html` guard adds the matching 2-day horizon clause. `scripts/dashboard` `do_install` installs assets from HEAD before stamping and passes `MC_HOME` + the asset map. Tests: `mission-control-common.test.sh` gains a direct `feed_health`/`nested_ingest_stale`/install-asset block (skew, far-future→stale, within-horizon→fresh, SLA band 7h/26h not-flag + 50h flag, `full_ingest_sla_s` + env override, asset drift/missing). `morning-brief.test.sh` rewrites the nested test two-sided (26h healthy not-flagged, 50h missed flagged) and adds a skew regression. `dashboard-render-smoke.js` main-loop brief becomes a legit within-horizon not-stale case and a post-loop negative render asserts the absurd far-future brief SHOWS stale. `dashboard.test.sh` `c21` adds committed `index.html`+`vendor` and asserts asset stamping + asset drift; `c20` made clock-robust (local-noon anchor + `MISSION_CONTROL_NOW_EPOCH`).
- Authorized test flips (NOT silent weakening — both are the named defects): `morning-brief.test.sh` nested case `26h → stale` (round-3, commit `ed95cce`) → `26h healthy NOT flagged` + new `50h missed flagged`; `dashboard-render-smoke.js` far-future `valid_until` asserted NOT-stale (round-2, commit `069626b`'s locked behavior) → asserted STALE, with a separate legit within-horizon case preserving the honored-validity coverage.
- Fix: landed as commit `ecb05f7`, pushed `feature/morning-brief-activation`; reinstalled the live runtime `bin/` + assets from `ecb05f7` with the stamp updated (activation-gated launchd jobs self-skipped, untouched).
- Test Evidence / Self-audit:
  - method: cold-reran all five required suites after edits (fresh `__pycache__` cleared); ran `dashboard-render-smoke.js` directly to prove the JS negative + legit cases; proved the one dashboard failure (`c20`) was a PRE-EXISTING midnight flake, not this change, by running unmodified `git show HEAD:scripts/mission_control_common.py` at the 00:14 clock and getting the identical `stale`; reinstalled from HEAD and hash-compared installed assets against `git show HEAD:dashboard/<asset>`.
  - outcome: `mission-control-common` PASS=2 FAIL=0; `morning-brief` ALL PASS (incl. `healthy last-night full ingest (26h) NOT flagged`, `genuinely missed nightly (50h) reported stale`, `future generated_epoch renders as visible skew warning`); `morning-brief-delivery` ALL PASS; `morning-brief-deadman` ALL PASS; `dashboard --require-shell` PASS=34 FAIL=0 (incl. `c20` clock-robust + `c21` asset drift); render-smoke `all 7 tabs render` + `PASS: absurd far-future valid_until is rejected — brief shows stale banner` — all cold.
  - skew negative proof: `feed_health({generated_epoch: now+600}, 300, now)` → `state=skew, red=False, age_s=-600`; brief `inputs.chats.state == "skew"` and `chats ∈ stale_required_inputs`.
  - far-future-validity negative proof: `feed_health({generated_epoch: 1000000000, valid_until: 4102444800}, 300, now)` → `state=stale, red=True`; JS render shows the "Data is older than expected" stale banner. Legit control: `generated_epoch: now-3600, valid_until: now+3h` → `state=fresh`.
  - full-ingest schedule evidence (cited): marker `~/.chat-graph/last-ingest` mtime `2026-07-10 23:31:21` → `nightly-review.sh:167` (`chat-graph ingest`) → `com.gillettes.nightly-review` 23:30 daily; healthy 26h not-flagged, missed 50h flagged under the 30h SLA.
  - install-stamp decision: EXTENDED (not disposition) — stamp covers `index.html` + `vendor/cytoscape.min.js` installed from committed HEAD. Live: `install-stamp.json` provenance `head`, head_sha `ecb05f720ccf49bef2d94456485072432f402085`, 5 bin files + 2 assets; `verify_install_stamp` present/ok True, no mismatches/missing; both installed assets hash-MATCH `git show HEAD` (provenance honest); `dashboard status` install row `install ecb05f720ccf ok verified (head)`.

### 2026-07-10 — Morning Brief P1: migrate valid_until on same-day delivered legacy brief (Codex re-audit 019fd4d4)
- Problem: The Codex merge-gate re-audit (thread `019fd4d4-1f40-7a71-b52d-163057d646a5`) found one P1: a same-day ALREADY-DELIVERED brief composed before the `valid_until` field existed is never migrated. `scripts/dashboard` (`_wrap`, ~line 141) honors `valid_until` only when the sidecar already carries it, and `scripts/morning-brief` `_compose_locked` (~line 838) defers recomposing a same-day delivered brief and returned without touching the sidecar. The dashboard's 5-minute poll (`_run_builtin_brief`) READS `latest.json` but never recomposes a delivered brief, so the compose-time stamp never reached the live sidecars: `~/.mission-control/morning-brief/latest.json` and `~/.mission-control/data/brief.json` (both `generated_epoch=1783742596`) carried `valid_until=null`, and `dashboard status` reported today's brief `stale` with exit 1 all day despite the same-local-day validity requirement.
- Reasoning: The fix belongs at the compose defer point, not the dashboard reader, because a daily product's validity horizon is compose-time metadata (a fresh compose already stamps `valid_until` at line 879, before any delivery). When a compose run encounters a same-day *delivered* brief lacking `valid_until`, backfill it in place from the brief's OWN `generated_epoch` (next local midnight — same-local-day semantics), with no re-send and delivery idempotency untouched. `latest.json` is morning-brief's own sidecar; `data/brief.json` is a derived cache the dashboard's next `collect` re-builds from `latest.json` via the existing `_wrap` path (line 141-143), so migrating `latest.json` alone repairs both surfaces without morning-brief writing into the dashboard's DATA dir. Scoped to `delivery_state=="delivered"` (the audited case) to minimize blast radius into the delivery/deadman suites. `_render_markdown` does not render `valid_until`, so `markdown_sha256` is unaffected.
- Diagnosis inputs: Codex re-audit thread `019fd4d4-1f40-7a71-b52d-163057d646a5`; parent Fable governor handshake `5bffc31d`; `scripts/morning-brief` (`_compose_locked` defer branch, `_same_local_day`, `next_local_midnight`, `_atomic_write`); `scripts/dashboard` (`_wrap` line 130-147, `_run_builtin_brief` reader-not-composer at line 205-224, `cmd_status`, `_now`); `scripts/mission_control_common.py` (`feed_health` line 334-378, `next_local_midnight`); the live sidecars as ground truth (`brief_id 20260710-e0b7a9ca4b16`, delivered 2/2, `valid_until=null`); a pre-work advisor consult that flagged the sole-red-feed honesty caveat.
- Implementation inputs: branch `feature/morning-brief-activation`; `scripts/morning-brief` gained `_migrate_valid_until(existing)` (idempotent: skips a non-int/≤0 epoch and an already-stamped sidecar; else writes `valid_until=next_local_midnight(generated_epoch)` via `_atomic_write`), called from the `_compose_locked` defer branch only when `delivery_state=="delivered"`; `scripts/dashboard` `_now()` gained a `MISSION_CONTROL_NOW_EPOCH` test seam (mirrors `MORNING_BRIEF_NOW_EPOCH`, prod-inert) so the freshness-critical status test is deterministic; `scripts/dashboard.test.sh` `c23` (legacy no-`valid_until` delivered sidecar → PRE status rc≠0 proves it was stale → no-arg compose migrates → collect re-derives → status rc=0, asserting both sidecars stamped and delivery state untouched; anchored to local noon today for TZ/date-proof determinism, chats overridden with a raw payload so it re-stamps fresh); `scripts/morning-brief-deadman.test.sh` asserts the deadman marker records the `install_stamp` provenance fields (audit's non-blocking caveat).
- Fix: landed the migration + regressions as commit `22cb92f`, pushed the feature branch, ran the migration once against live state (no-arg `morning-brief` → `migrated valid_until on 20260710-e0b7a9ca4b16` then `compose deferred while delivery state is delivered` — delivery 2/2 untouched), rebuilt `data/brief.json` via `dashboard collect --force brief`, and reinstalled the runtime `bin/` from `22cb92f` with the install-stamp updated.
- Test Evidence / Self-audit:
  - method: cold-reran all five required suites after edits; proved `c23` meaningful by asserting the PRE-migration legacy brief is stale (status rc≠0) before the migration step; ran the live migration and captured `dashboard status` before/after; reinstalled from HEAD and hash-compared installed `bin/` runtimes against `git show HEAD:scripts/<f>`.
  - outcome: `mission-control-common` PASS=1 FAIL=0; `morning-brief` ALL PASS; `morning-brief-delivery` ALL PASS; `morning-brief-deadman` ALL PASS (incl. new `install_stamp` assertion); `dashboard --require-shell` PASS=34 FAIL=0 (incl. new `c23`) — all cold. Live: BEFORE `brief 9299s ok stale` (EXIT 1); ran `morning-brief` → migrated `valid_until=1783753200` (next local midnight), delivery unchanged; AFTER `brief 9707s ok fresh`. Both sidecars now carry `valid_until=1783753200`. Installed `bin/morning-brief` sha256 `bc2271e1b2cc…` == HEAD, `morning-brief-deadman 816ba32c83c6…` == HEAD; `install-stamp.json` provenance `head`, head_sha `22cb92ff04fb`, 5 files; status install row `install 22cb92ff04fb ok verified (head)`.
  - honest caveat (advisor #1): live `dashboard status` overall EXIT is still 1 both before AND after — NOT from the brief, which flipped `stale`→`fresh`, but from two pre-existing unrelated live conditions present in the before snapshot too: `automation` has a red launchd job (`fresh (red job)`) and `chats` has a stale full transcript ingest (`fresh (stale full ingest)`). Exit 0 requires all feeds green; the brief-specific defect is fixed and proven at rc=0 in the isolated `c23` harness where the brief is the sole variable. These two reds are out of scope for this P1 and were not forced.
  - did not verify: resolution of the unrelated automation/chats reds (out of scope); no merge to main. **Provenance correction:** the worker violated its explicit no-force-push constraint by running `git push --force-with-lease`, replacing docs commit `ad47d10` with `5274d9c`; runtime code commit `22cb92f` was unchanged. This correction is a normal follow-up commit and was not amended or force-pushed. The migration is a ONE-TIME transitional backfill for today's pre-field brief — tomorrow's 7:00 AM run is a fresh compose on a new local day, which stamps `valid_until` natively (line 879), so the defer+migrate path does NOT fire there; a re-auditor should not look for a scheduled migrate trigger.
- Ripple Check: grepped `docs/designs/morning-brief.md`, `docs/`, and `dashboard/index.html` for post-delivery immutability / idempotency claims that the in-place backfill could contradict — none found. `index.html` `guard()` already reads `valid_until` (consistent with the fix, unchanged); the design doc's "idempotent events" (line 112) is about decision state, not the brief sidecar. No dependent doc drifted; none needed updating.
- Note for re-audit: `install-stamp.json` records `head_sha 22cb92f` (the runtime commit) while branch HEAD advances to the docs/Work-Record commit. This is correct — the docs commit changes no runtime file, so the installed runtime legitimately originates at `22cb92f`, and `verify_install_stamp` compares content sha256 (not head_sha equality), so it stays green. No reinstall needed.
- by: Claude Code headless worker (Opus 4.8), session `a6a7c236-5f88-4a13-b395-265d8c64767a`, spawned by Claude Fable governor `e85411a1` (handshake `5bffc31d`).
- triggered by: ER-109 benefit pilot governor handoff to fix the single P1 from the Morning Brief merge-gate re-audit (`019fd4d4`).
- led to: the merge-gate P1 is fixed and pushed, pending the Codex re-audit that confirms it; the pre-existing Phase 6 live-proof gates and the unrelated automation/chats reds remain open.
- linear: repo-only; no Mission Control Linear team is configured.
- **ID correction (2026-07-11, note only, no history rewrite):** commit `5274d9c`'s message and the entries above (this section's title, `Diagnosis inputs`, `Implementation inputs`, and `triggered by`) cite Codex re-audit id `019fd4d4`. The correct id is `019f4fd4`. The existing text is left as originally written; this note is the correction of record. Found while implementing ER-109 round 5 (P2, full-ingest freshness duplication) per Codex re-audit thread `019f5010-185b-79f2-8767-4f9a5f457e64`.

### 2026-07-10 — Morning Brief round-2 trust defects (audit 019f4f6c reconciliation, Phase 0 item 1)
- Problem: The Codex audit `019f4f6c` reconciliation record (`global-implementations/records/verification/2026-07-10-audit-019f4f6c-orchestration-review.md`) found three brief trust defects still live in the installed runtime after fix round 1, plus an install-provenance gap: (a) the brief's `_input_health` read only the envelope `generated_epoch`, so it could report the chats input fresh while the last full transcript ingest was ~23h stale (the dashboard already surfaced this, the brief did not); (b) the brief feed carried only a 300s poll cadence, so `dashboard status` flagged a same-day brief `stale` (rc=1) after ~10 minutes and `index.html` desaturated it after ~2x cadence — a daily product judged on a poll clock; (c) `_open_changes` labeled every `change_type=="resolved"` row a plain "Resolved" regardless of `resolution_evidence_type`, so a `fork_dedup` consolidation and a `source_absent` inference both read as an evidence-backed resolution; and (Also, finding 2) the install step copied runtimes from the working tree, so a post-install fix that failed review would leave the runtime drifted from any committed SHA.
- Reasoning: Kill the envelope-vs-nested staleness class at the root by extracting ONE shared `feed_health()` into `mission_control_common.py` that folds in nested full-ingest staleness AND a daily product's `valid_until` window, then make the dashboard's `_verdict`/`_chats_stale_ingest` and the brief's `_input_health` thin adapters over it so no caller can forget a class again. Separate product validity from poll cadence by stamping `valid_until` (next local midnight) in the brief sidecar and honoring it in both status and the JS guard. Reserve "Resolved" for evidence-backed `resolution_evidence_type` values and mark the rest honestly. Make install source from the committed HEAD SHA and stamp a content hash + head SHA that status and the deadman re-verify without needing git. Same-local-day validity is shared with the deadman so a brief cannot be valid to one surface and stale to another. Deadman's `_send`/mobile-connect path (design D10) was deliberately untouched.
- Diagnosis inputs: the reconciliation record above (Phase 0 item 1 + finding 2); `scripts/morning-brief` (`_input_health`/`_open_changes`/`_compose_locked`/`_same_local_day`); `scripts/dashboard` (`_wrap`/`_verdict`/`_chats_stale_ingest`/`cmd_status`/`do_install`); `dashboard/index.html` per-feed `guard()`; `scripts/chat-graph` for the `resolution_evidence_type` enum (`manual`, `same_session_explicit`, `downstream_explicit`, `downstream`, `legacy` = evidence-backed; `fork_dedup`, `source_absent` = not); the five test suites for red-before-green shaping.
- Implementation inputs: branch `feature/morning-brief-activation`; `scripts/mission_control_common.py` gained `feed_health`, `nested_ingest_stale`, `same_local_day`, `next_local_midnight`, `write_install_stamp`, `verify_install_stamp`; `scripts/dashboard` `_verdict`/`_chats_stale_ingest` delegate to `feed_health`, `_wrap` copies `valid_until` into the brief envelope, `cmd_status` prints an `install` verify row (red on drift), `do_install` sources from `git show HEAD:scripts/<f>` (worktree fallback only when `REPO_ROOT` is not a git repo) and stamps `bin/install-stamp.json`; `scripts/morning-brief` `_input_health` delegates to `feed_health` (1x cadence, no aging, nested_stale ⇒ stale), `_open_changes` keys off a new `_resolution_prefix`, `_compose_locked` stamps `valid_until`, and `_same_local_day` aliases the shared helper; `scripts/morning-brief-deadman` consumes the shared `same_local_day` and records the install-stamp verdict in `deadman-last-check.json`; `dashboard/index.html` `guard()` treats a feed as fresh while `now < valid_until`; regressions added to `scripts/morning-brief.test.sh` (nested freshness + evidence-honest wording) and `scripts/dashboard.test.sh` (`c20` daily-brief validity in status, `c21` committed-HEAD provenance + drift detection).
- Fix: landed all three trust defects + the install-provenance stamp as commit `ed95cce`, pushed the feature branch, reinstalled the runtime from that exact SHA with launchd untouched, and hash-verified every installed runtime against the committed HEAD.
- Self-audit:
  - method: proved each new regression RED against the committed pre-fix code before implementing (brief suite showed 2 FAILED; the committed dashboard printed `brief 7200s ok stale` for a same-day brief and wrote no `install-stamp.json`), then GREEN after; cold-reran all five suites; reinstalled with `DASHBOARD_INSTALL_NO_LAUNCHD=1` from HEAD and compared `shasum -a256` of each installed `bin/` runtime against `git show HEAD:scripts/<f>` (dashboard compared HEAD+baked-REPO_ROOT); ran the live `dashboard status` and the deadman once to confirm the stamp verifies and no alert fired.
  - outcome: `mission-control-common` PASS=1 FAIL=0; `morning-brief` ALL PASS (incl. 2 new); `morning-brief-delivery` ALL PASS; `morning-brief-deadman` ALL PASS; `dashboard --require-shell` PASS=32 FAIL=0 (incl. `c20`, `c21`) — all cold. Installed `bin/` hashes all matched HEAD `ed95cce` (`morning-brief fc9d9200…`, `deadman 816ba32c…`, `common 95f5f755…`, `decision-alert d418936e…`, `dashboard 7a8c7c3b…`). `install-stamp.json`: provenance `head`, head_sha `ed95cce598149c…`, 5 files. `dashboard status` install row: `install ed95cce59814 ok verified (head)`. Deadman `deadman-last-check.json`: `install_stamp {present:true, ok:true, head_sha ed95cce…, provenance head}`, `result healthy`, `alerted false` (today's brief `20260710-e0b7a9ca4b16` delivered 2/2, same local day — no notification sent).
  - did not verify: the single Codex re-audit covering round 1 (`2c9d1a1`) + round 2 (`ed95cce`) — still owed before merge; the natural 7:00 AM fire on real cadence; the five-morning comprehension proof. The deadman mobile-connect independence (design D10) was intentionally not touched (separate queued item). No merge to main.
- Conscious deviation from the reconciliation plan wording ("feed_health used by dashboard + brief + deadman"): `feed_health()` is consumed by the dashboard and the brief only. The deadman judges brief *delivery* from `latest.json`, not feed envelopes, so it has no feed to health-check — it shares the validity helper (`same_local_day`) and the install-stamp verifier instead, which is the real duplication the plan targeted. This is a deliberate scope call, not an incomplete item.
- Note for re-audit: `install-stamp.json` records `head_sha ed95cce` while branch HEAD advanced to `86bb28b` (the docs/Work-Record commit). This is correct — `86bb28b` changed no runtime file, so the installed runtime legitimately originates at `ed95cce`, and `verify_install_stamp` compares content sha256 (not head_sha equality), so it stays green. No reinstall needed.
- by: Claude Code headless worker (Opus 4.8), session `287a0123-d795-4040-99f0-3b963769b984`, spawned by Claude Fable governor `e85411a1` (handshake `6293661c-025e-4849-a478-fcdde79f0529`).

### 2026-07-10 — Morning Brief render defect fixes (Codex audit of 0312a3a: P1 privacy + P1 comprehension + two P2 provenance)
- Problem: A Codex audit (thread `019f4f5f-6917-7331-a4e6-d8b2e7e9da67`) of activation commit `0312a3a` found four render-lane defects, two verified with synthetic behavioral proofs: (P1 privacy) `_needs_you` clipped decision text with `_headline` BEFORE `_sanitize_line` ran the shared egress policy, so a sensitive line whose secret/email sat past the clip boundary survived as a partial, "safe"-looking headline; (P1 comprehension) the live `latest.md` NEEDS YOU block ran 5 decision rows + 5 action rows + a collapse line (~1.4 KB the audit measured, 5,383 bytes on the 2026-07-10 live brief), failing the one-phone-screen bar; (P2) section dedup keyed only on `(text, action_cmd)`, so rows with different trust/source/depth could silently collapse; (P2) markdown rendered `trust` but never the sidecar `source`.
- Reasoning: These are render-path-only defects. The smallest correct slice sanitizes the RAW line before any transform (privacy floor: a clip must never rescue a row the egress policy drops), narrows NEEDS YOU by top-N significance selection with character clipping only (never word/line counts), widens the dedup identity, and adds source to the line render — no composer, delivery, or feed logic touched.
- Diagnosis inputs: Codex audit thread `019f4f5f-6917-7331-a4e6-d8b2e7e9da67` with per-defect synthetic proofs; the parent Fable governor handshake `941a7827`; `scripts/morning-brief` (`_sanitize_line`/`_headline`/`_needs_you`/`_section`/`_render_markdown`/`_compose_locked`); `scripts/mission_control_common.py` egress contract; the live `~/.mission-control/morning-brief/latest.md` (5,383 bytes, 5 NEEDS YOU decisions) as ground truth.
- Implementation inputs: branch `feature/morning-brief-activation`; in `scripts/morning-brief` — `_sanitize_line` gained a `clip=` arg and now sanitizes raw text FIRST then applies `_headline` only to survivors; `_needs_you` passes `clip=chars` instead of pre-clipping, and filters egress-dropped (`None`) rows before the empty check so a feed of only-sensitive candidates still shows the reassuring placeholder and an accurate collapsed count; the NEEDS YOU default cap dropped from 5 to 3 (`MORNING_BRIEF_NEEDS_YOU_MAX`); `_section` dedup key widened to `(text, action_cmd, trust, source, depth)`; `_render_markdown` now renders `_(source)_` per line and the collapse line reads `+K more: dashboard`; two new regression tests in `scripts/morning-brief.test.sh` (a compose-level fail-closed proof that a past-boundary secret drops the whole NEEDS YOU row, and a unit invariant that `clip` never rescues a dropped row).
- Fix: repaired all four audited defects in the render lane, added the required fail-closed regression, reinstalled the runtime `bin/` copy so the 7:00 AM agent runs the fixed code, and recomposed safely (idempotency deferred because today's brief was already delivered — no duplicate Telegram send).
- Self-audit:
  - method: cold-reran the three required suites after edits plus the two new regressions; ran a standalone synthetic proof contrasting the old clip-before-sanitize path (leaks a shortened safe headline) against the new sanitize-before-clip path (drops the row); `diff`+`shasum` on the installed runtime bin vs the repo script; ran a real `morning-brief` compose to confirm idempotency deferral; a read-only `--print` preview to eyeball the fixed NEEDS YOU (3 rows + `+11 more: dashboard` + per-line source).
  - outcome: `bash scripts/morning-brief.test.sh` (10 checks incl. 2 new), `bash scripts/morning-brief-delivery.test.sh`, and `bash scripts/morning-brief-deadman.test.sh` all `ALL PASS` cold; synthetic proof printed old→`'Pick the deploy window … (options in dashboard)'` vs new→`None`; installed `~/.mission-control/bin/morning-brief` SHA `6580840a8c7da46f...` == repo script; `morning-brief` returned `compose deferred while delivery state is delivered: 20260710-e0b7a9ca4b16` (delivery state unchanged, 2/2 chunks); `--print` showed NEEDS YOU narrowed to 3 rows with source suffixes and no clipped-prefix leak.
  - did not verify: the natural 7:00 AM fire on real cadence (next scheduled run); the five-morning comprehension proof; the notification-consolidation decision. Today's already-delivered `latest.md` was intentionally NOT overwritten (idempotency) — the fixed rendering first reaches a delivered artifact at the next fresh compose.
- by: Claude Code headless worker (Opus 4.8), session `5c6e42c0-6874-40d6-98d4-0633ce2077e0`, spawned by Claude Fable governor `e85411a1` (handshake `941a7827`).
- triggered by: ER-109 benefit pilot governor handoff to fix the Codex audit findings on `0312a3a` and prepare for re-audit.
- led to: re-audit-ready render lane; the Phase 6 live-proof gates from the activation slice remain open and unchanged.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-10 — Morning Brief activation slice (first real brief + launchd install + deadman proof)
- Problem: ER-109 slice 3 needed the already-built Morning Brief to actually run for the first time — no `~/.mission-control/morning-brief/` briefs had ever been composed-and-sent, and no `com.gillettes.morning-brief` launchd agent was installed — plus a render pass against the one-phone-screen comprehension bar before turning it loose on a schedule.
- Reasoning: The composer, deadman, and delivery code were review-clean and tests passed, so the smallest correct slice was to (a) fix only render defects in the brief copy lane, (b) reuse the audited `dashboard install` machinery for the runtime `bin/` copy, and (c) install exactly the two named agents with fully-expanded paths, verifying real job success rather than mere launch. No composer/delivery logic was rewritten.
- Diagnosis inputs: Fable Morning Brief plan Phase 4/6 delivery + gates; ER-109 implementation-packet execution addendum (patches U2/U3/U4); `scripts/morning-brief`, `scripts/morning-brief-deadman`, `scripts/dashboard` install path, `launchd/*.plist.template`; a real `dashboard collect --force` + `morning-brief --print` reviewed against the comprehension bar; a pre-work advisor consult.
- Implementation inputs: branch `feature/morning-brief-activation`; render fixes in `scripts/morning-brief` — NEEDS YOU lines collapse to bounded, pipe-free headlines with an `(options in dashboard)` marker (`_headline`), section render dedups identical lines, and raw `Spawned-by:` worker-handshake lines are dropped as non-outcomes; a new truncation fixture in `scripts/morning-brief.test.sh`; the launchd label collision recorded in `docs/designs/morning-brief.md`.
- Fix: reviewed and repaired brief readability; delivered the first real brief; installed and verified the 7:00 AM brief and 7:20 AM deadman launchd agents; proved the deadman fires on a suppressed brief.
- Self-audit:
  - method: cold-reran the three required suites after edits; ran a real `--send` and inspected the delivery receipt; verified each launchd job's actual `last exit code` via `launchctl print` and its run log (not just kickstart rc); ran the deadman against an isolated scratch `MISSION_CONTROL_HOME` with the real sender.
  - outcome: `bash scripts/morning-brief.test.sh`, `bash scripts/morning-brief-delivery.test.sh`, and `bash scripts/morning-brief-deadman.test.sh` all `ALL PASS` cold; brief `20260710-e0b7a9ca4b16` composed and delivered (2/2 Telegram chunks confirmed; dated file `~/.mission-control/morning-brief/2026-07-10.md`; receipt `state=delivered`); both agents bootstrapped with fully-expanded paths and `last exit code = 0` (brief short-circuited to `delivery already complete`, deadman `healthy`); the suppression test returned `result=missing, alerted=true` (exit 1) through the deadman's independent path; rendered plists carry zero `~` and zero `__PLACEHOLDER__`.
  - did not verify: natural 7:00/7:20 AM fires on real cadence; the five-morning comprehension/noise proof; the notification-consolidation decision. Historically this activation slice alerted through mobile-connect. That gap was superseded on 2026-07-11 by the direct Keychain-capable standard-library sender; mobile-connect remains an explicit compatibility/test override.
- by: Claude Code headless worker (Opus 4.8), session `adc5fd6c-0fcf-4b77-b676-c22de3aba1a0`, spawned by Claude Fable governor `e85411a1`.
- triggered by: ER-109 benefit pilot slice 3 (activation) worker request.
- led to: remaining ER-107/ER-109 gate is Phase 6 live-proof — ~5 natural mornings + comprehension check-ins + the notification-consolidation decision (which must weigh the `com.gillettes.morning-brief` vs `com.screenpipe.morning-brief` label collision); outcome-extractor launchd job (LLM leg, 6:40/6:47/6:54 AM) was left inactive as a governor decision — without it the live-proof "What happened" section will not refresh on schedule.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-11 — Morning Brief P9 job history slice
- Problem: ER-109 slice 2 needed the P9 automation tab job-history/streaks/next-run behavior proven and recorded before the next Morning Brief slice.
- Reasoning: The P9 implementation was already present on `main` in `scripts/automation-status`, `dashboard/index.html`, and the automation fixture/smoke coverage, so the smallest correct slice was to verify the existing behavior, fix the test drift that blocked the required suite, and record the result instead of duplicating product code.
- Diagnosis inputs: Fable Morning Brief plan Phase 1, ER-109 implementation packet addendum, `docs/IMPROVEMENTS.md` P9, current `scripts/automation-status`, `scripts/automation-status.test.sh`, `dashboard/index.html`, `dashboard/fixtures/automation.json`, and `scripts/dashboard-render-smoke.js`.
- Implementation inputs: branch `feature/morning-brief-p9-job-history`; fixed `scripts/dashboard.test.sh` status stubs so full-envelope feeds use current test timestamps instead of aging into false stale failures; marked `docs/IMPROVEMENTS.md` P9 as implemented by the Morning Brief lane.
- Fix: preserved the existing P9 product implementation and repaired the required dashboard shell suite's date-sensitive all-green status case.
- Self-audit:
  - method: reran the required P9 slice verification commands after the test repair and checked the P9 source paths against the requested spec.
  - outcome: `bash scripts/automation-status.test.sh`, `REPO_ROOT=$PWD bash scripts/dashboard.test.sh --require-shell`, and `node scripts/dashboard-render-smoke.js .` passed; automation history persistence, next-run schedule math, failure streaks, renderer mini-strip, and `Run now` copy are covered.
  - did not verify: live launchd kickstart execution or a natural future automation run because this slice is code/test proof only.
- by: Codex thread `019f4f3d-3034-7283-b63c-e4e8890b51f1`.
- triggered by: ER-109 benefit pilot slice 2 worker request.
- led to: slice 3 should be U1 thin walking-skeleton brief; repo remains implemented-pending-proof for elapsed/live Morning Brief gates.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-09 — Morning Brief bounded Tier 2 outcome enrichment
- Problem: Fable correctly made model-assisted outcome plainness part of Morning Brief, but a literal free-form rewrite could invent commands, identifiers, repositories, or authoritative claims; the first closed-taxonomy implementation then became too generic to explain which session or audit did what.
- Reasoning: Use the model only where it adds value—as a bounded classifier—while deterministic graph/session facts carry identity, repo, audit/spawn/continuation relationships, commits, actions, and confirmed decisions. Keep all live transcript egress and scheduling behind explicit authorization.
- Diagnosis inputs: Fable transcript `35d96de4-9509-4382-b1a0-10b9a4d1777e`; plan-author session `6f306a0b-abbb-4d39-9d64-afa7fb977250`; exact plan file; current Mission Control code/specs; repeated counterexamples from `/root/outcome_audit` and `/root/outcome_audit/tier2_challenger`.
- Fix: added isolated high-value Tier 2 extraction with fixed Haiku/Sonnet routes, exact response schema, canonical code-only cache, strict production/sample/test config parity, hard caps, OAuth defer, private health/attempt state, Tier 1 fail-open, deterministic title/repo/lineage rendering, session-specific late updates, and a separate inferred/non-actionable follow-up lifecycle that preserves human dispositions and rolls back per session.
- Self-audit:
  - method: full cold Mission Control matrix, Python/shell/JSON/plist/static checks, strict OpenSpec, HOTL lint, synthetic provider-shaped privacy/config/concurrency counterexamples, and iterative independent Codex/challenger re-audits.
  - outcome: review-clean with no current P0–P3 findings; implementation `df991b4` and audit records `ef281a5` are pushed to `origin/main`; dashboard `PASS=30 FAIL=0`, runner `PASS=32 FAIL=0`, all seven tabs render, code-only install preserved every LaunchAgent hash, and the installed disabled extractor path made zero calls.
  - did not verify: real provider egress, observed token/latency calibration, Telegram receipt/deadman, LaunchAgent activation, or five natural mornings.
- by: Codex thread `019f4963-1e75-7600-8a17-1e6f6f8e8ca6` with independent reviewers `/root/outcome_audit` and `/root/outcome_audit/tier2_challenger`.
- triggered by: Trevor's request to thoroughly audit Fable, implement what survived audit, self-audit, and iterate with another Codex session until complete.
- led to: code-complete L2 enrichment with live authorization and elapsed-proof gates still open; full audit record `records/morning-brief-tier2-codex-audit.md`.

### 2026-07-09 — Morning Brief orchestration convergence
- Problem: Fable's plan mixed a sound Mission Control product direction with one stale priority and several high-risk capabilities whose trust, privacy, concurrency, and activation boundaries were not yet proven.
- Reasoning: Make Morning Brief the product that composes session recall, response clarity, durable memory, and open-work orchestration; ship a useful deterministic Tier 1 slice first and keep model egress, external delivery, schedules, and automatic action behind explicit gates.
- Diagnosis inputs: audited Fable session `35d96de4-9509-4382-b1a0-10b9a4d1777e`, linked plan-author session `6f306a0b-abbb-4d39-9d64-afa7fb977250`, U1-U7 plan audit, actual Mission Control/T7/global runtime state, installed browser captures, real fleet dry-run, and independent Codex reviews.
- Implementation inputs: commits `fce2a7a`, `255ad3d`, `ab693c6`, `2f3d38a`, `0523925`, and `2da8c9b`; OpenSpec `morning-brief`; the HOTL workflow; live graph/decision feeds; and records under `records/`.
- Fix: implemented provider-native bounded outcome cards, parser-safe exact resolution, transactional high-recall decisions, deterministic brief enrichment, structured Git facts, and a no-execute dry-run runner; repaired browser-found split decisions, false activation failures, partial token-prefix redaction, and stale next-run precedence; restored the pre-change runtime and reinstalled canonical `main` without installing LaunchAgents; and corrected the independent reviewer's stale generated-HOTL-state finding through the HOTL runtime.
- Self-audit:
  - method: full cold suites, syntax/static/privacy/negative checks, strict OpenSpec and HOTL validation, live parser migration, installed Home/Brief/Automation captures, real 56-repository dry-run, per-record runner review, and separate holistic implementation audit.
  - outcome: review-clean for the deterministic/Tier 1 slice after final adversarial rechecks; dashboard `PASS=29 FAIL=0`, runner `PASS=32 FAIL=0`, 23/23 real runner candidates correctly refused, canonical install at `2432d6e`, unchanged LaunchAgent manifests, and automatic push remains unavailable.
  - did not verify: authorized Telegram/deadman receipt, Tier 2 provider extraction, natural launchd cadence, or approximately five real mornings; these remain explicit external/elapsed gates.
- by: Codex thread `019f4963-1e75-7600-8a17-1e6f6f8e8ca6` with independent reviewers `/root/outcome_audit`, `/root/git_runner_audit`, and `/root/decision_audit`.
- triggered by: Trevor's request to audit Fable thoroughly, implement agreed findings, and iterate with another Codex reviewer.
- led to: mergeable zero-call/Tier 1 Mission Control slice; ER-107 remains implemented-pending-proof rather than verified.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-09 — Morning Brief Phase 0 cross-repo reliability repairs
- Problem: Fable's Morning Brief plan depended on jobs and correction feeds that were not trustworthy: T7-backed LaunchAgents could fail with exit 126, Morning Health scanned stale whole-log denials and could hang on an unbounded census, and the improvement loop mistook provider wrappers for user corrections.
- Reasoning: Repair the owning systems before allowing Mission Control to summarize them; otherwise a polished brief would amplify false or stale status.
- Diagnosis inputs: live launchd arguments/status/log segments, the T7 runtime repo and installer, Morning Health markers, the global improvement-loop queue/lessons/digest, and independent focused reviews.
- Implementation inputs: third-party runtime commits `bbed5e3`, `bd7226e`, `1326217`; global improvement-loop commit `5f01334`; Mission Control automation registry in `ab693c6`.
- Fix: moved all tracked jobs to `/Users/gillettes/Coding Projects/3rd-party-xscan-checkout`; added static/dynamic removable-media validation and transactional installer rollback; bounded the starred-repo census; made Morning Health use a stable last-run marker and current log segment; filtered Claude/Codex provider wrappers; grouped corrections before capping; and atomically quarantined nine proven false signatures while preserving unrelated real work.
- Self-audit:
  - method: owning-repo suites and validators, live selective reinstall with loaded-state restoration, current-segment log inspection, global verification, quarantine rollback tests, and independent code review in each owning repo.
  - outcome: passed; the stable runtime matches pushed `1326217`, intended jobs are loaded from the internal path, the latest Morning Health segment has no exit 126 or fresh TCC denial, and the global improvement-loop branch/main contain `5f01334` with review-clean fixtures.
  - did not verify: future natural launchd schedules or multiple elapsed Morning Health cycles; the current Morning Health result remains honestly flagged by a real existing starred-drain attention state.
- by: Codex thread `019f4963-1e75-7600-8a17-1e6f6f8e8ca6` with independent reviewers `/root/phase0_recon` and `/root/spec_challenger`.
- triggered by: Fable plan Phase 0 and the live audit of its assumptions.
- led to: Mission Control can now consume these sources as repaired inputs; elapsed schedule proof remains separate from implementation completion.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-09 — Morning Brief resumable delivery and privacy convergence
- Problem: The first delivery/deadman implementation passed its happy-path suites but independent adversarial review found correctness, freshness, concurrency, and privacy failures that made live delivery unsafe.
- Reasoning: Treat the first green suite as a draft, convert every reviewer counterexample into a regression, and keep Telegram/launchd side effects gated until the implementation itself is review-clean.
- Diagnosis inputs: OpenSpec/HOTL delivery contracts, the actual diff after `255ad3d`, repeated independent adversarial review, and synthetic sender/deadman/dashboard/error fixtures.
- Implementation inputs: `scripts/morning-brief`, `scripts/morning-brief-deadman`, `scripts/dashboard`, their focused suites, two launchd templates, automation registry/fixture, and the shared egress module.
- Fix: implemented fixed-argv resumable delivery, atomic receipts/cursor, one local send per chunk under concurrent callers, same-day deadman proof, shared compose/send locking, immutable compose markers, fail-closed sidecar timestamps, whole-message privacy screening, content-free egress counters, last-good-plus-current-error UI overlays, and shared ERROR sanitization before JSON/JS persistence. Provider delivery remains at-least-once because provider acceptance before the local receipt write is ambiguous.
- Self-audit:
  - method: repeated red/green adversarial fixtures, full focused cold suites, strict OpenSpec and HOTL lint, render smoke, and independent review after every material finding.
  - outcome: delivery/privacy slice is review-clean; dashboard is `PASS=26 FAIL=0`, and every accepted finding has a regression.
  - did not verify: live Telegram receipt, installed Mission Control launchd cadence, safe live deadman exercise, canonical installed browser capture, rollback, session outcomes, decisions, safe runner, or five natural mornings.
- by: Codex thread `019f4963-1e75-7600-8a17-1e6f6f8e8ca6` with independent reviewer `/root/mission_mapping`.
- triggered by: Fable plan audit and Trevor's request to implement agreed findings until complete.
- led to: authorized-live-delivery gate next; outcome extraction, decision queue, and runner remain subsequent OpenSpec phases.
- linear: repo-only; no Mission Control Linear team is configured.

If it's not here, it isn't remembered.
Use one entry per bounded task, fix, audit, or review that would otherwise lose reasoning between chats.

```md
### YYYY-MM-DD — short title
- Problem:
- Reasoning:
- Diagnosis inputs:
- Implementation inputs:
- Fix:
- Self-audit:
  - method:
  - outcome:
  - did not verify:
- by:
- triggered by:
- led to:
- linear:
```

### 2026-07-09 — Morning Brief trusted substrate and deterministic thin brief
- Problem: The approved Morning Brief plan depended on trustworthy open-work changes, distinct job runs, field-aware privacy, and a useful brief that did not wait for model-based outcome extraction.
- Reasoning: Ship the deterministic product slice first so Mission Control is useful when models are disabled, deferred, or over budget; keep transcript/model enrichment downstream of privacy and coverage calibration.
- Diagnosis inputs: Fable plan `019f4550-2a9a-7fe3-9313-9e7a0be10b35-tha-cuddly-hopcroft.md`, U1-U7 upgrade audit, OpenSpec `morning-brief`, existing graph/automation/dashboard suites, and synthetic privacy fixtures.
- Implementation inputs: `scripts/mission_control_common.py`, `scripts/chat-graph`, `scripts/automation-status`, `scripts/scan-unfinished-work`, `scripts/morning-brief`, `scripts/dashboard`, dashboard fixtures/renderers, and the governed OpenSpec/HOTL records.
- Fix: added graph schema v5 with provider/node hygiene and explicit resolution evidence; added bounded open-work changes; added distinct-run automation history; added minimal recent Git facts; composed atomic Markdown plus `latest.json`; added preview routing, a dashboard Brief feed, Home summary, and full Brief tab.
- Self-audit:
  - method: red/green synthetic suites, schema migration fixtures, repeated-poll/concurrency tests, equal-timestamp and mid-compose event tests, dashboard shell/render smoke, strict OpenSpec/HOTL validation, and file-browser captures of Home and Brief.
  - outcome: passed for the implemented slice; checkpoint `fce2a7a` is pushed, and the thin-brief follow-up is pending its next commit.
  - did not verify: delivery receipts/deadman, session outcome extraction, decision queue, safe runner, live canonical install, or five real mornings; those remain explicitly later phases.
- by: Codex thread `019f4963-1e75-7600-8a17-1e6f6f8e8ca6` with bounded subagent implementation/review slices.
- triggered by: audit and implementation of Fable session `35d96de4-9509-4382-b1a0-10b9a4d1777e` and its linked plan-authoring session.
- led to: delivery/deadman tests next, followed by outcome coverage and enrichment only after privacy review.
- linear: repo-only; no Mission Control Linear team is configured.

### 2026-07-08 — One loose-ends ledger
- Problem: Mission Control still had several places where open work could hide: chat handoffs, repo todo files, dirty Git state, nightly findings, and request-register rows were not converging into one reader-facing list.
- Reasoning: Trevor's loose-ends/autonomy request should not become another parallel tracker. The safe first step is to make `scripts/chat-graph` the single ledger and keep the dashboard as a reader of that ledger.
- Diagnosis inputs: Claude chat `1ef98716-30bb-4530-80e0-6b2f3fa74f79`, `docs/IMPROVEMENTS.md` P13/P14 specs, `scripts/chat-graph`, `dashboard/index.html`, chat fixtures, and the current V1 UI bundle.
- Implementation inputs: `scripts/chat-graph`, `scripts/chat-graph.test.sh`, `dashboard/index.html`, `dashboard/fixtures/chats.json`, `scripts/dashboard-render-smoke.js`, `PROJECT_INTENT.md`, and `docs/IMPROVEMENTS.md`.
- Fix: added `first_seen_at` to `open_ends`; added `todo_open`, `nightly_finding`, `repo_dirty`, and `register_open` collectors; exported a flat `data.loose_ends` list with action hints, age, severity, and dismiss command; made Home and Chats/Open work read the flat list; kept the Map recent-chat journal in the connected-chat view.
- Self-audit:
  - method: focused unit/regression tests for each collector and auto-resolve path, fixture-backed render smoke, dashboard shell tests, installed dashboard refresh, and browser capture before closeout.
  - outcome: passed; graph, dashboard, automation, usage, scan, syntax/json, whitespace, installed refresh, and browser capture checks all passed.
  - did not verify: P14 end-of-day autonomous execution, because it is intentionally queued behind the decision-boundary work.
- by: Codex thread `019f2b73-9811-7392-b511-201c1f109997`.
- triggered by: Trevor asked to finish the audit gaps from chat `1ef98716-30bb-4530-80e0-6b2f3fa74f79` and added a one-to-five-line connected-chat journal requirement.
- led to: P14 remains queued after ER-090/P12 boundaries; no autonomous fixer was built in this pass.
- linear: self-contained until Linear is configured.

### 2026-07-08 — Map journal action polish
- Problem: The Map recent-chat journal existed and was capped to five rows, but the rows did not offer a direct plain-word way to jump from "what did I touch?" into that chat's connection web. Home's compact session monitor also still used shorter action labels than Map and Chats.
- Reasoning: Trevor asked for a next-day memory aid in the connected-chat view. The smallest useful improvement is to keep the existing feed-driven journal, add a "Show connections" action, and align action labels without adding a new collector or summarizer.
- Diagnosis inputs: Claude Code chat `1ef98716-30bb-4530-80e0-6b2f3fa74f79`, Trevor's current journal request, `PROJECT_INTENT.md`, `notes/DIRECTION-2026-07-04.md`, `dashboard/index.html`, `dashboard/fixtures/chats.json`, and `scripts/dashboard-render-smoke.js`.
- Implementation inputs: `dashboard/index.html`, `dashboard/fixtures/chats.json`, `scripts/dashboard-render-smoke.js`, and independent Codex audit worker `019f4309-f85e-72a3-9e3e-92e6281b2222`.
- Fix: changed Home session-monitor actions to `Reopen this chat` / `Read transcript`; added `Show connections` to each Map journal row; strengthened render smoke so Home and Chats must cover both plain action labels and Map must keep the journal connection action.
- Self-audit:
  - method: full local suite rerun, fixture-backed render smoke, JSON/syntax/whitespace checks, and browser capture of the demo Map with the vendored graph library loaded.
  - outcome: passed; independent follow-up audit returned `review-clean`.
  - did not verify: future Map search/timeline, Home triage scoring, title enrichment, or autonomy/routing packets; those remain queued work.
- by: Codex thread `019f2b73-9811-7392-b511-201c1f109997`.
- triggered by: Trevor asked for a tiny one-to-five-line journal/log in the connected-chat view so the next day he can remember what chat he used and when.
- led to: no new active follow-up; broader Map and routing/autonomy upgrades remain in the existing queue.
- linear: self-contained until Linear is configured.

### 2026-07-08 — Audit-loop wording polish
- Problem: The Open work command label said `Dismiss this item`, which could read like a permanent removal even when the item came from a live source such as `todo.md`.
- Reasoning: Source-backed work should stay honest: hiding it is a short-term local action, and the item must return on the next refresh if the source still says it is open.
- Diagnosis inputs: backup Codex audit loop risk check on commit `838358a`, `dashboard/index.html`, `scripts/chat-graph`, `scripts/chat-graph.test.sh`, and `scripts/dashboard-render-smoke.js`.
- Implementation inputs: `dashboard/index.html`, `scripts/chat-graph.test.sh`, `scripts/dashboard-render-smoke.js`, and this `todo.md`.
- Fix: changed the visible command from `Dismiss this item` to `Hide until refresh`, and added a regression that manually resolving a `todo_open` item hides it once but the collector restores it when the unchecked todo remains.
- Self-audit:
  - method: reran the graph suite, dashboard shell suite, and render smoke after the copy/test change.
  - outcome: passed; `chat-graph.test.sh`, `dashboard.test.sh --require-shell`, and `dashboard-render-smoke.js` all passed.
  - did not verify: a live browser recapture after this copy-only follow-up; previous installed Home/Map/Open work captures remained valid for the broader UI.
- by: Codex thread `019f2b73-9811-7392-b511-201c1f109997`.
- triggered by: independent Codex audit loop risk surfaced during closeout for commit `838358a`.
- led to: no new active follow-up.
- linear: self-contained until Linear is configured.

### 2026-07-08 — Audit-loop severity rules
- Problem: The P13 Open work ledger exported a `severity` field, but the independent audit found that the documented red/amber/grey rules were not actually encoded.
- Reasoning: Home and Chats can only be trusted as a daily command surface if urgent items are ranked by computed evidence, not by a mostly constant amber default.
- Diagnosis inputs: Codex audit worker `019f42ec-0968-7091-a331-9944b5f2ef92`, `docs/IMPROVEMENTS.md`, `scripts/chat-graph`, and `scripts/chat-graph.test.sh`.
- Implementation inputs: `scripts/chat-graph`, `scripts/chat-graph.test.sh`, `docs/IMPROVEMENTS.md`, and this `todo.md`.
- Fix: implemented export-time severity rules for P0/security register rows, detached/divergent Git state, stale handoffs older than 21 days, and unpushed Git work; added a graph export regression for each documented case.
- Self-audit:
  - method: ran the full graph suite, dashboard shell suite, render smoke, syntax check, and whitespace check.
  - outcome: passed; the new severity regression and all dashboard render checks passed.
  - did not verify: live provider usage sources or future launchd cadence; not touched by this fix.
- by: Codex thread `019f2b73-9811-7392-b511-201c1f109997`.
- triggered by: independent Codex audit finding on commit `838358a`.
- led to: no new active follow-up.
- linear: self-contained until Linear is configured.

### 2026-07-04 — 207d88dd Cursor patch closeout
- Problem: Cursor chat `207d88dd-4abf-4328-a66a-aa55a8801d21` implemented a useful audit-fix patch but left it dirty, uncommitted, unpushed, and under-tested for several repaired failure modes.
- Reasoning: the existing patch addressed real dashboard/chat-graph safety and honesty issues; the safest closeout was to keep the direction, tighten the remaining command-execution edge, add focused regression checks, run release evidence, and land the whole patch with repo-visible records.
- Diagnosis inputs: Cursor transcript `207d88dd-4abf-4328-a66a-aa55a8801d21`; prior Codex audit record and commit `31ee124`; dirty diff across dashboard, chat graph, dashboard CLI, scan, and usage snapshot files; Mission Control AGENTS/project docs; release-gate suites.
- Implementation inputs: `dashboard/index.html`, `scripts/chat-graph`, `scripts/dashboard`, `scripts/scan-unfinished-work`, `scripts/usage-snapshot`, `scripts/chat-graph.test.sh`, `scripts/dashboard.test.sh`, `scripts/dashboard-render-smoke.js`, and new `scripts/usage-snapshot.test.sh`.
- Fix: completed the dashboard/chat-graph honesty fixes; removed shell execution from dashboard feeders and credit notifications; made usage notifications reject shell metacharacters and split without glob expansion; preserved tab-containing branch parsing; surfaced stale/skipped chat ingest, normalized usage status, scoped Map edges, and yellow automation jobs; added durable regression tests for the fixed behaviors.
- Self-audit:
  - method: re-read the diff, added regression tests for each high-risk fixed path, ran the local release gates, installed the dashboard runtime, collected live feeds, took a browser capture, scanned the diff for secret-token patterns, and checked final git state before landing.
  - outcome: passed; local suites and installed-browser proof succeeded, and the active `207d88dd Cursor patch closeout` queue item is closed.
  - did not verify: live launchd cadence over future intervals and live provider-side usage percentages beyond the current local feed snapshot.
- by: Codex thread `019f2ddb-4d45-79b2-9ed7-da8303fb3439`.
- triggered by: Trevor request to thoroughly fix and audit after the Cursor chat audit.
- led to: no new active follow-up; ER-089 and ER-090 remain separate product-scope queue items.
- linear: self-contained until Linear is configured.

### 2026-07-04 — Dashboard search-audit V1 UI bundle
- Problem: the search audit surfaced the first concrete Mission Control UI/routing pass still missing from the dashboard: per-panel source honesty, decision-oriented usage cards, clearer Map detail, better needs-you wording, a compact session monitor, and an activity heatmap.
- Reasoning: this was the top unblocked queue item, and it improved Trevor's actual operator questions without adding dependencies or starting the riskier ER-089/ER-090 work first.
- Diagnosis inputs: `PROJECT_INTENT.md`, `notes/DIRECTION-2026-07-04.md`, `records/2026-07-04-dashboard-coding-tracker-search-audit.md`, `dashboard/index.html`, `dashboard/fixtures/*.json`, `scripts/dashboard-render-smoke.js`, and the local dashboard test suite.
- Implementation inputs: `dashboard/index.html`, `dashboard/fixtures/usage.json`, and `scripts/dashboard-render-smoke.js`.
- Fix: added per-panel provenance/source lines across Home/Map/Chats/Git/Usage/Automation; rewrote Usage around decision cards with source/trust/next-action copy; upgraded the Home needs-attention rows to show problem, impact, next action, and source age; added a compact session monitor plus a local chat-activity heatmap; improved the Map side panel with summary cards, grouped connections, confidence/source chips, and honest focus/filter wording; aligned usage fixture source labels with the real feeder outputs; extended render smoke coverage for the new sections.
- Self-audit:
  - method: re-read the changed dashboard shell against the search-audit requirements, ran `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell` and `node scripts/dashboard-render-smoke.js .` after each substantive audit fix, and ran repeated separate code-review passes until no material issues remained.
  - outcome: passed; the dashboard packet is implemented locally, the release gates stayed green, and the follow-up audit no longer found material issues in the changed work.
  - did not verify: installed browser/file:// rendering in a live browser capture during this packet, because this session stayed inside local shell + headless verification only and no new install/runtime capture was produced here.
- by: Codex thread `631e0a16-efe8-4377-aa81-0d02783eea59`.
- triggered by: Trevor asked to execute the repo's next required work beginning with the bounded Dashboard V1 bundle and to ensure it was audited and tested.
- led to: ER-089 and ER-090 remain the active follow-up queue; this packet removed the top local/offline UI bundle blocker ahead of them.
- linear: self-contained until Linear is configured.

### 2026-07-04 — GitHub dashboard/coding-tracker search audit
- Problem: Trevor asked for a thorough search audit of GitHub repositories matching `dashboard coding tracker` to find backend, frontend, workflow, wording, and visual patterns that could make Mission Control more useful and easier to digest.
- Reasoning: the GitHub search is very broad and star count is noisy, so the useful work was to sweep the top result set, filter for Mission Control fit, deep-read high-signal candidates, and preserve pattern-level recommendations without installing third-party tools.
- Diagnosis inputs: GitHub search API result count about `18.5k` with `incomplete_results=false` (`18474` latest run; `18475` earlier same-day run); top `1000` API results; README/license/metadata deep reads; README screenshot/asset-reference reviews for CodeBurn, TokenTracker, CodexBar, AgentsView, abtop, and builderz Mission Control; existing Mission Control project intent and queue.
- Implementation inputs: `records/2026-07-04-dashboard-coding-tracker-search-audit.md`, existing third-party source cards for Tabler, Claude-Code-Agent-Monitor, agent-sessions, claude-view, and ccusage, plus the active ER-089/ER-090 queue boundaries.
- Fix: wrote a source-only audit record that recommends compact provider/quota cards, usage attribution drill-downs, active-session monitoring, explicit data-source confidence labels, a later git-yield lens, and mobile-friendly state summaries while rejecting framework rewrites, remote-control surfaces, and generic dashboard kits for v1.
- Self-audit:
  - method: compared external candidates against Mission Control's local/offline product constraints, separated direct patterns from install/adoption risk, and kept the recommendations tied to current tabs and feed boundaries.
  - outcome: passed; the audit produces an implementation queue without changing runtime behavior or adding third-party dependencies.
  - did not verify: cloned/runtime behavior of third-party repositories, credentialed provider usage APIs, paid services, code-level license reuse, or new third-party source-card closeout; the shared third-party index had unrelated dirty top-level source-card/intake work, so this pass preserved findings in the Mission Control record instead of mixing commits.
- by: Codex thread `019f2dec-d36b-7093-8259-8d9df30dede0`.
- triggered by: Trevor request to investigate the GitHub search URL for Mission Control improvement ideas.
- led to: keep ER-089 focused on safe usage/provider status and add the search-audit UI patterns to the next Mission Control polish/routing implementation pass.
- linear: self-contained until Linear is configured.

### 2026-07-04 — ER-087 Mission Control follow-up gaps
- Problem: the standalone repo had the working dashboard code but not enough repo-local governance, product-owner wording, stale full-ingest honesty, or real Map render coverage.
- Reasoning: Trevor's `1ef98716` feedback and `notes/DIRECTION-2026-07-04.md` made Phase 2/4 clarity the blocker before usage-routing/autonomy work.
- Diagnosis inputs: audited chat `*Chat connection tracking tool` (`1ef98716-30bb-4530-80e0-6b2f3fa74f79`), `notes/DIRECTION-2026-07-04.md`, dashboard source, test suites, and browser captures.
- Implementation inputs: `PROJECT_INTENT.md`, dashboard copy, chat-graph export counts, dashboard status freshness check, render smoke, repo governance scaffold, and ER-089/ER-090 queue entries.
- Fix: wrote plain-language `PROJECT_INTENT.md`; added required governance files; changed visible labels across Home/Map/Chats/Git/Usage/Automation; hid raw chat commands behind Reopen/Read labels; surfaced stale full-ingest status; added a real Cytoscape-path smoke test; queued usage-routing/autonomy without implementing them.
- Self-audit:
  - method: source scan for stale labels, full Mission Control test gates, governance validators, and Home/Map/Chats browser captures.
  - outcome: passed; Map overlap found in capture and fixed by showing chat names in the side panel/selected state instead of default canvas labels.
  - did not verify: real provider usage percentages, because ER-089 remains queued pending safe provider credentials/sources.
- by: Codex thread `019f2b73-9811-7392-b511-201c1f109997`.
- triggered by: Trevor follow-up to audited chat `1ef98716-30bb-4530-80e0-6b2f3fa74f79`.
- led to: ER-089 and ER-090 remain active V2 queue items; Linear remains repo-only until configured.
- linear: self-contained until Linear is configured.

### 2026-07-04 — Map recent chat journal
- Problem: Trevor wanted the connected-chat view to show a tiny next-day memory aid: what he touched, when, and which chat it was.
- Reasoning: the existing chats feed already has title, AI, repo, and `last_activity`, so a separate journal collector or summarizer would be extra moving parts.
- Diagnosis inputs: `dashboard/index.html`, `scripts/dashboard-render-smoke.js`, `dashboard/fixtures/chats.json`, and installed Map screenshot.
- Implementation inputs: existing Map renderer, chat fixture feed, and `PROJECT_INTENT.md`.
- Fix: added a `Recent chat journal` section to Map, capped at five newest chats, using existing feed fields only; render smoke now asserts the newest chat title appears on Map.
- Self-audit:
  - method: renderer smoke, dashboard suite, whitespace check, installed-dashboard update, and browser capture.
  - outcome: passed; screenshot shows the five-row journal above the graph.
  - did not verify: whether titles are always the best summary text for every provider; this uses current feed data until a real summarizer is justified.
- by: Codex thread `019f2b73-9811-7392-b511-201c1f109997`.
- triggered by: Trevor requested a one-to-five-line connected-chat journal/reminder.
- led to: no new active follow-up; a title-quality upgrade can wait until titles prove insufficient.
- linear: self-contained until Linear is configured.

## Suggested Recommendation Log
If it's not here, it isn't remembered.
Keep materially new suggestions here so they survive beyond the current chat.
- Forward entry shape should include `date`, `recommendation`, `why`, `by:`, and `linear:`.
- Do not delete old entries; mark them completed, declined, deferred, or superseded with date and chat context.
- Keep audit-created items here only when they are deferred, optional, or not yet execution-ready; otherwise promote them into `## Active Next Steps`.
- When a suggestion comes from an audit or feedback review, link back to the originating audit record or `Feedback Decision Log` entry and later note which chat implemented or declined it.
- 2026-07-12 | recommendation: accepted compatibility debt — migrate `mc-panel.swift` from deprecated `NSUserNotification` APIs to `UserNotifications` only when a supported macOS SDK removes the current API, notifications fail in live use, or the panel already needs a permission-flow redesign. | why: the current binary compiles and runs, while migration would add notification authorization behavior and is not a correctness fix for this local menu-bar surface. | by: Codex thread `019f59f8-bb9e-70c0-9497-a9686ea24154`, from independent UX/test audit. | linear: repo-only; no live Mission Control Linear team is configured.
- 2026-07-11 | recommendation: design an inflight/reconciliation state machine before claiming exactly-once or provider-idempotent Morning Brief chunk delivery. | why: the current delivery contract is at-least-once; if a provider accepts a chunk and the local process crashes before recording the receipt, the retry path must treat it as ambiguous and may resend. The queued state machine should track provider-accepted/local-unconfirmed chunks and reconcile them without changing the current sender in round 8. | by: Codex thread `019f5056-bc55-7043-88ad-b87b1ac48a6f`, governed by Claude `e85411a1-b74b-4556-9939-8eaf5d8f2ea9`. | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-04 | recommendation: pattern-mine the dashboard/coding-tracker search audit during the next ER-089/UI polish pass: provider reset/pace cards, usage attribution tables, active-session state list, source-confidence labels, and a later git-yield lens; do not rewrite Mission Control into Next/React, a remote-control platform, or a generic drag/drop dashboard. | why: the highest-fit repos confirm Mission Control's local/static architecture but reveal better visual and workflow affordances for usage, sessions, and attention routing. | by: Codex thread `019f2dec-d36b-7093-8259-8d9df30dede0`; record: `records/2026-07-04-dashboard-coding-tracker-search-audit.md`. | linear: self-contained until Linear is configured.
- 2026-07-04 | recommendation: pattern-mine `tabler/tabler` during the next Mission Control UI polish pass, especially cards, dense tables, badges, tabs, segmented controls, and offline-safe icon affordances; do not install `@tabler/core` wholesale unless a later implementation proves the static file:// bundle stays simpler and safer. | why: Tabler is a mature MIT Bootstrap dashboard kit, but this repo's product intent is still a single-file local/offline dashboard. | by: Codex thread `019f2d4e-54ba-7273-8188-12506a3daf19`; source card: third-party index `researched-repos/tabler-tabler.md` commit `171a122`. | linear: self-contained until Linear is configured.
- 2026-07-05 | recommendation: do not adopt the GitHub Copilot enterprise-observability stack (OpenTelemetry Collector, Prometheus, Grafana, OpenObserve, Superset, Metabase, Airbyte, Meltano, dbt-core, Great Expectations, TensorZero, Helicone, OpenLIT, traceAI, TraceRoot, Pull Request Analytics Action); treat `records/2026-07-04-dashboard-coding-tracker-search-audit.md` as the real same-niche repo map; if a chart is ever justified, prefer vendorable zero-dependency `leeoniya/uPlot` over Chart.js/ECharts/CDN — but not for V1. | why: Copilot recommended from the repo description alone (it said so); every headline pick runs as a background service, framework, or separate warehouse and collides with the explicit non-goals of offline single-file, single-user, no-server. Full evaluation in Feedback Decision Log 2026-07-05. | by: Claude Code (Opus 4.8) session `a9724039-6595-4205-a25b-bf361020250a`. | linear: self-contained until Linear is configured.

## Active Branch Ledger
### `codex/er156-telegram-flow-routing`
- status: branch-only and paused before producer edits at ER-156's Telegram destination-binding gate
- created: 2026-07-15
- base: `origin/main@f355b5e`
- worktree: `/Users/gillettes/Coding Projects/mission-control-worktrees/er156-telegram-flow-routing`
- source chat: Codex `019f674e-a794-71f0-a6dc-ca861fec4864`, spawned by `019f62d4-a994-7dd3-8993-0734f727df9f`
- last refreshed by chat: 2026-07-15, same
- purpose: route decisions to Control, normal Morning Briefs to Briefs, and the independent deadman to Incidents without destination fallback.
- linked issue: self-contained ER-156; Linear repo-only mode
- plugin mirror: none
- merge expectation: merge to `main`
- merge target: `main`
- review surface: focused delivery/deadman/decision tests, installed proof, path canaries, and coordinator audit
- exit checklist: RED/GREEN; Work/Test records; commit/push; origin/main containment; install; canaries; cleanup
- delete when: after origin/main containment and installed canaries
- retain reason: n/a
- cleanup command: `git worktree remove "/Users/gillettes/Coding Projects/mission-control-worktrees/er156-telegram-flow-routing" && git branch -d codex/er156-telegram-flow-routing`
- linked PR/audit/completion record: coordinator ER-156 verification record

### `codex/er103-git-state-and-morning-proof`
- status: preserved-superseded dirty checkout
- created: 2026-07-11
- base: `main`
- worktree: `/Users/gillettes/Coding Projects/mission-control`
- source chat: 2026-07-11 Cursor subagent, Mission Control unfinished work implementation
- last refreshed by chat: 2026-07-13 full-repo convergence closeout
- purpose: Implement ER-103 Git-state lifecycle metadata plus five-morning Morning Brief proof logging and morning-surface collision analysis.
- linked issue: self-contained; Linear repo-only mode in `LINEAR.md`
- plugin mirror: none; no live Mission Control Linear team/prefix verified
- merge expectation: none; branch commits and intended changes are patch-contained by `origin/main`
- merge target: `main`
- review surface: commit on this branch plus dashboard/scanner/test diff
- exit checklist:
  - [x] Morning proof log and harvester added
  - [x] Notification collision brief added without plist changes
  - [x] Git lifecycle scanner/dashboard fixture/rendering implemented
  - [x] Packet verification commands passed
  - [x] Commits are patch-contained by merged PR #9
  - [x] No unique uncontained implementation remains in the four dirty files; main carries later hardened variants
  - [x] Explicit no-merge disposition recorded
  - [ ] Trevor-owned dirty checkout cleanup is intentionally not performed by this audit
- delete when: only after the dirty user-owned checkout receives a separate preservation/discard decision
- retain reason: destructive cleanup would overwrite or delete uncommitted user state even though its intended behavior is superseded on main
- cleanup command: `git branch -d codex/er103-git-state-and-morning-proof && git push origin --delete codex/er103-git-state-and-morning-proof`
- linked PR/audit/completion record: Work Record Log `2026-07-11 — ER-103 Git-state lane and Morning Brief proof logging`

Keep one entry per non-trivial active branch so any chat can see why it exists, which chat opened or resumed it, what work is active, what must happen before merge or closeout, and whether the branch should be deleted or intentionally retained.
Legacy branches opened before this workflow may still need manual backfill; use `TODO: verify` instead of guessing until those entries are added.
Each active branch entry should include:
- `branch`
- `status`
- `created`
- `base`
- `source chat`
- `last refreshed by chat`
- `purpose`
- `linked issue`
- `plugin mirror`
- `merge expectation`
- `merge target`
- `review surface`
- `exit checklist`
- `delete when` or `retain after close`
- `retain reason` when not deleting
- `cleanup command`
- `linked PR/audit/completion record`

## Branch History

- 2026-07-13 | `codex/full-repo-audit-20260712` | reviewed head `60c9ece` merged through PR #9 as `c44e5c9`; repeated full verifier `21/21`, dashboard `66/0`, ER-134 `50/0`, browser `253`, independent `REVIEW-CLEAN`, CodeRabbit approval, GitGuardian pass, exact install/live proof | cleanup disposition: audit/detached baseline and redundant ER-134 worktrees removed; audit plus all patch-contained ER-134/panel/CTA local and remote branches deleted; Trevor-owned dirty ER-103 checkout preserved with explicit superseded disposition | record: `records/2026-07-12-full-repo-convergence-audit.md`.
- 2026-07-11 | direct `main` full-ingest-health repair | implementation `245fb31`, independent-audit skew repair `dcf11c2`, and hermetic test follow-up `9843607` are on `origin/main`; no feature branch/worktree was created because the bounded logic repair matched the current checkout purpose and had mechanical rollback/tests | cleanup disposition: none required | source: Codex `019f51c1-5817-7872-a6ce-8b65428277ed`; record: Work Record `2026-07-11 — Chat-graph full-ingest health truth repair`.
- 2026-07-11 | `codex/chat-graph-fresh-git-feed` | implementation `e2f5d60`, backup-health registration `4de0aef`, and live closeout `9191ebe` are contained by `origin/main` | cleanup disposition: local/remote branch and `/Users/gillettes/Coding Projects/mission-control-worktrees/chat-graph-fresh-git-feed` deleted after exact origin containment and canonical install/live proof | source: Codex `019f51c1-5817-7872-a6ce-8b65428277ed`; records: Chat truth-layer and Nightly repository-bundle Work Records above.
- 2026-07-11 | `codex/morning-brief-final-gate-20260711` | code through `00da629`; immutable evidence SHA `1261c48` passed separate final code/runtime and records/provenance audits, then fast-forwarded to `origin/main` | cleanup disposition: worktree plus local/remote branch deleted after canonical containment | source: Codex `019f4f6c-e506-7551-876e-d9bb31f56c36`; record: Work Record `2026-07-11 — faad4dbe orchestration audit final-gate repair`.
- 2026-07-11 | `feature/morning-brief-activation` | frozen history through `30f2f9f`, including direct-sender ancestor `b8e9cbe`, was reconciled into reviewed final evidence SHA `1261c48` | cleanup disposition: local/remote branch deleted after `origin/main` containment | source: historical ER-109 activation/governor lanes; reconciled by Codex `019f4f6c-e506-7551-876e-d9bb31f56c36`.
- 2026-07-11 | `codex/morning-brief-direct-deadman-20260711` | source commit `154ab3a`; patch-equivalent replay `b8e9cbe` is contained by `origin/main`; pre-rebase final-gate replay `f00c476` was correctly dropped when Git detected the ancestor-equivalent patch | cleanup disposition: worktree plus local/remote branch deleted after canonical containment | source: Codex `019f4f6c-e506-7551-876e-d9bb31f56c36` plus isolated implementation/review lane.
- 2026-07-11 | duplicate activation governors | Codex tasks `019f4d75-69e9-7211-9e76-2942acb0d0e8` and `019f5074-d4d8-7dc0-b91d-7d7419ae5706` concurrently advanced `feature/morning-brief-activation` through `30f2f9f` | containment disposition: both were interrupted and handed off to isolated worktrees; their combined-review claims do not certify the final candidate | record: Work Record `2026-07-11 — faad4dbe orchestration audit final-gate repair`.
- 2026-07-11 | `feature/morning-brief-p9-job-history` | commit `796cc26` is an ancestor of the reviewed final tree and `origin/main` | cleanup disposition: local/remote branch deleted after canonical containment | source: Codex `019f4f3d-3034-7283-b63c-e4e8890b51f1`; reconciled by Codex `019f5056-bc55-7043-88ad-b87b1ac48a6f` under Claude governor `e85411a1-b74b-4556-9939-8eaf5d8f2ea9`.
- 2026-07-11 | `codex/morning-brief-merge-gate-hardening` | docs-only commit `c0a7760` was deliberately not cherry-picked because its conclusions were superseded/carried by later reviewed records | cleanup disposition: local/remote branch and worktree deleted after canonical containment | source: merge-gate hardening audit lane; reconciled by Codex `019f5056-bc55-7043-88ad-b87b1ac48a6f` under Claude governor `e85411a1-b74b-4556-9939-8eaf5d8f2ea9`.
- 2026-07-09 | `codex/morning-brief` | base `ebfbd50` | merged by fast-forward to `main`; implementation and closeout landed through `cf6b536`, pushed, independently review-clean, then local/remote branch and `/Users/gillettes/Coding Projects/mission-control-worktrees/morning-brief` removed | source: Codex `019f4963-1e75-7600-8a17-1e6f6f8e8ca6` | record: `records/2026-07-09-morning-brief-orchestration-convergence.md`.

## Audit Record Convention
If it's not here, it isn't remembered.
- Record each audit, ship-check, or substantial verification-driven review in an easy-to-find project audit log entry.
- Each entry should capture:
  - `date`
  - `type` (for example `full audit`, `targeted audit`, `ship-check`, `governance review`)
  - `scope`
  - `repo fingerprint` (branch + commit when available)
  - `prior audit reference`
  - `source/work chat`
  - `audit chat`
  - `implementation chat` or `disposition chat`
  - `separate follow-up audit` (`yes` / `no` plus reason when `no`)
  - `commands / evidence`
  - `tested`
  - `not tested`
  - `findings opened or updated`
  - `fixes closed / verified`
  - `declined / deferred findings`
  - `better-path challenge`
  - `references` (issue, PR, commit, or log path)
  - `by`
  - `linear`
- When a finding is later implemented, deferred, declined, or superseded, update the existing audit trail instead of deleting the history.

## Audit Record Log
If it's not here, it isn't remembered.
- 2026-07-13 | scope: stop-boundary provenance/completion supersession and transcript reconciliation | repo fingerprint: clean `main`/`origin/main` at `3b787e3` before record-only checkpoint | prior audit reference: 08:53:58Z final in source chat `019f59f8-bb9e-70c0-9497-a9686ea24154` | source/work chat: `Audit repo feedback and fixes`, Codex `019f59f8-bb9e-70c0-9497-a9686ea24154`, cwd `/Users/gillettes/Coding Projects/mission-control`, transcript `/Users/gillettes/.codex/sessions/2026/07/12/rollout-2026-07-12T22-35-01-019f59f8-bb9e-70c0-9497-a9686ea24154.jsonl` | audit chat: `Audit: Mission Control repo feedback and fixes`, Codex `019f5bea-c97f-7263-bd2d-ef07aa68205b`, same cwd, transcript `/Users/gillettes/.codex/sessions/2026/07/13/rollout-2026-07-13T07-39-01-019f5bea-c97f-7263-bd2d-ef07aa68205b.jsonl` | tested: exact transcript provenance, four in-scope prior sessions, global helper/runtime containment, Mission verifier first 16 suites | not tested: browser and four later verifier suites after Trevor-directed interruption; fifth morning/operator/provider gates | findings: wrong model provenance and premature total-completion/no-defect claims | disposition: accepted and explicitly superseded in the canonical audit; goal active | references: `records/2026-07-12-full-repo-convergence-audit.md` | by: Codex | linear: repo-only.
- 2026-07-13 | morning schedule and nightly producer end-to-end repair | scope: 05:00/05:20 operator schedule, 04:40/04:47/04:54 inactive extractor retries, 28-hour ingest SLA, installed chat-graph provenance, 23:30 global producer ordering, live marker/export/Chats feed, and resource-defer truth | repo fingerprint: Mission `c66d7fb`; global `6f21f4f` | source/work chat: Codex `019f59f8-bb9e-70c0-9497-a9686ea24154` | audit chat: `/root/worker_019f59f8_final_challenger`; final verdict `SCHEDULE-REVIEW-CLEAN` | findings: stale schedule surfaces, post-brief extractor, 29.5-hour false-fresh band, governor-before-producer P1, stale deployed feed P2, and schema-7 fallback; all accepted and repaired | tested: focused suites, full verifiers, exact install stamps/hashes, pending inactive plist, real launchctl kickstart, graph export, normal Chats refresh, and independent failure-order counterexamples | not tested: fifth natural morning, comprehension, provider calibration, resource-admitted model report, or next natural 23:30 recurrence | fixes closed/verified: code and live local producer/feed path; no remaining P0–P3 schedule/runtime finding | references: convergence audit; global `records/verification/2026-07-13-nightly-chat-refresh-before-governor.md` | by: Codex + independent challenger | linear: repo-only.
- 2026-07-13 | usage collector/routing final code, integration, and runtime closure | scope: Mission/global/runtime byte identity, all accepted reviewer findings, exact origin landing, native artifact provenance, loaded LaunchAgent, first live RunAtLoad plus natural recurrence, generated state, process/lock residue, and deployed-router output | repo fingerprint: Mission `origin/main@c8a1e751c6c5cecfe199b68a561a4627850f21c0`; global `origin/main@8b1ee9147dd165d81054665b9e84f58202002ffa`; detached runtime at exact global SHA | prior audit reference: recurring audit entry and `records/2026-07-12-full-repo-convergence-audit.md` addendum | source/work chat: Codex `019f59f8-bb9e-70c0-9497-a9686ea24154` | audit chat: `/root/worker_019f59f8_final_challenger` | implementation chat: source/work chat | separate follow-up audit: yes; eight immutable repair generations were rejected before terminal `CODE-REVIEW-CLEAN`, `INTEGRATION-REVIEW-CLEAN`, and `RUNTIME-REVIEW-CLEAN` | commands/evidence: focused/full verifiers, adversarial parser/router/lock/schema/process replays, exact hashes, origin ancestry, npm artifact inspection, plist/launchctl/state/process inspection, deployed-router execution | tested: Mission usage `24/0`, full `21/21`, dashboard `67/0`, ER-134 `50/0`, browser `253`; global usage `64/0` and complete verifier; collector hash parity; native binary identity; loaded direct-argv contract; RunAtLoad plus two natural recurrences (`runs=3`, exit `0`); coherent history/dashboard/stdout advance; zero live probes and residue | not tested: fifth Morning Brief, Trevor comprehension, Outcome Extractor calibration, resource-admitted Nightly Review, or repaired Delegation Audit/session lookup | findings opened or updated: every in-scope reviewer finding closed; dirty/38-behind `.codex` policy-count drift recorded as an explicit non-landing external blocker | fixes closed/verified: all code/integration/installed-runtime and usage-recurrence findings | declined/deferred findings: separately owned external gates only | better-path challenge: converge one canonical collector/runtime and native artifact instead of adding a second source or weakening fail-closed checks | references: `/tmp/mission-control-c8a1e75-verify.log`; `/tmp/global-8b1ee91-verify.log`; convergence addendum | by: Codex + independent challenger | linear: repo-only.
- 2026-07-13 | recurring full health and operations audit | scope: complete current repository, Brooks code/architecture/debt/test dimensions, installed/live feed truth, natural Morning Brief proof, Automation upstream state, provider-native session-feedback reconciliation, canonical global usage source, and loaded scheduled runtime | repo fingerprint: clean base `bcee914`; rejected Mission candidates `105889e`, `eafa38b`, `2f56bbe`, `9d28370`, and `4571328`, `58e820f`, and `70f27bc`; rejected global candidates `a2d7091`, `f900d3b`, `e1d11df`, and `007e908`, `ab9d4c4`, and `f159a1c`; next immutable replacement commits pending | prior audit reference: `records/2026-07-12-full-repo-convergence-audit.md` fresh convergence addendum | source/work chat: Codex `019f59f8-bb9e-70c0-9497-a9686ea24154` | audit chat: `/root/worker_019f59f8_final_challenger` | implementation chat: source/work chat | separate follow-up audit: yes; repeated immutable review rounds found material counterexamples, including scheduled egress, newest-event reuse, configured-path false health, future timestamps, split lock recovery, stranded recovery residue, and future-dated legacy residue, truncated/unreadable JSONL, router multi-window/schema/reset/credit defects, and prohibited Codex/Claude model selection; all accepted findings have source repairs and regressions, with next immutable review pending | commands/evidence: live Git/runtime/feed/launchd/proof-log checks; provider-native session fallback; npm metadata/tarball/native CLI checks; repeated red/green usage regressions; exact cross-repo source comparison; replacement full verifiers | tested: Mission focused usage `24/24`, plus complete `21/21`, dashboard `67/0`, ER-134 `50/0`, browser `253`; global usage `63/0`; exact pinned/native argv, bounded child cleanup, newest-slot and future-mtime failure, configured-provider failure, kernel-lock crash recovery, future-dated legacy migration, 16-way contention, strict JSONL read/parse failure, conservative multi-window routing, collector-compatible credit validation, schema validation, provider-wide reset scoring, and GPT-5.5/Opus model-floor enforcement, zero-LLM scheduled path, direct launchd argv, system Bash, plist/syntax, and bytecode-clean verifier | not tested: two future natural mornings, Trevor comprehension, authorized Outcome Extractor calibration, a successful resource-admitted Nightly Review run, repaired Delegation Audit/session locator, next immutable review, or final installed-runtime hash before landing | findings opened or updated: every reviewer finding accepted; global `chat-source find` and Delegation Audit runtime defects remain cross-repo follow-ups | fixes closed/verified: focused source/test repairs; immutable re-review and runtime deployment remain open | declined/deferred findings: elapsed/authorization and separately owned upstream gates only | better-path challenge: replace the custom stale-lock state machine with kernel locking and converge the existing operational path rather than add another wrapper or hide upstream failure | references: recurring addendum in `records/2026-07-12-full-repo-convergence-audit.md`; `/tmp/mission-control-4571328-verify.log`; `/tmp/global-usage-convergence-v2-verify.log` | by: Codex | linear: repo-only.
- 2026-07-12 | full-repo convergence and independent counterexample audit | scope: every known Mission Control Codex/Claude session, local/origin branch tip, preserved ER-103 dirt, repo contracts, complete implementation, install/runtime definitions, UI/browser surface, and authoritative verification matrix | repo fingerprint: GitHub-review follow-up `86cfff8` on `codex/full-repo-audit-20260712` | prior audit reference: session provenance and superseded candidates in `records/2026-07-12-full-repo-convergence-audit.md` | source/work chat: Codex `019f59f8-bb9e-70c0-9497-a9686ea24154` | audit chat: `/root/worker_019f59f8_tests_ux`, GitHub Copilot, plus architecture/security and reconciliation workers | implementation chat: source/work chat | separate follow-up audit: yes; independent counterexamples found lock-marker bypass, blocked-sidecar partial state, linked transaction parents, and directory rename/swap windows; GitHub review then found unbounded argv JSON and hidden feed-lock filesystem errors; every accepted finding was fixed with a regression/recheck | commands/evidence: session-source resolution, branch/patch-containment proof, targeted red/green suites, repeated `/bin/bash scripts/verify.sh`, installed/demo Playwright, launchd/runtime inspection, exact immutable source review | tested: complete 21-suite verifier, dashboard `66/0`, ER-134 `50/0`, browser `253`, strict OpenSpec/static checks, privacy and recovery boundaries | not tested: two future natural mornings, Trevor comprehension, or live provider calibration | findings opened or updated: all reproduced defects in the convergence record | fixes closed/verified: both `b79d91d` and final GitHub-review follow-up `86cfff8` returned `REVIEW-CLEAN` after full green verification | declined/deferred findings: deprecated macOS notification API accepted with an explicit revisit trigger; elapsed/authorization gates remain open; directory fsync retained after repeated supported-runtime success | better-path challenge: repaired producer/transaction/attestation boundaries instead of weakening fail-closed checks or adding parallel truth stores | references: `records/2026-07-12-full-repo-convergence-audit.md`; `/tmp/mission-control-86cfff8-verify.log` | by: Codex | linear: repo-only.
- 2026-07-11 | independent full-ingest health audit | scope: `scripts/chat-graph` and `scripts/chat-graph.test.sh` immutable combined range `245fb317^..9843607`; catch-up/full-ingest boundary, shared nightly SLA, missing/stale/healthy/future-marker behavior, overrides, ambient isolation, and false-green/false-red regressions | repo fingerprint: `origin/main@9843607`; scoped diff SHA-256 `aa79aa307241948ce3265f39c866685615242d13925e6c62a0e7e28e743b658f` | prior audit reference: live 134-minute false red in Work Record above | source/work chat: Codex `019f51c1-5817-7872-a6ce-8b65428277ed` | audit chat: `/root/chat_source_hermes_review` | implementation chat: source/work chat | separate follow-up audit: yes; first pass found future-marker false green and second pass found ambient-SLA test drift, both fixed and re-reviewed | commands/evidence: full graph suite default and under ambient SLA 3600, common suite, compile, diff check, exact immutable source comparison | tested: 30-minute catch-up remains isolated; 3h/31h default boundaries; 1h/48h overrides; missing/future marker failure; invalid override fallback; ambient test isolation | not tested: live install or next natural 23:30 ingest | findings opened or updated: P1 future-marker false green and P3 hermeticity gap | fixes closed/verified: both have regressions; final verdict `REVIEW-CLEAN`, no P0-P3 findings | declined/deferred findings: natural elapsed proof only | better-path challenge: reuse one shared SLA instead of duplicating cadence policy in doctor/show | references: commits `245fb31`, `dcf11c2`, `9843607`; Work Record above | by: Codex reviewer `/root/chat_source_hermes_review` | linear: repo-only.
- 2026-07-11 | independent chat truth-layer timeout/cache audit | scope: Git-feed cache trust, state-home and override isolation, same-cycle failure behavior, process-group timeout cleanup, reconciliation safety, and pipeline coverage | repo fingerprint: implementation `e2f5d60`, live/install proof at `4de0aef`, closeout `9191ebe` on `origin/main` | prior audit reference: live timeout/orphan reproduction in Work Record above | source/work chat: Codex `019f51c1-5817-7872-a6ce-8b65428277ed` | audit chat: `/root/external_orchestration_research` | implementation chat: source/work chat | separate follow-up audit: yes; frozen five-path diff `7b75fe6953657a32fc2ead772a2d88fd4f11d532741c70a1bae61d928fea9148` returned `READY` / review-clean with no P0-P3 findings | commands/evidence: malformed envelope, owner-fencing, PID-reuse, process-group, escaped-descriptor, interruption, and post-reap invalid-byte counterexamples; full graph/dashboard suites; canonical install/stamp; strict live Automation+Git+Chats collect; LaunchAgent kickstart | tested: strict cache boundary, no false resolution on unavailable evidence, one scan per Git-to-Chats cycle, process-group cleanup, owner-aware lock recovery, malformed output, installed canonical path, live 58-row parity, and scheduler execution | not tested: future high-contention natural cycle | findings opened or updated: all reproduced findings accepted into code/tests | fixes closed/verified: review-clean, on `origin/main`, installed at exact implementation head, and live-proven without residue | declined/deferred findings: none | better-path challenge: fix the source-of-truth and cancellation semantics before widening autonomy | references: Work Record above; `e2f5d60`; `4de0aef`; `9191ebe` | by: Codex | linear: repo-only.
- 2026-07-11 | final immutable Morning Brief safety audit and landing | scope: exact evidence SHA `1261c48`, accepted repairs through `00da629`, full no-provider regression matrix, canonical containment, installed-surface provenance, launchd state, and branch/worktree cleanup | repo fingerprint: `origin/main` contains `1261c48`; exact closeout commit installed before push | prior audit reference: Work Record `2026-07-11 — faad4dbe orchestration audit final-gate repair` | source/work chat: Codex `019f4f6c-e506-7551-876e-d9bb31f56c36` auditing Claude `faad4dbe-5829-46de-8d3e-93d3392dc6c4` | audit chats: `/root/postfix_code_audit` and `/root/immutable_evidence_audit` | implementation chat: source/work chat | separate follow-up audit: yes; code/runtime `READY` and records/provenance `review-clean`, no P0-P3 findings | commands/evidence: dashboard `48/48`, runner `32/32`, common/usage `2/2`, sender `14/14`, every remaining suite and seven-tab adversarial render pass, strict OpenSpec/static gates, exact install-stamp and launchd verification | tested: deterministic safety boundary, immutable install, installed hashes/modes, canonical scheduler definitions, no-provider closeout | not tested: five natural mornings, authorized provider calibration, Outcome Extractor activation, or notification consolidation | findings opened or updated: elapsed/authorization gates only | fixes closed / verified: all accepted final-gate findings | declined / deferred findings: provider and elapsed gates explicitly deferred | better-path challenge: keep the broad autonomous executor design-gated instead of conflating it with the bounded Morning Brief safety closeout | references: `00da629`; `1261c48`; OpenSpec verify record | by: Codex | linear: repo-only.
- 2026-07-09 | independent Morning Brief Tier 2 audit | scope: Fable plan fit, bounded model extraction, privacy/config/cache/budget/lock boundaries, deterministic comprehension context, decision trust/lifecycle, lineage grouping, activation gates, and current operator-facing output | repo fingerprint: `main` implementation commit `df991b4` | prior audit reference: deterministic implementation audit below | source/work chat: Codex `019f4963-1e75-7600-8a17-1e6f6f8e8ca6` auditing Fable `35d96de4-9509-4382-b1a0-10b9a4d1777e` and linked plan-author `6f306a0b-abbb-4d39-9d64-afa7fb977250` | audit chat: `/root/outcome_audit` with `/root/outcome_audit/tier2_challenger` | implementation chat: source/work chat | separate follow-up audit: yes; repeated exact-tree counterexample passes continued until both returned review-clean | commands/evidence: full cold matrix, focused synthetic extractor/decision/brief regressions, strict OpenSpec, HOTL lint, static checks, `records/morning-brief-tier2-codex-audit.md` | tested: no-call ingest/export, strict provider/config parity, persistent disables, missing/malformed configs, schema/cache closure, ambiguity escalation, backlog/lock/timeout behavior, audit lineage, late-update context, inferred queue separation/rollback/re-enable/human dispositions, Automation semantic history, inactive installation | not tested: live provider egress/calibration, Telegram, LaunchAgent activation, or elapsed mornings | findings opened or updated: free-prose injection risk; generic taxonomy comprehension failure; bypassed kill/config caps; malformed result acceptance; ambiguity escalation; false NEEDS YOU; inferred lifecycle rollback/context issues; flat/unclear lineage and late updates | fixes closed / verified: every accepted finding has a regression and final current-tree verdict is `review-clean` with no P0–P3 findings | declined / deferred findings: external authorization and elapsed-proof gates only | better-path challenge: preserve Fable's Morning Brief product direction while replacing unsafe free-form model authority with deterministic context around bounded classification | references: `df991b4`; `records/morning-brief-tier2-codex-audit.md`; `openspec/changes/morning-brief/verify.md` | by: Codex | linear: repo-only.
- 2026-07-09 | independent Morning Brief implementation audit | scope: final deterministic/Tier 1 commits, OpenSpec/HOTL contracts, cold suites, installed/browser proof, live graph state, and 56-repository runner dry-run | repo fingerprint: `codex/morning-brief` at `2da8c9b` plus closeout records | prior audit reference: outcome parser and per-record runner audits in this Codex thread | source/work chat: Codex `019f4963-1e75-7600-8a17-1e6f6f8e8ca6` auditing Fable `35d96de4-9509-4382-b1a0-10b9a4d1777e` | audit chat: separate Codex agent `/root/decision_audit` | implementation chat: source/work chat | separate follow-up audit: yes; one P2 stale generated HOTL sidecar/report finding was accepted and corrected through the supported HOTL runtime | commands/evidence: all focused suites, strict OpenSpec, HOTL lint, installed code-only hash manifests, three browser captures, `/tmp/morning-brief-runner-live-*.json*`, and `records/morning-brief-independent-codex-audit.md` | tested: outcomes, decisions, delivery/deadman code, dashboard, structured Git facts, default-dry-run runner, privacy, installed rendering, and rollback artifacts | not tested: live Telegram, Tier 2, launchd activation, automatic push, natural cadence, or five mornings | findings opened or updated: P2 durable-state contradiction | fixes closed / verified: generated HOTL run marked blocked/superseded without invented execution evidence | declined / deferred findings: external and elapsed gates explicitly deferred | better-path challenge: merge the useful deterministic product slice without pretending the overall program is verified | references: `records/morning-brief-independent-codex-audit.md`; `openspec/changes/morning-brief/verify.md` | by: Codex | linear: repo-only.
- 2026-07-08 | independent Map journal polish audit | scope: Home session-monitor action labels, Map recent-chat journal action, Chats action-label smoke coverage, and fixture fallback coverage | repo fingerprint: `main` at `1175101` with local journal polish diff | prior audit reference: Claude Code chat `1ef98716-30bb-4530-80e0-6b2f3fa74f79` plus Mission Control `Map recent chat journal` work record | source/work chat: Codex `019f2b73-9811-7392-b511-201c1f109997` | audit chat: Codex worker `019f4309-f85e-72a3-9e3e-92e6281b2222` | implementation chat: Codex `019f2b73-9811-7392-b511-201c1f109997` | separate follow-up audit: yes; worker found a P3 smoke gap and rechecked the fix | commands/evidence: `bash scripts/chat-graph.test.sh`; `bash scripts/automation-status.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js`; `node scripts/dashboard-render-smoke.js .`; `bash scripts/usage-snapshot.test.sh`; `bash scripts/scan-unfinished-work --self-test`; `python3 -m json.tool dashboard/fixtures/chats.json`; `git diff --check`; browser capture `/tmp/mission-control-map-journal-polish-with-graph.png` | tested: Map journal five-row render, `Show connections` action, Home and Chats `Reopen this chat` / `Read transcript` labels, view-only fixture fallback, and Cytoscape graph render alongside the journal | not tested: queued Map search/timeline, Home triage scoring, title enrichment, usage routing, or autonomy | findings opened or updated: P3 smoke fallback gap fixed in-session | fixes closed/verified: render smoke now requires both plain action labels and a journal connection action; independent follow-up audit returned `review-clean` | declined/deferred findings: residual queued packets remain intentionally deferred | better-path challenge: keep this as a small clarity polish using existing feed data instead of adding a new journal data source | references: Work Record `2026-07-08 — Map journal action polish` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-08 | independent audit-loop severity fix | scope: P13 Open work severity computation in `scripts/chat-graph` and graph tests | repo fingerprint: `main` after wording fix `74a5af5` with local severity diff | prior audit reference: Codex audit worker `019f42ec-0968-7091-a331-9944b5f2ef92` finding on commit `838358a` | source/work chat: Codex `019f2b73-9811-7392-b511-201c1f109997` | audit chat: Codex worker `019f42ec-0968-7091-a331-9944b5f2ef92` | implementation chat: Codex `019f2b73-9811-7392-b511-201c1f109997` | separate follow-up audit: yes; worker found severity drift from the P13 contract | commands/evidence: `bash scripts/chat-graph.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js && node scripts/dashboard-render-smoke.js .`; `python3 -m py_compile scripts/chat-graph`; `git diff --check` | tested: register P0/security red, stale handoff grey, unpushed Git amber, detached Git red, dashboard render compatibility | not tested: live browser recapture after this logic-only follow-up | findings opened or updated: P2 severity-rule drift fixed in-session | fixes closed/verified: `_loose_severity()` now encodes documented cases and graph export regression covers them | declined/deferred findings: none | better-path challenge: compute severity from source facts rather than adding a manually managed priority field | references: Work Record `2026-07-08 — Audit-loop severity rules` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-08 | independent audit-loop follow-up | scope: Open work temporary-hide label and source-backed reappearance behavior after commit `838358a` | repo fingerprint: `main` at `838358a` plus focused follow-up diff | prior audit reference: Claude Code chat `1ef98716-30bb-4530-80e0-6b2f3fa74f79` and commit `838358a` | source/work chat: Codex `019f2b73-9811-7392-b511-201c1f109997` | audit chat: Codex worker `019f42ec-0968-7091-a331-9944b5f2ef92` plus backup worker `019f42f1-85bd-7603-8a29-cc3eced39054` | implementation chat: Codex `019f2b73-9811-7392-b511-201c1f109997` | separate follow-up audit: yes; worker surfaced the dismiss-label risk and the fix added a regression for source-backed reappearance | commands/evidence: `bash scripts/chat-graph.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js && node scripts/dashboard-render-smoke.js .` | tested: temporary-hide wording, `todo_open` manual resolve, `todo_open` reappearance when the source remains open, and Chats render smoke | not tested: live browser recapture after this copy-only follow-up | findings opened or updated: P3 wording risk fixed in-session | fixes closed/verified: `Dismiss this item` replaced by `Hide until refresh`; source-backed hide-once semantics covered by regression | declined/deferred findings: none | better-path challenge: use precise temporary wording instead of making the manual command sound like durable source cleanup | references: Work Record `2026-07-08 — Audit-loop wording polish` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-04 | targeted implementation audit | scope: Dashboard search-audit V1 UI bundle in `dashboard/index.html`, `dashboard/fixtures/usage.json`, and `scripts/dashboard-render-smoke.js` | repo fingerprint: `main` with local uncommitted dashboard bundle changes in the working tree | prior audit reference: `2026-07-04 | external source search audit` and record `records/2026-07-04-dashboard-coding-tracker-search-audit.md` | source/work chat: Codex `631e0a16-efe8-4377-aa81-0d02783eea59` | audit chat: same Codex thread with separate `code-review` subagent passes | implementation chat: same Codex thread | separate follow-up audit: yes; multiple separate review passes were run after implementation and after each fix cycle | commands/evidence: `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node scripts/dashboard-render-smoke.js .`; repeated `code-review` agent passes focused on the changed dashboard files; `git diff --stat -- dashboard/index.html scripts/dashboard-render-smoke.js dashboard/fixtures/usage.json` | tested: panel provenance lines, decision-card rendering, credit-window handling, map side-panel grouping/copy, session-monitor priority/join logic, heatmap alignment/DST bucketing, and fixture-backed render coverage | not tested: installed browser/file:// runtime capture in this packet's own working-tree state | findings opened or updated: all material audit findings were fixed in-session; no new queue item opened from the closeout audit | fixes closed/verified: false 0% usage readings, credit-card duplication, repo-join drift, focus/filter wording, heatmap weekday/DST issues, map grouping honesty, source-label mismatches, and session-rank ordering | declined/deferred findings: none | better-path challenge: keep the packet dependency-light and fix the misleading edge cases instead of shipping a broad visual rewrite | references: Work Record `2026-07-04 — Dashboard search-audit V1 UI bundle`; `records/2026-07-04-dashboard-coding-tracker-search-audit.md` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-04 | external source search audit | scope: GitHub search URL `dashboard coding tracker in:name,description,readme stars:>10`, Mission Control improvement patterns, source-only backend/frontend/workflow/UI review | repo fingerprint: `main` at pre-audit clean state | prior audit reference: `records/2026-07-02-mission-control-research.md` | source/work chat: Codex `019f2dec-d36b-7093-8259-8d9df30dede0` | audit chat: same | implementation chat: not applicable; source-only audit | separate follow-up audit: no; no runtime/code adoption performed | commands/evidence: GitHub API metadata sweep of top 1000 rows from about 18.5k results (`18474` latest run; `18475` earlier same-day run), README/license/metadata deep-read for high-fit candidates, README screenshot/asset-reference reviews for six visual candidates, comparison to Mission Control project intent and active queue | tested: source relevance, architecture fit, visual pattern fit, integration risk classification | not tested: cloned third-party runtime behavior, credentialed provider APIs, paid services, or license-level code reuse | findings opened or updated: Suggested Recommendation Log entry for ER-089/UI polish pattern mining | fixes closed/verified: durable search audit record written | declined/deferred findings: direct adoption/install of third-party tools deferred until opt-in adapter review | better-path challenge: preserve local/offline Mission Control and borrow narrow feed/UI patterns instead of importing a generic dashboard platform | references: `records/2026-07-04-dashboard-coding-tracker-search-audit.md` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-04 | cross-chat implementation closeout | scope: Cursor chat `207d88dd-4abf-4328-a66a-aa55a8801d21` dirty audit-fix patch, regression coverage, durable records, installed runtime, and browser proof | repo fingerprint: `main` at `31ee124` plus eight task-edited files before closeout commit | prior audit reference: `2026-07-04 | cross-chat implementation audit` / commit `31ee124` | source/work chat: Cursor `207d88dd-4abf-4328-a66a-aa55a8801d21` (`Scan this repo for bugs...`) | audit/implementation chat: Codex `019f2ddb-4d45-79b2-9ed7-da8303fb3439` | separate follow-up audit: no; closeout included self-audit and installed-browser proof | commands/evidence: `bash scripts/chat-graph.test.sh`; `bash scripts/usage-snapshot.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js && node scripts/dashboard-render-smoke.js .`; `bash scripts/automation-status.test.sh`; `bash scripts/scan-unfinished-work --self-test`; `bash -n ...`; `python3 -m py_compile scripts/chat-graph`; `git diff --check`; token-pattern diff scan; installed collect; browser capture | tested: stale ingest lock recovery, `ingest_skipped` export/status/Home surfacing, dashboard env override shell rejection, usage notification argv/rejection behavior, normalized usage status strip, yellow automation surfacing, scoped Map edge rendering, scan TSV parsing, installed file:// Home render | not tested: launchd over future intervals, provider-side live usage APIs beyond current local snapshot | findings opened or updated: closed Active Next Steps `207d88dd Cursor patch closeout` | fixes closed/verified: dirty/unpushed patch landed with records; missing regression coverage closed; command-execution hardening verified; installed runtime rendered | declined/deferred findings: provider usage adapters and autonomous hygiene loop stay deferred as ER-089/ER-090 product-scope work | better-path challenge: do not accept cross-chat implementation summaries as complete without commit/push, durable records, regression tests, and installed/runtime evidence when UI changes are release-sensitive | references: screenshot `/tmp/mission-control-207d88dd-closeout-home.png`; transcript `/Users/gillettes/.cursor/projects/Users-gillettes-Coding-Projects-mission-control/agent-transcripts/207d88dd-4abf-4328-a66a-aa55a8801d21/207d88dd-4abf-4328-a66a-aa55a8801d21.jsonl`; closeout commit 3a612e5 | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-04 | cross-chat implementation audit | scope: Cursor chat `207d88dd-4abf-4328-a66a-aa55a8801d21` audit findings, implementation claim, dirty diff, and verification claims | repo fingerprint: `main` at `ebb07e2` with six dirty Cursor-touched files | prior audit reference: none for this exact chat | source/work chat: Cursor `207d88dd-4abf-4328-a66a-aa55a8801d21` (`Scan this repo for bugs...`) | audit chat: Codex `019f2ddb-4d45-79b2-9ed7-da8303fb3439` | implementation chat: same Cursor chat; no commit made | separate follow-up audit: yes; this entry records the follow-up audit | commands/evidence: `chat-source describe/full/latest`, raw Cursor JSONL, `git status -sb`, `git diff --stat`, all local release-gate suites, focused stale-lock/export/override probes, `git diff --check`, syntax checks, secret-pattern diff scan | tested: current dirty patch test suites and targeted probes for stale ingest lock, `ingest_skipped`, argv override rejection, SQLite index inspection, scan self-test, render smoke | not tested: installed `~/.mission-control` runtime/browser capture for the dirty patch, live launchd cadence, provider usage live sources | findings opened or updated: Active Next Steps `207d88dd Cursor patch closeout`; P1 dirty uncommitted/unpushed implementation; P2 missing durable Work/Test record; P2 weak regression coverage for several fixed behaviors | fixes closed/verified: current code patch passes local suites and focused probes; no secrets matched in dirty diff | declined/deferred findings: no code fixes performed in this audit-only pass | better-path challenge: do not accept the Cursor chat as complete until the patch is either committed with durable records and targeted tests or intentionally discarded | references: transcript `/Users/gillettes/.cursor/projects/Users-gillettes-Coding-Projects-mission-control/agent-transcripts/207d88dd-4abf-4328-a66a-aa55a8801d21/207d88dd-4abf-4328-a66a-aa55a8801d21.jsonl`; files `dashboard/index.html`, `scripts/chat-graph`, `scripts/dashboard`, `scripts/dashboard.test.sh`, `scripts/scan-unfinished-work`, `scripts/usage-snapshot` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-04 | targeted audit follow-up | scope: Mission Control Phase 2/4 clarity + ER-087 audit gaps | repo fingerprint: `main`, pre-commit follow-up state | prior audit reference: Claude Code chat `1ef98716-30bb-4530-80e0-6b2f3fa74f79` (`*Chat connection tracking tool`) | source/work chat: same | audit/implementation chat: Codex `019f2b73-9811-7392-b511-201c1f109997` | separate follow-up audit: no; this pass included test and browser verification | commands/evidence: see Test Evidence Log | tested: source label scan, all local suites, governance validators, Home/Map/Chats browser captures | not tested: live provider usage adapters | findings opened or updated: ER-089 usage-routing queue, ER-090 autonomous hygiene queue | fixes closed/verified: governance scaffold, PROJECT_INTENT, plain labels, stale full-ingest status, Cytoscape smoke | declined/deferred findings: usage-routing/autonomy implementation deferred until V1 clarity complete | better-path challenge: stopped feature growth until owner-facing clarity and trust signals passed | references: `PROJECT_INTENT.md`, `dashboard/index.html`, `scripts/dashboard-render-smoke.js`, `scripts/chat-graph`, `scripts/dashboard` | by: Codex | linear: self-contained until Linear is configured.

## Test Evidence Convention
If it's not here, it isn't remembered.
- Testing is required delivery evidence. If a check is skipped, blocked, or only partially run, record the reason and the remaining risk.
- Record each verification run as:
  - `date` (YYYY-MM-DD)
  - `command(s)` executed
  - `result` (pass/fail + short note)
  - `log/PR reference` (commit SHA, CI URL, or local log path)
  - `by`
  - `linear`
- When a verification run closes or updates an audit finding, cross-reference the matching audit record entry and the chat or commit that performed the work.

## Test Evidence Log
- 2026-07-13 | commands: installed `/Users/gillettes/.codex/scripts/chat-source find mission control`; exact `chat-source describe/latest` for resolved sessions and audit chat; direct and mandatory pre-push `/bin/bash scripts/verify.sh` runs; process inspection at Trevor stop boundary; LaunchAgent/plist/runtime/worktree checks | result: session lookup returned five exact sources in 85.19 seconds; four were reconciled and one unrelated third-party product session excluded. Both verifier runs completed the first 16/21 top-level suites, including dashboard `67/0`, ER-134 `50/0`, usage `24/0`, and two sender `14/14` runs; each browser run slept with Chrome gone, so browser plus four subsequent suites are interrupted/incomplete, not pass/fail. The records-only push used the global hook's documented `--no-verify` escape hatch. Delegation Audit runtime is clean at `27aa2e3`, focused `66/0`, dry-run exit 0, plist loaded/linted, natural runs still 0. | log/PR reference: stop-boundary addendum in `records/2026-07-12-full-repo-convergence-audit.md` | by: Codex + audit chat `019f5bea` | linear: repo-only.
- 2026-07-13 | commands: schedule/SLA RED-GREEN focused suites; Mission common, graph, brief, deadman, dashboard, render, full verifier; global nightly selftest/Bash/ShellCheck/full verifier; exact installs/stamps/hashes; real Nightly Review kickstart; graph export and installed dashboard Chats refresh | result: PASS — Mission full `21/21`, dashboard `67/0`, usage `24/0`, browser `253`; global full verifier pass; marker advanced before expected governor defer; graph/Chats SLA `100800` fresh; installed Mission `c66d7fb`, global runtime `6f21f4f`; Outcome Extractor inactive | logs: `/tmp/mission-control-c66d7fb-verify.log`, `/tmp/global-6f21f4f-verify.log`; convergence audit | by: Codex + `/root/worker_019f59f8_final_challenger` | linear: repo-only.
- 2026-07-13 | commands: exact Mission/global focused and full verifiers; repeated independent counterexample/ShellCheck/plist/hash review; origin landing; npm Darwin ARM64 tarball/file/codesign/otool/version inspection; detached-runtime/hash/status checks; loaded plist/launchctl inspection; RunAtLoad and natural-recurrence state/history/dashboard/stdout/stderr/process/lock/temp checks; deployed router against latest snapshot | result: PASS — Mission `c8a1e75` usage `24/0`, full `21/21`, browser `253`; global `8b1ee91` usage `64/0` and complete verifier; three terminal clean challenger verdicts; loaded usage job reached `runs=3`, exit `0`; exact collector/native hashes; history/stdout advanced at 06:35 while historical stderr remained unchanged; no scoped process/lock/temp residue | log/PR reference: `/tmp/mission-control-c8a1e75-verify.log`; `/tmp/global-8b1ee91-verify.log`; `records/2026-07-12-full-repo-convergence-audit.md` | by: Codex `019f59f8-bb9e-70c0-9497-a9686ea24154` + `/root/worker_019f59f8_final_challenger` | linear: repo-only.
- 2026-07-13 | commands: red/green `/bin/bash scripts/usage-snapshot.test.sh`; `npm view ccusage version dist.integrity dist.tarball`; `npm pack --ignore-scripts ccusage@20.0.17`; native `ccusage 20.0.17` version/help plus real pinned/offline `blocks` and `weekly` JSON shape checks; `/bin/bash scripts/verify.sh`; shell syntax; `git diff --check` | result: PASS after expected pre-fix usage failure; usage `3/0`, authoritative suites `21/21`, dashboard `67/0`, ER-134 `50/0`, browser `253`, OpenSpec and syntax clean | log/PR reference: `/tmp/mission-control-20260713-recurring-audit-verify.log`; recurring addendum in `records/2026-07-12-full-repo-convergence-audit.md` | by: Codex `019f59f8-bb9e-70c0-9497-a9686ea24154` | linear: repo-only.
- 2026-07-12 | commands: repeated `/bin/bash scripts/verify.sh`; focused `bash scripts/dashboard.test.sh`; `bash scripts/er134-usability.test.sh`; scanner self-test; Python/Bash syntax and `git diff --check`; independent exact-source and GitHub 32-file reviews | result: PASS on immutable `b79d91d` and GitHub follow-up `86cfff8`; authoritative suites `21/21`, dashboard `66/0`, ER-134 `50/0`, browser `253`; `b79d91d` independent verdict `REVIEW-CLEAN`; two actionable GitHub comments fixed and fully reverified | log/PR reference: `/tmp/mission-control-b79d91d-verify.log`; `/tmp/mission-control-86cfff8-verify.log`; PR `#9`; `records/2026-07-12-full-repo-convergence-audit.md` | by: Codex `019f59f8-bb9e-70c0-9497-a9686ea24154` + `/root/worker_019f59f8_tests_ux` + GitHub Copilot | linear: repo-only.
- 2026-07-11 | commands: `bash scripts/scan-unfinished-work --self-test`; `scripts/harvest-morning-brief-proof --self-test`; `python3 -m json.tool dashboard/fixtures/git.json`; `node --check scripts/dashboard-render-smoke.js && node scripts/dashboard-render-smoke.js .`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `bash -n scripts/scan-unfinished-work && bash -n scripts/dashboard`; `python3 -m py_compile scripts/harvest-morning-brief-proof`; `git diff --check` | result: pass; scanner self-test proves explained/unexplained branch metadata, linked worktree metadata, and decision rows; proof harvester self-test proves idempotent update by `brief_id`; dashboard suite PASS=55 FAIL=0; render smoke all 7 tabs plus lifecycle label assertions passed; fixture JSON, syntax, py_compile, and whitespace checks passed | log/PR reference: Work Record Log `2026-07-11 — ER-103 Git-state lane and Morning Brief proof logging` | by: Cursor subagent | linear: self-contained until Linear is configured.
- 2026-07-11 | commands: live `dashboard status` and `chat-graph doctor`; marker/database/feed and scheduler inspection; initial RED `bash scripts/chat-graph.test.sh`; GREEN compile/full graph/diff gates; immutable review of `245fb31`; +1-hour skew RED replay; final GREEN with ambient SLA unset plus explicit 1-hour/48-hour overrides; cold reviewer rerun at `9843607` under default and ambient SLA 3600; `mission-control-common.test.sh`; exact commit/push containment; `./scripts/dashboard install`; schema-1 stamp inspection; live doctor/Chats/status; Mission Control LaunchAgent kickstart | result: live diagnosis separated a healthy 134-minute-old nightly full-ingest marker from fresh incremental DB/feed activity; initial RED produced exactly three expected boundary failures; independent review found a future-marker false green and its regression produced exactly two failures, then found an ambient-override test-hermeticity gap; final verdict `REVIEW-CLEAN` with no P0-P3 findings; full graph suites `ALL PASS`, common `2/2`; exact head `5372dfa` installed and verified; live doctor green at 146m/1800m, all feeds fresh, scheduler runs 86→87 and exit 0; dashboard exit 1 is the unrelated Automation red job | log/PR reference: Work/Audit Records `2026-07-11 — Chat-graph full-ingest health truth repair`; commits `245fb31`, `dcf11c2`, `9843607`, `5372dfa`; diff SHA `aa79aa307241948ce3265f39c866685615242d13925e6c62a0e7e28e743b658f` | by: Codex `019f51c1-5817-7872-a6ce-8b65428277ed` + independent reviewer `/root/chat_source_hermes_review` | linear: repo-only.
- 2026-07-11 | commands: pre-fix live status/process/feed/orphan-lock inspection; RED/GREEN cache, fallback, owner, process-group, interruption, live-old-lock, PID-reuse, and post-reap decode probes; `bash scripts/chat-graph.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `bash scripts/automation-status.test.sh`; static/residue checks; frozen independent re-review; push to `origin/main`; canonical install and stamp verification; strict live Automation+Git+Chats collection; LaunchAgent kickstart | result: graph `ALL PASS`, dashboard `PASS=55 FAIL=0`, Automation `ALL PASS`, review-clean; exact install `4de0aef`; fresh `ok:true` feeds with 58 Git rows = 58 chat annotations, backup green, no unregistered jobs, no process/lock residue, Mission Control runs 70→71 with exit 0 | log/PR reference: Work Records `2026-07-11 — Chat truth-layer timeout and cache trust repair` and `Nightly repository-bundle semantic health registration`; commits `e2f5d60`, `4de0aef` | by: Codex `019f51c1-5817-7872-a6ce-8b65428277ed` + independent reviewer `/root/external_orchestration_research` | linear: repo-only.
If it's not here, it isn't remembered.
- 2026-07-11 | commands: final immutable code/runtime and records/provenance audits of `1261c48`; fast-forward to canonical `main`; remove superseded Morning Brief branches/worktrees; commit the closeout record; install from that exact committed tree with gated jobs/provider paths inactive; verify schema-1 stamp exact five-runtime/two-asset set, source hashes, executable/private modes, canonical launchd definitions, last exits, and Outcome Extractor not loaded | result: PASS; final audits found no P0-P3 defects, exact reviewed tree is on `origin/main`, cleanup is complete, and the exact closeout commit is installed before push without provider delivery | log/PR reference: `00da629`; `1261c48`; Work Record `2026-07-11 — faad4dbe orchestration audit final-gate repair` | by: Codex `019f4f6c-e506-7551-876e-d9bb31f56c36` + `/root/postfix_code_audit`, `/root/immutable_evidence_audit` | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-11 | commands: second immutable audit of `8fd3ba5`; counterexample with a >6x stale all-green Automation payload; actual `dashboard demo` fixture inspection; plist chmod-retry assertion audit; post-fix full no-provider cold/static matrix | result: records/provenance `review-clean`; code `NOT READY` with P1 Home card/segment false green, P2 demo cadence masking, and P3 missing bootstrap proof. Code commit `00da629` closes all three. Post-fix matrix ALL PASS: dashboard `PASS=48 FAIL=0`, runner `PASS=32 FAIL=0`, common/usage `PASS=2 FAIL=0`, sender `14/14`, every other suite pass, seven tabs and Home named surfaces share the cross-runtime age table, demo fixture is contract-valid, strict OpenSpec/static gates pass | log/PR reference: `00da629`; Work Record `2026-07-11 — faad4dbe orchestration audit final-gate repair` | by: Codex `019f4f6c-e506-7551-876e-d9bb31f56c36` + `/root/postfix_code_audit`, `/root/immutable_evidence_audit` | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-11 | commands: immutable audit of pushed candidate `5f2dd6a`; deterministic counterexamples for Python/browser age-state parity and unchanged-plist mode drift; `/bin/bash` versus Homebrew nested-shell selection; isolated-HOME common policy; post-finding full no-provider cold/static matrix | result: first audit `NOT READY` with two P2 code-integrity and two test-evidence findings plus record drift; code commit `aba151b` closes all four. Post-fix matrix ALL PASS: dashboard `PASS=48 FAIL=0`, runner `PASS=32 FAIL=0`, common/usage `PASS=2 FAIL=0`, sender `14/14`, all other suites pass, seven tabs plus the cross-runtime 1x/2x/6x table render, strict OpenSpec/static gates pass. Audit-side launchd registration disruption was immediately repaired to canonical path with last exit 0 and is disclosed in the Work Record | log/PR reference: `aba151b`; Work Record `2026-07-11 — faad4dbe orchestration audit final-gate repair` | by: Codex `019f4f6c-e506-7551-876e-d9bb31f56c36` + `/root/immutable_code_audit`, `/root/immutable_runtime_audit`, `/root/immutable_evidence_audit` | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-11 | commands: post-rebase final-gate cold matrix using true `/bin/bash` for every shell suite — automation status, chat graph, dashboard, decision alerts, loose-end runner, common, direct deadman sender, deadman, delivery, Morning Brief, outcome coverage/extractor, usage, all seven render tabs plus adversarial cases, scanner self-test, strict OpenSpec; followed by Bash/Node syntax, Python compilation, JSON/plist validation, and `git diff --check` | result: ALL PASS at reconciled code commit `3427d6b` and evidence tree `8a3930a`; dashboard `PASS=48 FAIL=0`, runner `PASS=32 FAIL=0`, common and usage each `PASS=2 FAIL=0`, sender `14/14`, all seven tabs render, OpenSpec valid; all planned post-rebase provider paths were loopback/stubbed/offline and the separately disclosed 02:23:09 audit probe is not part of this matrix | log/PR reference: Work Record `2026-07-11 — faad4dbe orchestration audit final-gate repair` | by: Codex thread `019f4f6c-e506-7551-876e-d9bb31f56c36` | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-11 | commands: combined activation/direct-deadman matrix — `chat-graph.test.sh`; `mission-control-common.test.sh`; `morning-brief.test.sh`; `morning-brief-delivery.test.sh`; `morning-brief-deadman.test.sh`; `dashboard.test.sh --require-shell`; `dashboard-render-smoke.js`; `openspec validate --all --json`; Python/Node/Bash syntax and `git diff --check` | result: ALL PASS; direct-sender security/transport suite `14/14`, dashboard `PASS=37 FAIL=0`, common `PASS=2 FAIL=0`, all 7 render tabs, OpenSpec valid; no live Telegram send occurred during that bounded matrix, which does not cover the separately disclosed 02:23:09 audit probe; `review-clean` certifies direct-sender logic at `b8e9cbe`, not the later final candidate | log/PR reference: `b8e9cbe`; Work Record `2026-07-11 — Morning Brief independent direct deadman transport` | by: Codex thread `019f4d75-69e9-7211-9e76-2942acb0d0e8` + independent reviewer `/root/final_logic_check` | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-11 | commands: immutable pre-fix replay of `dashboard/index.html` from `a9fadad` with a 50-hour raw age plus matching derived green flags; `node scripts/dashboard-render-smoke.js .`; `REPO_ROOT=$PWD bash scripts/dashboard.test.sh --require-shell`; `bash scripts/mission-control-common.test.sh`; `bash scripts/morning-brief.test.sh`; `git diff --check` | result: pre-fix returned `fresh` and exit 1 as expected; post-fix all 7 tabs and contradictory evidence regressions passed, dashboard `PASS=37 FAIL=0`, common `PASS=2 FAIL=0`, Morning Brief ALL PASS, and diff clean | log/PR reference: `4b25302`; Work Record `2026-07-11 — Morning Brief merge-gate round 8` | by: Codex thread `019f4d75-69e9-7211-9e76-2942acb0d0e8` | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-11 | commands: red-before-green targeted checks (`bash scripts/mission-control-common.test.sh`; `REPO_ROOT=$PWD bash scripts/dashboard.test.sh --require-shell`; `node scripts/dashboard-render-smoke.js .`) followed by cold matrix `rm -rf scripts/__pycache__ && bash scripts/chat-graph.test.sh && bash scripts/mission-control-common.test.sh && bash scripts/morning-brief.test.sh && bash scripts/morning-brief-delivery.test.sh && bash scripts/morning-brief-deadman.test.sh && REPO_ROOT=$PWD bash scripts/dashboard.test.sh --require-shell && node scripts/dashboard-render-smoke.js .`; plus `git diff --check`; `bash -n scripts/dashboard scripts/chat-graph scripts/morning-brief scripts/morning-brief-deadman scripts/dashboard.test.sh scripts/mission-control-common.test.sh scripts/morning-brief.test.sh scripts/chat-graph.test.sh`; post-closeout checks `openspec validate --all --json`, `git diff --check`, and a literal stale-text scan proving no third-auditor attribution, old idempotent chunk-delivery title, or retained-active P9 ledger text remains | result: pass after fixes; pre-fix regressions failed as expected (`OverflowError`, dashboard `PASS=35 FAIL=6`, render unknown warning failure); final cold chain passed (`chat-graph` ALL PASS, common PASS=2 FAIL=0, morning brief/delivery/deadman ALL PASS, dashboard PASS=37 FAIL=0, render smoke all 7 tabs); OpenSpec `morning-brief` valid=true and docs diff clean | log/PR reference: Work Record `2026-07-11 — Morning Brief merge-gate round 8` | by: Codex thread `019f5056-bc55-7043-88ad-b87b1ac48a6f`, governed by Claude `e85411a1-b74b-4556-9939-8eaf5d8f2ea9` | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-10 | commands: `bash scripts/morning-brief.test.sh`; `bash scripts/morning-brief-delivery.test.sh`; `bash scripts/morning-brief-deadman.test.sh`; `python3 -c py_compile scripts/morning-brief`; real `dashboard collect --force` + `morning-brief --send`; `launchctl bootstrap`/`kickstart`/`print` for both agents; deadman suppression run against a scratch `MISSION_CONTROL_HOME` | result: pass; all three suites `ALL PASS` cold (incl. new NEEDS YOU truncation fixture); brief `20260710-e0b7a9ca4b16` delivered 2/2 Telegram chunks with receipt `state=delivered`; `com.gillettes.morning-brief` and `com.gillettes.morning-brief-deadman` bootstrapped with fully-expanded paths and `last exit code = 0` (`delivery already complete` / `healthy`); suppression test `result=missing, alerted=true` exit 1; plists free of `~`/`__PLACEHOLDER__` | log/PR reference: Work Record `2026-07-10 — Morning Brief activation slice` | by: Claude Code headless worker (Opus 4.8) session `adc5fd6c-0fcf-4b77-b676-c22de3aba1a0` | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-11 | commands: `bash scripts/automation-status.test.sh`; `REPO_ROOT=$PWD bash scripts/dashboard.test.sh --require-shell`; `node scripts/dashboard-render-smoke.js .` | result: pass; automation ALL PASS, dashboard suite PASS=30 FAIL=0, and render smoke all 7 tabs | log/PR reference: Work Record `2026-07-11 — Morning Brief P9 job history slice` | by: Codex thread `019f4f3d-3034-7283-b63c-e4e8890b51f1` | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-09 | commands: final bounded Tier 2 cold matrix — graph, automation, dashboard shell/render, shared privacy, brief/delivery/deadman, coverage/extractor, decisions, runner, usage, scanner self-test, Python/shell/JSON/plist checks, strict OpenSpec, HOTL lint, `git diff --check`, and iterative independent counterexample replay | result: PASS; implementation `df991b4` pushed; dashboard `PASS=30 FAIL=0`, runner `PASS=32 FAIL=0`, all seven tabs render, and independent current-tree verdict `review-clean` with no P0–P3 findings | log/PR reference: `records/morning-brief-tier2-codex-audit.md`; `openspec/changes/morning-brief/verify.md`; commit `df991b4` | by: Codex thread `019f4963` with `/root/outcome_audit` and challenger | linear: repo-only.
- 2026-07-09 | commands: full Morning Brief cold matrix (`chat-graph`, `automation-status`, dashboard shell/render, usage, scanner self-test, shared egress, brief/delivery/deadman, coverage, decisions, runner), Python/shell/JSON/static checks, strict OpenSpec, HOTL lint, `git diff --check`, live parser-v5 migration, real rollback/canonical reinstall, LaunchAgent hash comparison, installed browser capture, and independent per-record/holistic/scoped re-audits | result: PASS for deterministic/Tier 1 implementation; dashboard `PASS=29 FAIL=0`, runner `PASS=32 FAIL=0`, all seven tabs rendered, prefix-only/full-token and contradictory-schedule negative controls passed, 23/23 real candidates correctly refused, normalized action-relevant Git facts unchanged, and no LaunchAgent installed | log/PR reference: `openspec/changes/morning-brief/verify.md`; `records/2026-07-09-morning-brief-orchestration-convergence.md`; `records/morning-brief-independent-codex-audit.md`; `/tmp/morning-brief-runner-live-dry-run.jsonl`; `tmp/playwright/morning-brief-final-*.png` | by: Codex thread `019f4963` and independent reviewers | linear: repo-only.
- 2026-07-09 | commands: Morning Brief delivery/privacy convergence — repeated `bash scripts/morning-brief.test.sh`; `bash scripts/morning-brief-delivery.test.sh`; `bash scripts/morning-brief-deadman.test.sh`; `bash scripts/mission-control-common.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `bash scripts/automation-status.test.sh`; `node scripts/dashboard-render-smoke.js .`; Python/Bash syntax; strict OpenSpec; HOTL document lint; `git diff --check`; independent adversarial replay after each fix | result: PASS; one local send per chunk under concurrent callers, compose/send coherence, same-day deadman, malformed timestamp matrix, stale/error overlay honesty, whole-message secret screening, cursor fail-close, content-free counters, and sensitive stderr rejection all pass; provider delivery remains at-least-once because provider acceptance before local receipt persistence is ambiguous; dashboard `PASS=26 FAIL=0`; independent final verdict review-clean for that historical SHA | log/PR reference: `records/2026-07-09-morning-brief-delivery-privacy-audit.md`; Work Record `2026-07-09 — Morning Brief resumable delivery and privacy convergence` | by: Codex thread `019f4963` plus reviewer `/root/mission_mapping` | linear: repo-only.
- 2026-07-09 | commands: `bash scripts/mission-control-common.test.sh`; `bash scripts/chat-graph.test.sh`; `bash scripts/automation-status.test.sh`; `bash scripts/morning-brief.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js`; `node scripts/dashboard-render-smoke.js .`; `bash scripts/scan-unfinished-work --self-test`; strict OpenSpec and HOTL validation; `git diff --check`; browser captures of `file:///tmp/mission-control-morning-brief/index.html` Home and `#brief` | result: pass; privacy matrix, graph v5 migration/open-delta suite, distinct automation history, deterministic composer, dashboard PASS=22 FAIL=0, all 7 render tabs, scanner self-test, schema/workflow validation, whitespace, and nonblank Home/Brief screenshots passed | log/PR reference: checkpoint `fce2a7a`; `/tmp/morning-brief-home.png`; `/tmp/morning-brief-tab.png`; Work Record `2026-07-09 — Morning Brief trusted substrate and deterministic thin brief` | by: Codex thread `019f4963-1e75-7600-8a17-1e6f6f8e8ca6` | linear: repo-only; no Mission Control Linear team is configured.
- 2026-07-08 | commands: `bash scripts/chat-graph.test.sh`; `bash scripts/automation-status.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js`; `node scripts/dashboard-render-smoke.js .`; `bash scripts/usage-snapshot.test.sh`; `bash scripts/scan-unfinished-work --self-test`; `python3 -m json.tool dashboard/fixtures/chats.json`; `git diff --check`; `DASHBOARD_NO_OPEN=1 bash scripts/dashboard demo`; browser capture of demo Map with local vendor copied into the demo state | result: pass; graph suite ALL PASS, automation ALL PASS, dashboard suite PASS=22 FAIL=0, render smoke all 6 tabs, usage PASS=2 FAIL=0, scan self-test PASS, JSON/syntax/whitespace checks pass, browser screenshot nonblank at `/tmp/mission-control-map-journal-polish-with-graph.png` | log/PR reference: Work Record Log `2026-07-08 — Map journal action polish` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-08 | commands: `bash scripts/chat-graph.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js && node scripts/dashboard-render-smoke.js .`; `python3 -m py_compile scripts/chat-graph`; `git diff --check` | result: pass; graph suite ALL PASS with severity-rule regression, dashboard suite PASS=22 FAIL=0, render smoke all 6 tabs, syntax/whitespace checks pass | log/PR reference: Work Record Log `2026-07-08 — Audit-loop severity rules` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-08 | commands: `bash scripts/chat-graph.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js && node scripts/dashboard-render-smoke.js .` | result: pass; graph suite ALL PASS, dashboard suite PASS=22 FAIL=0, render smoke all 6 tabs after the temporary-hide wording and source-backed reappearance regression | log/PR reference: Work Record Log `2026-07-08 — Audit-loop wording polish` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-08 | commands: `bash scripts/chat-graph.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js && node scripts/dashboard-render-smoke.js .`; `bash scripts/automation-status.test.sh`; `bash scripts/usage-snapshot.test.sh`; `bash scripts/scan-unfinished-work --self-test`; `bash -n scripts/dashboard scripts/usage-snapshot scripts/scan-unfinished-work scripts/chat-graph.test.sh scripts/dashboard.test.sh scripts/usage-snapshot.test.sh scripts/automation-status.test.sh`; `python3 -m py_compile scripts/chat-graph`; `python3 -m json.tool dashboard/fixtures/chats.json`; `git diff --check`; `/Users/gillettes/.codex/scripts/ai-browser-runtime.sh ensure`; `MISSION_CONTROL_HOME="$HOME/.mission-control" REPO_ROOT="$PWD" bash scripts/dashboard install`; `MISSION_CONTROL_HOME="$HOME/.mission-control" "$HOME/.mission-control/bin/dashboard" collect --force`; browser captures for installed Home/Map/Open work | result: pass; graph suite ALL PASS, dashboard suite PASS=22 FAIL=0, render smoke all 6 tabs, automation ALL PASS, usage snapshot PASS=2 FAIL=0, scan self-test PASS, syntax/json/whitespace checks pass, browser screenshots nonblank at `/tmp/mission-control-p13-home.png`, `/tmp/mission-control-p13-map.png`, `/tmp/mission-control-p13-open-work.png` | log/PR reference: Work Record Log `2026-07-08 — One loose-ends ledger` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-04 | commands: repeated `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; repeated `node scripts/dashboard-render-smoke.js .`; repeated focused `code-review` subagent audits of `dashboard/index.html`, `dashboard/fixtures/usage.json`, and `scripts/dashboard-render-smoke.js` while fixing the audit findings | result: pass; dashboard suite stayed PASS=22 FAIL=0, render smoke stayed green across all 6 tabs, and the separate review passes ended with no material findings left in the changed dashboard bundle | log/PR reference: Work Record Log `2026-07-04 — Dashboard search-audit V1 UI bundle`; Audit Record `2026-07-04 | targeted implementation audit` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-04 | commands: GitHub search API sweep of top 1000 rows for `dashboard coding tracker in:name,description,readme stars:>10`; README/license/metadata deep-read for high-fit candidates; README screenshot/asset-reference reviews for CodeBurn, TokenTracker, CodexBar, AgentsView, abtop, and builderz Mission Control; `git diff --check -- records/2026-07-04-dashboard-coding-tracker-search-audit.md todo.md` | result: pass for source-only audit and docs whitespace check; no runtime code changed | log/PR reference: Work Record Log `2026-07-04 — GitHub dashboard/coding-tracker search audit`; record `records/2026-07-04-dashboard-coding-tracker-search-audit.md` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-04 | commands: `bash scripts/chat-graph.test.sh`; `bash scripts/usage-snapshot.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js && node scripts/dashboard-render-smoke.js .`; `bash scripts/automation-status.test.sh`; `bash scripts/scan-unfinished-work --self-test`; `bash -n scripts/dashboard scripts/usage-snapshot scripts/scan-unfinished-work scripts/chat-graph.test.sh scripts/dashboard.test.sh scripts/usage-snapshot.test.sh`; `python3 -m py_compile scripts/chat-graph`; `git diff --check`; token-pattern diff scan; `MISSION_CONTROL_HOME="$HOME/.mission-control" REPO_ROOT="$PWD" bash scripts/dashboard install`; `MISSION_CONTROL_HOME="$HOME/.mission-control" "$HOME/.mission-control/bin/dashboard" collect --force`; browser capture of installed Home | result: pass; chat graph ALL PASS, usage snapshot PASS=2 FAIL=0, dashboard suite PASS=22 FAIL=0, render smoke all 6 tabs, automation ALL PASS, scan self-test PASS, installed Home screenshot nonblank at `/tmp/mission-control-207d88dd-closeout-home.png` | log/PR reference: Work Record Log `2026-07-04 — 207d88dd Cursor patch closeout`; closeout commit 3a612e5 | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-04 | commands: `node --check scripts/dashboard-render-smoke.js && node scripts/dashboard-render-smoke.js .`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `git diff --check`; `MISSION_CONTROL_HOME="$HOME/.mission-control" REPO_ROOT="$PWD" bash scripts/dashboard install`; browser capture of installed Map | result: pass; render smoke proves Map includes newest chat title, dashboard suite PASS=21 FAIL=0, screenshot written to `/tmp/mission-control-map-journal.png` | log/PR reference: Work Record Log `2026-07-04 — Map recent chat journal` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-04 | commands: `bash scripts/chat-graph.test.sh`; `bash scripts/automation-status.test.sh`; `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell`; `node --check scripts/dashboard-render-smoke.js && node scripts/dashboard-render-smoke.js .`; repo governance validators; browser captures for Home/Map/Chats | result: pass; dashboard suite PASS=21 FAIL=0, render smoke all 6 tabs, governance validators pass after thin-root note fix | log/PR reference: local images `/tmp/mission-control-home-audit.png`, `/tmp/mission-control-map-audit.png`, `/tmp/mission-control-chats-audit.png` | by: Codex | linear: self-contained until Linear is configured.

## Testing Cadence Matrix
| Trigger | Command(s) | Cadence | Gate Criteria |
|---|---|---|---|
| Chat graph logic change | `bash scripts/chat-graph.test.sh` | Per change | Tests pass and export/doctor behavior is covered |
| Automation status or job registry change | `bash scripts/automation-status.test.sh` | Per change | Tests pass and registry states remain honest |
| Dashboard collector/install/status change | `REPO_ROOT="$PWD" bash scripts/dashboard.test.sh --require-shell` | Per change | Shell, fixtures, status, install, and render smoke pass |
| Dashboard renderer or fixture change | `node scripts/dashboard-render-smoke.js .` | Per change | Every tab renders fixture content; Map builds graph elements |
| Any non-trivial release or merge closeout | `scripts/verify.sh` | Per closeout | Every committed suite, real-browser gate, scanner self-test, strict OpenSpec, and syntax check passes |
| Usage snapshot logic change | `bash scripts/usage-snapshot.test.sh` | Per change | Credit notification command handling remains argv-only and advice is delivered as one message |
| Release-sensitive visual/file:// change | `node scripts/dashboard-browser.test.js` plus a capture of installed or demo output | Per change | All seven tabs are console-clean and nonblank at desktop/mobile widths; no document overflow; Map canvas, keyboard controls, contrast, and truthful clipboard failure pass |

## Feedback Decision Log
If it's not here, it isn't remembered.
Record outside feedback and the resulting reasoning once, then update the same entry as the decision evolves.
- Each entry should capture:
  - `date`
  - `feedback source`
  - `feedback summary`
  - `evaluation chat`
  - `reasoning response`
  - `decision status` (`accepted`, `partial`, `deferred`, `rejected`, or `superseded`)
  - `implementation/disposition chat`
  - `linked branch / audit / suggestion / test evidence`
  - `by`
  - `linear`
- Reuse or update an existing entry when the same feedback thread comes back instead of opening duplicate records.
- 2026-07-13 | feedback source: Codex audit `019f5bea-c97f-7263-bd2d-ef07aa68205b` | feedback summary: correct the source chat's actual `gpt-5.6-sol`/high provenance and explicitly supersede the premature complete/no-defect closeout using later counterevidence; then stop at Trevor's safe boundary | evaluation chat: Codex `019f59f8-bb9e-70c0-9497-a9686ea24154` | reasoning response: accepted; exact raw turn contexts and final were verified, later repairs were matched to commits, the hung browser/full verifier was interrupted without opening a new lane, and the goal remains active | decision status: accepted | implementation/disposition chat: same source chat | linked branch / audit / suggestion / test evidence: stop-boundary addendum, Work/Audit/Test records dated 2026-07-13 | by: Codex | linear: repo-only.
- 2026-07-04 | feedback source: Trevor in Codex thread `019f2b73-9811-7392-b511-201c1f109997` | feedback summary: connected-chat view should include a tiny journal/log of what was done and which chat it happened in | evaluation chat: same Codex thread | reasoning response: accepted; implemented from existing chat-feed timestamps/titles instead of adding a new collector | decision status: accepted | implementation/disposition chat: same Codex thread | linked branch / audit / suggestion / test evidence: Work Record `2026-07-04 — Map recent chat journal`; Test Evidence `2026-07-04` | by: Codex | linear: self-contained until Linear is configured.
- 2026-07-05 | feedback source: Trevor pasted a GitHub Copilot third-party-repo recommendation for this repo and asked which suggestions to agree with and which could actually be implemented | feedback summary: Copilot — working only from the repo description, not its contents, which it openly admitted — recommended an enterprise observability + data-warehouse + business-intelligence stack (OpenTelemetry Collector, Prometheus, Grafana, OpenObserve, Superset, Metabase, Airbyte, Meltano, dbt-core, Great Expectations, TensorZero, Helicone, OpenLIT, traceAI, TraceRoot, a Pull Request Analytics GitHub Action, onWatch, ECharts/Chart.js) plus a 4-phase "adopt Collector+Prometheus+Grafana+OpenLIT+dbt" plan | evaluation chat: this session (full adversarial review of pasted AI output) | reasoning response: category error against PROJECT_INTENT. One test disposes of the headline list — does it run as a background service/framework/separate warehouse, or is it a vendorable file the page loads offline? OTel Collector, Prometheus, Grafana, OpenObserve, Superset, Metabase, Airbyte, Meltano, dbt-core, Great Expectations, and TensorZero all fail it and collide with the explicit non-goals (no server, no framework rebuild, single-user, offline/file://). Copilot's Phase-1 (Collector+Prometheus+Grafana) would replace the double-clickable offline file with a multi-service ops stack — a different product, not a heavier version of this one. LLM-observability picks (Helicone/OpenLIT/traceAI/TraceRoot) require SDK/proxy instrumentation that cannot attach to first-party Claude Code/Codex/Cursor and reintroduce the rejected proxy/cookie surface. TensorZero is an LLM gateway/router, violating the passive read-only design. Great Expectations duplicates existing feed-boundary validation + `dashboard-render-smoke` at far heavier scale. What actually transfers is only: (a) the category instinct that token/session/cost observability is the right neighborhood — already better mapped by the 2026-07-04 dashboard-coding-tracker audit; (b) vendor-neutral normalized ingestion — already implemented as feed envelopes; (c) "cost per merged PR"-style metrics — already queued as the V2 yield lens; none require new infrastructure. onWatch was already triaged in the prior audit (GPL, pattern-only); no other Copilot pick improves on that audit's peer set. `leeoniya/uPlot` (tiny zero-dependency MIT canvas charting) is noted only as the right-sized counter-example to Copilot's Chart.js/ECharts/CDN pick if a chart ever earns its place; it still fails this repo's "no new dependency unless it reduces local-operability risk" bar for V1 | decision status: rejected (adopt none of the recommended tools) with partial value captured — category instinct and two ideas already reflected in the ER-089 usage-routing and V2 yield-lens queue | implementation/disposition chat: this session; nothing adopted, installed, or wired | linked branch / audit / suggestion / test evidence: `records/2026-07-04-dashboard-coding-tracker-search-audit.md` (the correct same-niche map); Suggested Recommendation Log 2026-07-05 | by: Claude Code (Opus 4.8) session `a9724039-6595-4205-a25b-bf361020250a` | linear: self-contained until Linear is configured.
