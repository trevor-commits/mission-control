# LINEAR

## Local mode

Current recorded mode: `repo-only`. No live Linear team or prefix is verified for this repository. Do not invent issue IDs or ask for an integration just to satisfy a template.

Use `todo.md` `## Active Next Steps` for unfinished accepted work. Existing branch, issue, and testing ledgers remain available when relevant.

Use `self-contained:` when an issue reference is needed for repository-only work. Deliberately rejected work can use `no-action:`.

If a task already has a verified external issue, keep that issue and its repository record aligned when its state changes. New integrations or external issue creation require task-specific authorization.

The former per-task Linear-coverage and combined closeout gates are retired in the shared-entry reset. This file is not a startup reading requirement.
