# 2026-07-18 — Phase 0 rollup-answer CLI work record

- Problem: Mission Control needed one operator answer to reach only strictly equivalent rollup members while leaving each member open until verified owner consumption. Six independent audit rounds then exposed publication, replay, runtime, receipt, canonical-entry, and presentation boundaries that declared suites did not cover: missing receipt identity fields, receipt-backed symlink recovery, quarantine name-swap rollback, and a global Home headline derived only from decisions were the final frozen-head findings.
- Reasoning: Answering is durable operator direction, not completion. The database receipt must bind the exact held batch entry independent of invocation lifecycle; a delivered-notification receipt must contain and match full Markdown, deterministic chunking, every ordered chunk digest, and its own immutable bytes; one public transaction must use one runtime across every local read surface; global `Needs you` must reflect combined attention while bounded rows preserve actionable controls; and failure cleanup must reconcile descriptor-pinned and path-visible entries without following symlink targets or mutating orphan first-answer conflicts.
- Diagnosis inputs: approved seven-point OpenSpec/HOTL contract; `scripts/queue_admission.py`, `scripts/decision-alert`, `scripts/compose-decision-prompt.py`, `scripts/dashboard`, Home/panel/Morning Brief consumers; independent Codex audits `019f762c-c815-77b3-97c0-021c66fd3b7e`, `019f7680-90ce-7392-a991-5a76a3bae61b`, and `019f77f3-4975-7f51-b296-fbdc2dbd3d47`; upstream Claude audit `6b78e170-f063-47e2-92f8-48e6f5ce0600`.
- Approval provenance: `thread_goal_updated` at `2026-07-18T14:47:59.770Z` on Codex thread `019f73d8-e5dc-73a0-acc5-8a4916ac6819`, with goal text beginning “Yes thoroughly Approve the seven-point answered_pending contract and resume Lane D.”
- Implementation inputs: commits `754de93`, `ed8ce35`, `953de86`, `daa8c72`, first audit repair `34687c9`, records head `708031f`, second audit repair `8613d25`, occupied-parent repair `bfaf10b`, rejected fourth-audit head `af083a6`, local-view repair `0ce6d3d`, rejected fifth-audit receipt head `8b8fa77`, rejected sixth-audit head `0bf1c69`, and sixth-audit boundary repair `78672c46d94041f974ca97b0d2cfe5596c6b020a`; OpenSpec change `rollup-answer`; temporary Mission Control homes/stores and fake sender/collector fixtures only.
- Fix: added current-fingerprint immutable `answered_pending`, strict targeting, exact per-member consumption, deterministic private batches, persisted canonical manifest digest, fd-bound pre/post-commit verification, exact held-entry quarantine during publication/replay, directory/regular-file/symlink current-canonical recovery, verified rollback for a quarantine name swap, public `answer-rollup`, actionable-first pending-aware presentation, a combined-attention Home H1, and a same-runtime strict/no-egress success transaction spanning decisions, persisted Morning Brief, and public brief feed. Local refresh requires delivered Markdown/chunk/receipt identity fields before changing a delivered local view, preserves delivered receipt/cursor bytes, and fails closed without rewriting in-flight delivery content across a day rollover. The sixth repair also closes a live macOS gap: Python's `os` module does not expose `O_SYMLINK` or `O_PATH`, so the symlink-quarantine branch fell back to the raw Darwin constant `O_SYMLINK = 0x00200000` (confirmed in the macOS 26.5 SDK header `sys/fcntl.h`) before the Linux `O_PATH` branch; platforms with neither flag fail closed.
- Self-audit:
  - method: red-before-green behavior probes, focused rollup suite, authoritative `scripts/verify.sh`, strict OpenSpec, Python/macOS Bash 3.2 syntax, browser 254, source-artifact and diff checks, plus independent same-model audit.
  - outcome: first repair suite `14/14` and authoritative verifier `23/0`; second repair suite `17/17`, all seven render tabs, ER-134 `59/0`; third repair rollup `18/18`; exact records-complete `bc9014d` passed rollup `18/18` plus authoritative `23/0`; local-view repair `0ce6d3d` passed rollup `23/23` plus authoritative `23/0`; receipt/entry repair `c0d0a53` passed rollup `25/25` plus authoritative `23/0`. The sixth audit's four findings reproduced RED at `0bf1c69`; a live macOS defect (missing `O_SYMLINK` in Python's `os`) was also reproduced RED before the repair. Exact sixth-repair head `78672c46d94041f974ca97b0d2cfe5596c6b020a` passes targeted 4/4, rollup `29/29`, browser `254`, and the authoritative `SUITES PASS=23 FAIL=0` gate live on macOS 26.5. Every accepted audit code defect has a regression; no schema migration or dependency was added.
  - did not verify: a fresh independent (same-model/max) frozen-head audit at `78672c4`; hosted PR checks, merged-main behavior, install/deploy, provider delivery, or live-store behavior because those remain pending or explicitly prohibited.
- Ripple Check: reconciled production writer/composer/dashboard behavior with rollup OpenSpec, HOTL, `STATE.md`, todo Work/Test/Feedback/Branch ledgers, evidence, audit record, and PR packet; project intent and repo-only Linear posture remain unchanged. This repository has no separate `PROJECT_MEMORY.md` convention; `todo.md` is its declared operational-memory surface.
- by: Codex `019f73d8-e5dc-73a0-acc5-8a4916ac6819`; sixth-repair authoritative-gate run and receipt by Hermes session `20260718_221452_eb1c35`.
- triggered by: Phase 0 overnight packet plus Trevor's explicit answered-pending approval and independent-audit corrections.
- led to: authoritative-green sixth-audit repair head `78672c4` awaiting a new frozen-head same-model/max audit, then branch-only PR closeout; no main/live action.
- linear: self-contained; Mission Control remains repo-only because no team/prefix is configured.
