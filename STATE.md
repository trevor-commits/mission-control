# STATE — generated current state

> Generated by the closeout of the 2026-08-24 audit-backlog implementation.
> Historical lane records live in `records/` and dated files under `records/evidence/`.
> This file is a snapshot, not an append-only log.

## Branch and install

- Branch: `claude/silence-is-a-state`. PR #20 is open against `main`.
- Live installation: `~/.mission-control`, refreshed from this branch's verified head.
- Canonical verifier: `PYTHONDONTWRITEBYTECODE=1 /bin/bash scripts/verify.sh`.

## Implemented backlog (2026-08-24/25)

The 2026-08-23 bot-portfolio implementation extends point 5 without rewriting its historical approval: graph-verified owner evidence now records `consumed`, not terminal resolution. The exact deterministic lifecycle is `answered_pending -> delivered -> consumed -> running -> live_result_verified -> closed`; only an evidence-bearing `closed` transition changes the compatibility queue state to `resolved`. Consumption proof is current-fingerprint and current-delivery bound, graph watcher events carry deterministic identity plus `resolved_at` and `source_id`, every lifecycle event has a stable source, and status separates exact lifecycle counts from labeled compatibility counts. Former public rollup receipts with `source=""` remain invalid until exact replay verifies their current generation, event set, immutable source-less manifest, and artifact bytes, then appends source-bound successor events without rewriting historical rows or batch bytes.

Transition replay is bound to the current answer generation. Rollup batch identity includes each member's immutable evidence-activation event. Together, these rules prevent a returning fingerprint from adopting an older generation's receipt.

## Current implementation
| Area | Change | Verification |
| --- | --- | --- |
| Self-repair (T1) | Plan from shared `feed_health`. Malformed and future timestamps are repair candidates. Strict collectors. Retry budget with give-up reporting. Post-repair re-read decides success. Nonzero rc on failure. | self-repair self-test 19/19 |
| Install and rollback (T2) | Verified installs snapshot to `releases/<id>` plus `last-known-good.json`. `dashboard rollback` restores bytes and re-stamps truthfully. Pruning caps history at three. | dashboard.test.sh 93 PASS |
| Attestation (T3) | Stamp schema 2 attests rendered plists plus loaded `launchctl print` ProgramArguments digests plus panel source/binary/app-binary hashes. Unattested argv fails install. | dashboard and common suites |
| Brief delivery (T4) | Owner-aware advisory-lock lease replaces the mkdir lock. Ambiguous send timeout requires explicit reconciliation (`--reconcile`, schema-2 receipts). The lock file is retained by design. | delivery suite green |
| Usage attention (T5) | Locked read-modify-write state. Watermark advances transactionally with attention sync and never regresses. | usage-watch self-test 21/21 |
| Chat retention (T6/T7) | `chat-graph prune` rolls up superseded lanes with VACUUM and a meta roll-up counter. Doctor reports the size band. Export projection capped so newest cards win with a truthful note. | chat-graph suite green |
| Cached rows (T8) | GLM cache filter rejects hostile numerics and enums fail-closed. | usage-snapshot suite |
| Verifier matrix (T15) | Auto-discovered Python and shell syntax checks. Mandatory ShellCheck. Python floor 3.11. Self-repair self-test gating. Cap fixture exercises configuration rather than the default. CI prerequisites test. | verify.sh |
| Decision bounds (T13) | Bounded subprocess timeouts and output caps for decision-alert. Bounded flock waits map busy locks to exit 75. | decision-bounds 4/4 |
| Home expansion (T19) | `MISSION_CONTROL_HOME` supports `~` expansion everywhere. A failed fixture cat is a failed strict collection. Demo projection parity. | home and demo tests |
| Panel liveness (T11) | mc-panel writes a heartbeat. Jobs registry freshness tightened from 604800s to 3600s with identity fields. Registry schema test added. | mc-panel suites plus jobs-registry test |
| Logs (T17) | self-repair report.jsonl trimmed to the newest 2000 lines after each append. | self-repair self-test |
| CI governance (T12/T18) | Actions SHA-pinned. Concurrency cancel. Vendor hash lock committed and checked in CI and the verifier. Dependabot configured. GitHub ruleset `main-protection` (id 21411315) active: blocks force-push and deletion, requires the `verify` check. | gh api readback |
| Mobile 320px (T16) | `.mc-cmd` no longer forces a 320px width. Browser suite now covers 320px on every tab. | dashboard-browser 455 assertions |

- `scripts/decision-alert` derives pending and follow-through state from immutable current-fingerprint events. It exact-binds every transition to decision, resolution key, stable producer event, evidence, outcome, source, and predecessor; skipped states and cross-target replay fail closed. Single and rollup answers both begin non-terminally, and only `closed` sets the compatibility queue state to `resolved`.
- `scripts/compose-decision-prompt.py` validates before paths, creates deterministic rollup bytes, retains pinned descriptors, verifies name-to-fd and member bytes before/after commit and publication, quarantines the exact receipt-bound held artifact on first publication/replay failure, and fd/inode-binds either a directory or regular-file canonical conflict before private quarantine; orphan first-answer conflicts remain untouched.
- `scripts/dashboard` exposes public single/rollup answer commands and treats strict decisions, derived attention, persisted Morning Brief, and strict public brief feeds as one same-`SCRIPT_DIR` success boundary with `DECISION_ALERT_AUTO=0`; committed-but-refresh-failed is nonzero with receipt stdout and explicit degraded stderr.
- Morning Brief omits exactly current pending targets from `NEEDS YOU`; its local-only refresh preserves the established delivery receipt, cursor, and retry-byte boundaries. Home and panel show answer-recorded, delivered, consumed, running, and live-result-verified separately while preserving read-only ordering and combined non-decision attention.
- `scripts/rollup-answer.test.py` covers 32 temporary-state contracts, including write-before-validation, whole-value identifiers, ambient test controls, replay mutation/parent replacement, occupied canonical names, quarantine rollback, exact refresh through attention and Morning Brief, receipt validation, single-answer parity, and fake-sender no-egress proof.
## Known follow-ups (operator choices)

- GitHub conversation-resolution gates and PR merge-queue settings remain operator choices in repo settings.
- `todo.md` remains the append-only operational log. Dated evidence lives under `records/`.
